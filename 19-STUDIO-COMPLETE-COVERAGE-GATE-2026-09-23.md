# Micro studio — complete coverage gate for the color and visual revision

**Authority:** Read `17-COLOR-DECISION-2026-09-23.md` for the palette and `18-ZAI-STUDIO-REVISION-BRIEF-2026-09-23.md` for the revised compositions and functional fixes. This file defines what **complete coverage of the already implemented studio** means. It does not authorize changing production Micro or designing all future screens. Status before ZAI revises and verifies the source: `NOT STARTED`.

## 1. Inventory the real implementation before changing it

Create `interactive-design-studio/reports/REVISION-COVERAGE-LEDGER.md`. For each existing row below, and every additional rendered item discovered in the source, record: `ID | source path | rendered use/routes | variants and state fixtures | old color or visual pattern | new token/visual decision | inspection URL or interaction | evidence filename | result PASS/FAIL/UNVERIFIED/NOT IMPLEMENTED | residual issue`. Begin with the current `COMPONENT-CATALOG.md`, `SCREEN-CATALOG.md`, `STATE-MATRIX.md`, source imports/exports and actual browser output. A table in documentation alone is not evidence that source was updated.

| Coverage ID | Mandatory coverage of existing source and rendered UI |
|---|---|
| FOUNDATION | `src/foundations/{tokens.css,tokens.ts,reset.css,typography.css,motion.css}`; all primitive, semantic and component roles, dynamic CSS properties, contrast pairs, non-text boundaries, canvas and typography color; synchronize CSS and TS. |
| CORE | `components/core/{Button,Field,OpenRow,Overlays,SegmentedControl,Skeleton}.tsx`, `components/core.css`: Button and IconButton, text/amount fields, OpenRow and RowGroup, Sheet, Dialog, AnchoredMenu, SegmentedControl and Skeleton/StructuralLoad; inspect default, selected, pressed, focus, error, loading, disabled and open/closed **where actually implemented**. |
| FINANCIAL | `components/financial/{MoneyValue,TruthNote,SnapshotDeck,TransactionRow,ResultBlock,ImpactPreview}.tsx`, `components/financial.css`: all money truth and save states, partial/unknown, profit/loss, card positions/overflow, local/pending, expanded notes and impact. Amount and `د.أ` remain together. Check visual composition, not only token references. |
| CONTEXT | `components/contextual/{MicroSignal,Contextual}.tsx`, `components/contextual.css`: MicroSignal, ContextTrace, ContextSeam, SystemRibbon, RecoveryStage, EmptyState and Insight, with their current variants. |
| ICONS | `components/icons/{Icon,phosphor.generated}.ts[x]` and icon consumers: active/disabled/focus/currentColor/fills/strokes plus directionality, inherited SVG colors, and meaningful text alternatives; do not edit generated sources without reconciling their generator. |
| NAV | `navigation/{AppHeader,BottomNav,navModel}.ts[x]`, `navigation.css`, `app/{AppFrame,routes}.tsx`, `App.tsx`: edge-to-edge header and status area, account panel and logout dialog, five tab targets, selected/unselected badges, safe area, route changes, overlays and return/swipe behavior. |
| SCREENS | `screens/{OvrNow,OpsSaleCreate,FinOverview,DeferredScreen}.tsx`, `screens.css`: all rendered content, critical questions, page compositions and genuine deferred placeholders. The three implemented screens need purpose-specific composition, not a uniform recolor. |
| FIXTURES | `fixtures/` and `src/fixtures/`, `src/states/`: preserve Jordanian Arabic/English digits and truthful financial meaning; inspect every scenario rendering, scenario switches, default values, error/recovery, unknown and offline. Do not change fake financial data merely to produce nicer graphics. |
| STUDIO | `studio/{StudioShell,urlState}.tsx`, `studio.css`: all visible preview controls, selectors, labels, current/selected colors, device chrome, comparison tools, deep links, width/zoom/gray/reduced-motion switches and review-only UI. Make review chrome visibly distinct from the phone interface. |
| SURFACES | All remaining rendered assets and CSS: imported SVG/image assets, inline styles, pseudo-elements, CSS variables and opacity mixes, decorative strokes, selection/focus rings, shadows and chart colors if any chart actually exists. Expand the ledger for a newly found item instead of ignoring it. |

**Screen-state scenarios to visit at least once:** `OVR-NOW`: `complete`, `partial`, `insufficient`, `first-use`, `offline`, `error`; `OPS-SALE-CREATE`: `empty`, `filled`, `validation`, `impact`, `impact-credit`, `saving`, `failure`, `success`, `offline-save`; `FIN-OVERVIEW`: `complete`, `partial`, `insufficient`, `zero`, `negative`, `offline`. Also visit global header/account panel, five tab destinations, current `OPS-HOME`, `TOOL-HOME`, `MKT-HOME` deferred placeholders, filters/period menu, sheets/dialog, SnapshotDeck positions and studio controls. If the actual source exposes more variants, add them. Do not imply that a deferred page has been product-designed because its placeholder receives the new palette.

## 2. Migration method

1. Record the real inventory and all active legacy roles first. Make the approved `#D97757` the brand lead, then implement the complete role palette from 17 across foundation tokens and the rendered component variants; document any justified baseline adjustment. Remove unused legacy color primitives and inactive alias names from **current** code/tokens. Keep historical commit data and clearly dated before-screens as history only.
2. Recompose repeated layouts where the new palette reveals the existing template pattern. Explicitly inspect the three screen-specific faults in 18. Check large color masses, rhythm, financial hierarchy, spacing, Arabic line breaks, touch states and transitions. A global find/replace is insufficient.
3. Make every implemented component and variant inspectable: link it to a scenario or add a small, clearly labeled **studio-only component review**. This is for QA and does not create a new Micro product screen. Show typography/color/interaction variants and their contrasting surfaces, not a decorative swatch wall. Record unreachable variants as failures until exercised.
4. Regenerate the living deliverables after the code change: `TOKEN-DICTIONARY.md/.json`, `DESIGN-SYSTEM.md`, `COMPONENT-CATALOG.md`, `SCREEN-CATALOG.md`, `STATE-MATRIX.md`, `NAVIGATION-MAP.md`, `ASSET-MANIFEST.md`, `reports/{CONTRAST-REPORT,QA-SUMMARY,VISUAL-PROOF-REVIEW,DELIVERY-REPORT,OPEN-ISSUES}.md`, and all other **current** descriptions whose claims changed. Update `evidence/README.md` and create new screenshots from the running revision. Archive or label superseded screenshots; a before-image must never be labeled after-evidence. Update applicable root delivery/manifest references truthfully without modifying approved unrelated decisions.
5. Report source paths and ledger row IDs in the delivery report so a reviewer can tell what was changed and verify no current implemented surface was missed.

## 3. Acceptance checks — all required before “Ready for Owner Review”

| Check | Passing evidence |
|---|---|
| Active-color audit | Search the **entire active studio source, tokens and current generated documentation** for old Indigo/Citrus HEX, names and derived roles (`#4F46E5`, `#3F37C9`, `#E8E7FF`, `#D9F43B`, `#F3FBCB`, `#C45F46`, `Indigo`, `Citrus`, case-insensitive); document justified historical references outside active UI. Search for inline HEX, RGB/HSL, alpha/opacity and CSS color mix that could retain the old look. Verify CSS variables and TS definitions agree. |
| Component proof | Every already implemented row from `COMPONENT-CATALOG.md` plus newly found items has an updated ledger entry with a live interaction or fixture and inspectable evidence. Record both visual and interaction result; `UNVERIFIED` cannot count as `PASS`. |
| State and route proof | Visit all 21 listed screen scenarios, all current tab destinations and global overlays, including non-default, offline, error and deferred placeholders. Check no state reset leaks, false swipe prompts, dead “عرض الكل”, inert period/filter or dead modal actions. Document source/route, screenshot or short interaction proof and outcomes. |
| Device/readability proof | Review 320/360/390/412 widths, 100/150/200% text where controls support it, RTL, amount+`د.أ`, grayscale, reduced motion, focus and touch/keyboard alternatives. Report clipping and overflow per affected component. A sampled representative combination is acceptable; do not claim the full Cartesian product was tested. |
| Contrast proof | Recompute actual foreground/background and adjacent non-text pairs including selected, focused, pressed, disabled and overlay surfaces; state criterion, ratio, result and source location. Fix failures, and distinguish WCAG minimum from comfortable reading. |
| Build and trace | Run `npm ci` and `npm run build` from `interactive-design-studio/`, browser walkthroughs on new code, regenerate legible Arabic screenshots, verify report numbers against actual artifacts, record commit and working preview instructions. No build result or old report can stand in for a browser review. |

The agent must list a **specific blocker and affected rows** if any required check cannot pass. It may report work in progress and ask for help with a genuine environment blocker; it must not call a partial recolor a completed revision. Real-device Android, TalkBack and Jordanian-user research remain `UNVERIFIED` unless actually performed.

## 4. Handoff status

Complete revision means implemented-source update **plus** ledger, reviewable scenarios, working interactions and evidence linked to the new commit on `exec/interactive-design-studio-zed-20260923`. Owner approval happens **after** the owner reviews the updated studio. The existing production Micro repository is outside scope.
