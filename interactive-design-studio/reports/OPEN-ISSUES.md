# OPEN-ISSUES — القضايا المفتوحة بعد مراجعة V2 (2026-09-23)

تُحدَّث هذه القائمة بقرار المالك. لا تعني `Resolved` أن `Accepted`.

## قضايا تنتظر قرار المالك (مراجعة النسخة المراجَعة)

| ID | القضية | الأثر | التوصية |
|---|---|---|---|
| ISS-001 | حكم النسخة المراجَعة V2 (الألوان + التكوينات الجديدة للشاشات الثلاث + العرضان الجديدان): قبول كمرجع أم تعديل | يحدد التعميم على بقية الشاشات | مراجعة على الهاتف عبر شريط الأدوات ثم قرار موثق |
| ISS-002 | سهم التقدم (caret-left) في أزرار الإجراءات: قرار معتمد باتباع RTL، قد يبدو ملتبسًا لبعض المراجعين | P3 بصري | إن ألتبس: نقاش Proposal وليس تغييرًا صامتًا |
| ISS-003 | وسم «قيم عرض من Fixtures…» داخل قسم أثر العملية | شفافية ضد الالتباس مقابل نقاء الواجهة | إبقاؤه حتى وجود منطق منتج حقيقي يستبدل قيم العرض |
| ISS-004 | بند الاستضافة الدائمة للمعاينة: لا يوجد نشر مصرح خارج المستودع | بند تسليم | تشغيل محلي من README + معاينة جلسة مؤقتة؛ النشر يحتاج تفويضًا مستقلًا |
| ISS-005 (جديد) | `OVR-SNAPSHOT-ALL` نفذ كعرض استوديو حقيقي (بطاقات §13.4 نفسها ممددة) بأمر 18 §2 — لكن تصميمه كشاشة منتج كاملة (رؤوس/مجموعات/فلاتر) لم يُقرر بعد | تعريف حدود الشاشة عند التنفيذ | قرار المالك عند مراجعة V2 |
| ISS-006 (جديد) | دور Local (دون اتصال/محفوظ محليًا) أُسند إلى دور Information `#305968/#DFEDF1` لأنه حالة نظام — لا دور خاص به في ملف 17 | دقة دلالية | إن رغب المالك بتمييز بصري أكبر: قيمة مشتقة جديدة عبر Proposal |
| ISS-007 (جديد) | تسميات تبويبات الشريط السفلي عند 200% تلتف لأكثر من سطرين والشريط ينمو (بلا أي قصّ) — مقايضة إمكانية الوصول مقابل الارتفاع | سلوك مقصود وموثق | قرار المالك عند المراجعة على تكبير فعلي |

## فجوات أصول مفتوحة (من CONFLICT-AND-GAP-LOG)

- GAP-001 شعار Micro النهائي (لا يعطل: الـHeader بلا شعار بقرار).
- GAP-002 علامة Micro الرباعية: مرشحة موسومة — بلون الهوية القائدة الآن؛ اعتمادها قرار مالك.
- GAP-003 Micro Financial Glyphs: Phosphor حتى اعتماد الشعار.

## قيود تقنية قائمة (UNVERIFIED)

- جهاز Android فعلي وTalkBack ولوحة مفاتيح ولمس Swipe فعلي وSafe Areas وضوء الشمس.
- جولة مستخدمين أردنيين (5–7) وفق §16.7.
- أداء 60fps على هاتف متوسط.

## مقترحات مقيدة (Not Approved — لا تدخل التنفيذ تلقائيًا)

قيم أساس مشتقة نفذها ZAI بتفويض 17 §2 («Derived and documented by ZAI») وموثقة: `#8F3B27` (مضغوط Action)، `#C4663F` (عمق هوية غير نصي)، `#E4EAEC` (سطح ثانٍ)، `#8A959C` (معطل)، `#A6AEB4` (زخرفي) — قابلة للاستبدال بقرار موثق إن ظهر عيب قياس أو راحة. لا مقترحات أخرى من المنفذ؛ أي فكرة مستقبلية عبر `OPTIONAL-PROPOSALS` منفصلة.
