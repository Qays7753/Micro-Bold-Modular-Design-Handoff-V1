# Quality and Evidence — Micro V2

**نطاق هذا التقرير:** التصميم المعروض في الاستوديو بعد موافقة المالك، مع الفصل بين فحص المنفذ وفحص التصحيح المستقل. نجاح البناء لا يثبت صحة تجربة هاتف فعلي.

| الفحص | النتيجة | حدود الدليل |
|---|---|---|
| `npm ci` + `npm run build` | نجح على مصدر V2 بعد إصلاح الصفوف؛ 79 وحدة Vite | فحص أنواع وبناء فقط. أعيد بناء GitHub على النسخة السابقة ونجح قبل تنظيف المستودع. |
| تغطية 3 شاشات و21 حالة | المصدر وFixtures واللقطات موجودة | باقي شاشات جرد المنتج مؤجلة. |
| تدقيق ZAI السابق | بلّغ عن 47/47 حالة متصفح ناجحة | سكربت Playwright ونتائجه الخام لم يسلما بالمستودع؛ كانت الجولة قبل تعديل صفوف 200%. ليست إعادة تحقق مستقلة. |
| مبالغ عملية جزئية | صُحح `TransactionRow` ليقرأ الأصل/المدفوع/المتبقي من بيانات الصف | لا تُحوّل Fixtures إلى قواعد مالية. |
| تخطيط 200% | عُدل `OpenRow` و`TransactionRow` لعرض الشرح بعرض مناسب والقيمة بعده | لقطات الإصدار السابق عند 200% أزيلت؛ نتيجة العرض المصحح في متصفح وعلى جهاز **UNVERIFIED**. |
| أزواج ألوان المصدر | 25 زوجًا مسجلًا في `CONTRAST-REPORT.md` | تباين المتصفح والأجهزة الفعلية للحالات الجديدة يُفحص عند النقل. |
| GitHub Pages | إعداد الخدمة فشل برسالة `Resource not accessible by integration` | رابط عام غير متاح؛ لا يؤثر على بناء المصدر المحلي. |

**بوابة نقل المعايير:** ابنِ التوكنز والمكونات داخل Micro، ثم افحص العرض 320/360/390/412 والتكبير 150/200%، العربية وRTL، Focus وSafe Areas واللمس وقارئ الشاشة وحالات نقص البيانات وربط قيم البيع الواقعية. لا تسجل PASS قبل توافر الدليل. اختبارات مستخدمين أردنيين وضوء النهار والأداء على هاتف متوسط لم تنفذ بعد.
