# QA-SUMMARY — ملخص فحص الجودة (V2 — مراجعة الألوان والتكوين 2026-09-23)

**التاريخ:** 2026-09-23 · **البيئة:** Node v24.21.0 · Vite 5.4.11 · Chromium (Playwright) داخل بيئة التنفيذ · device_scale_factor=2 · سكربت `verify-studio-v2.py` (خارج المستودع، قابل للتكرار).
**مبدأ الحزم (09-QA):** لا `PASS` بلا فحص ودليل؛ غير المختبر `UNVERIFIED`. نتائج الإصدار السابق لا تنتقل إلى هذه الجولة.

## 1. نتائج فعلية مثبتة — 47/47 فحصًا آليًا ناجحًا (فحص متصفح داخل بيئة التنفيذ)

| المحور | الطريقة | النتيجة | الدليل |
|---|---|---|---|
| Build | `npm ci` + `npm run build` (tsc + vite) | **PASS** — 79 وحدة، بلا أخطاء أنواع | إخراج البناء |
| أخطاء Console / Page | جمع رسائل المتصفح عبر الجولة الكاملة | **PASS — صفر** | `verify-v2-results.json` |
| صحة التوجيه | تحقق `data-screen` لكل سيناريو (مضاد أخطاء المسار) | **PASS — 21/21** | نفس السكربت |
| تجاوز أفقي | scrollWidth/clientWidth لكل حالة/عرض/تكبير **بعد `document.fonts.ready`** | **PASS — صفر** | نفس السكربت |
| السيناريوهات الـ21 | لقطة عنصرية للهاتف لكل حالة + تحقق التمرير | **PASS** | `evidence/screenshots/*.png` (أعيد توليدها كلها) |
| **سحب بطاقة SnapshotDeck** | سحب Pointer فعلي: تتبع الإصبع (لقطة أثناء السحب) + 1→2 + 2→1 + مقاومة حافة بلا Loop + لا اختطاف لسحب الصفحات | **PASS** | `interaction_deck-drag-follow_360.png` |
| **السحب بين الصفحات (§11.12)** | سحب يسار/يمين على المستوى 1 + لا سحب في نموذج مالي + لا سحب مع طبقة مفتوحة + يعمل مع Reduced Motion | **PASS** | نفس السكربت |
| **«عرض الكل»** | يفتح OVR-SNAPSHOT-ALL بـ4 بطاقات + عودة تحفظ التسلسل | **PASS** | `ovr-snapshot-all_complete_360.png` |
| **تبديل سيناريو البيع** | إدخال (صنف/عميل/أخطاء) ثم التبديل: سلة الـFixture الجديدة، لا عميل/أخطاء/طريقة متسربة، وEcho لا يعلق | **PASS** | نفس السكربت |
| **لا نقر ميت** | 12 مسار إجراء مرئي → مسارات حقيقية أو مؤجلة صادقة | **PASS** | نفس السكربت |
| العروض 320/360/390/412 | لقطات + قياس لكل شاشة | **PASS** | `*_320/390/412.png` |
| تكبير نص 150/200 | `--text-scale` مع Reflow + قياس | **PASS** | `*_z150/z200.png` |
| تسميات الشريط السفلي | scrollHeight ≤ clientHeight لكل تسمية عند 320 و200% | **PASS — لا قصّ** | نفس السكربت |
| Grayscale | `gray=1` + فحص VLM: هرمية واضحة **وتمييز OVR عن FIN في الرمادي** | **PASS** | `*_gray.png` + VLM |
| Reduced Motion | `rm=1` + سحب يعمل فوريًا | **PASS** | `*_reduced-motion.png` |
| **التركيز المرئي** | Tab بلوحة مفاتيح فعلية + قياس outline 2px solid + `:focus-visible` | **PASS** | `interaction_focus-visible_360.png` |
| **حالة الضغط** | ضغط مستمر + قياس لون الخلفية `rgb(143,59,39)` = `#8F3B27` | **PASS** | `interaction_button-pressed_360.png` |
| عربية وصدق القيم | فحص VLM: عربية سليمة بلا Tofu؛ `385.75 د.أ` معًا؛ `−80.00` مع «خسارة»؛ لا `0.00` للناقص؛ ديون العميل غير المسجل تظهر نصًا لا صفرًا | **PASS** | لقطات + VLM |
| التبويبات الخمسة + Badge | التسميات الخمس المعتمدة + تنقل + شارة 3 | **PASS** | `interaction_bottom-nav-selected_360.png` |
| تباين الأزواج | حساب WCAG 2.2 برمجي من **أزواج النسخة الجديدة الفعلية** (25 زوجًا) | **PASS — 25/25** | `reports/CONTRAST-REPORT.md` |
| **تدقيق الألوان النشطة** | grep المصدر+fixtures+public+dist بحثًا عن HEX/أسماء Indigo/Citrus والمشتقات | **PASS — نظيف** | REVISION-COVERAGE-LEDGER |
| أسرار | فحص قبل الـCommit | **PASS — نظيف** | خطوة ما قبل الـCommit |

**عينات فحص VLM موثقة:** OVR (عربية سليمة؛ الكتلة البرتقالية تجيب عن الربح أولًا؛ تباين ممتاز؛ د.أ مع المبلغ) · FIN (لوحة بيضاء هادئة وصفوف محافظ خالية من الازدحام؛ إيقاع مميز) · OPS (إيقاع مريح لمهمة لمسية؛ أسطر واضحة؛ شريط إجراء مميز) · الرمادي (تمييز التركيبين جوهري) · ورقة المقارنة (عربية سليمة، مقارنة واضحة).

## 2. UNVERIFIED (صريح — بلا دليل جهاز/مستخدم فعلي)

| المحور | لماذا | خطوة التحقق المقترحة |
|---|---|---|
| Android فعلي + هاتف صغير + جهاز مرجعي | لا جهاز في بيئة التنفيذ | تشغيل محلي على أجهزة المالك (320/360/390/412) |
| لمس إصبع حقيقي للسحب وسلوك Pager | الفحص بالمؤشر | تمرير حقيقي بين التبويبات وبطاقات الـDeck |
| TalkBack / VoiceOver | لا قارئ فعلي | جولة قراءة شاشة على الجهاز |
| لوحة مفاتيح Android مع الحقول | لا لوحة فعلية | إدخال مباشر في نموذج البيع |
| الضوء القوي / سطوع منخفض / واقي شاشة | بيئة عرض فقط | فحص ميداني — «النجاح الحسابي ≠ راحة النهار» (17 §3) |
| مستخدمون أردنيون (5–7) | غير متاح هذه الجولة | جولة تكوينية وفق §16.7 |
| أداء 60fps على هاتف متوسط | لا قياس جهاز | قياس أثناء الفحص الميداني |

هذه البنود **لا تمنع** التسليم المرحلي (§21.5) وتمنع ادعاء «التجربة مقبولة».

## 3. عيوب ظهرت وعولجت أثناء هذه الجولة

سباق قيم سحب React (→ ref متزامن)، فيض StudioHome عند 360 (→ شبكة)، قياس قبل تحميل الخطوط (→ انتظار fonts.ready)، قصّ تسميات الشريط عند 200% (→ ارتفاع تكيفي)، ولقطات كانت تُلتقط للصفحة الرئيسية بسبب حالة أحرف في سكربت الفحص (→ تصحيح + تحقق مضاد). التفصيل في VISUAL-PROOF-REVIEW (سجل Self-Audit).

## 4. P3 مفتوحة (لا تؤثر على الفهم/الثقة/الوصول)

سهم التقدم RTL قد يُقرأ ملتبسًا (قرار معتمد — للنقاش)، وسم Fixtures داخل قسم الأثر (شرط صدق — للنقاش)، شريط النظام المحاكى (موثّق تحت الإطار).

## 5. قابلية التكرار

`npm ci && npm run build` من مصدر المستودع يعيد النتيجة. سكربت الفحص (`verify-studio-v2.py`) خارج المستودع؛ خطوات الفحص اليدوي في README، والجولة الكاملة أعلاه قابلة للتكرار بأي Playwright محلي.
