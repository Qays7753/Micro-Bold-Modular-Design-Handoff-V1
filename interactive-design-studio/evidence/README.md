# دليل الأدلة — evidence/ (V2 — مراجعة 2026-09-23)

كل ملف في `screenshots/` **لقطة فعلية من النسخة المراجَعة (V2)** بعد تطبيق منظومة `17-COLOR-DECISION` والتكوينات الجديدة — أُنتجت بسكربت فحص قابل للتكرار (Playwright/Chromium حقيقي، DPR×2) بعد `npm ci && npm run build && npm run preview`. اللقطات **دليل مساعد** — ليست بديلًا عن المصدر التفاعلي القابل للتعديل.

> **`history-v1-indigo/`:** لقطات الإصدار الأول (Indigo/Citrus) — **أرشيف تاريخي موسوم، ليس دليلًا على النسخة الحالية**، ولا يجوز نسبة نجاحه إلى هذه الجولة (18 §1). احتُفظ به لدعم مقارنة قبل/بعد فقط.

## اصطلاح التسمية (screenshots/)

- `<screen>_<state>_<width>.png` — لقطة **عنصرية لإطار الهاتف** (بلا كروم الاستوديو المهيمن — 18 §1.7) لكل سيناريو من الـ21.
- `<screen>_<state>_<width>_z150|z200.png` — تكبير نص 150%/200% مع Reflow.
- `<screen>_<state>_<width>_gray.png` — فحص إزالة اللون (§16.7).
- `fullpage-*.png` — سياق الصفحة كاملة للشاشات الرئيسية وSTUDIO-COMPONENTS.
- `interaction_*.png` — تفاعل حقيقي مسجل: **تتبع سحب البطاقة بالإصبع** (`deck-drag-follow`)، لوحة الحساب، حوار الخروج، قائمة الفترة واختيارها، Sheet تفصيل النتيجة، إضافة صنف للسلة، تحقق آجل بلا عميل، نجاح الحفظ→Echo، التركيز بلوحة المفاتيح، **حالة الضغط للزر الصلب**، التبويب المحدد.
- `ops-home|tool-home|mkt-home_deferred_360.png` — بطاقات التأجيل الصادقة.
- `comparison-contact-sheet.png` — **ورقة مقارنة قبل/بعد** بعربية سليمة (قبل = تاريخي، بعد = V2).

## ماذا يثبت هذا الدليل

1. **النتيجة تقود OVR-NOW:** بطاقة الهوية `#D97757` بنص داكن أولًا في كل حالات OVR (`ovr-now_*`).
2. **FIN بلا كتلة ملونة مهيمنة وصفوف محافظ نظيفة:** `fin-overview_complete_*` + z150/z200.
3. **إيقاع النموذج الجديد:** `ops-sale-create_filled_*` (السطر الشبكي وشريط الإجراء الكامل).
4. **كل الحالات:** 21 سيناريو بما فيها saving/failure/offline/error/first-use/zero/negative.
5. **الرمادي:** هرمية واضحة وتمييز OVR عن FIN (`*_gray` + تحليل VLM في VISUAL-PROOF-REVIEW).
6. **التفاعلات المصلحة:** سحب حي، عرض الكل حقيقي (`ovr-snapshot-all_complete_360.png`)، لا نقر ميت (سجلات QA-SUMMARY).
7. **العروض والتكبير:** 320/390/412 + z150/z200 بلا قصّ أو انفصال المبلغ عن «د.أ».

## حدود هذا الدليل (صريحة)

- فحص متصفح داخل بيئة تنفيذ — **ليس** اختبار Android أصليًا ولا TalkBack ولا مستخدمًا فعليًا (UNVERIFIED في `reports/QA-SUMMARY.md`).
- شريط النظام أعلى الإطار **محاكاة عرض** موثقة الحدود.
- سلاسة السحب بالإصبع الحقيقي و60fps على الجهاز — غير ممثلة باللقطات (السحب مثبت آليًا بأحداث Pointer وفُحص تتبعه).
