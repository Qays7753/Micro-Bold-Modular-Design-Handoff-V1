# Micro V2 — قرار الجاهزية لنقل التصميم المرئي

**التاريخ:** 2026-09-23 · **المرجع:** فرع `exec/interactive-design-studio-zed-20260923` في مستودع تسليم التصميم. هذا المستند يثبت موافقة مالك المنتج على اتجاه V2 المرئي في هذه المحادثة؛ لا يجيز تغيير قواعد Micro المالية أو اعتبار بيانات الاستوديو بيانات إنتاج.

## الحكم ونطاق الموافقة

**اعتماد التصميم المرئي V2 كمرجع للعمل:** نعم. أعجب المالك بالمخرج ووافق على اللون واللغة البصرية والتفاصيل المعروضة. لا حاجة لإعادة البحث عن اتجاه بصري أو طلب موافقة ثانية لبدء نقل الأساس المرئي.

**جاهزية نقل كل Micro دفعة واحدة:** لا. توجد ثلاث شاشات منتج مكتملة كنماذج مرئية، 21 حالة لها، وعرض «كل البطاقات» داخل الاستوديو، ومكتبة مكونات وTokens. بقية جرد المنتج، ومنها العمل والأدوات والسوق والشاشات المالية التفصيلية، ما زالت مؤجلة؛ المسارات التي تفتح بطاقات توثيق داخل الاستوديو لا تمثل شاشة منتج مصممة. قبول اللغة البصرية لا يحوّل الـFixtures إلى حسابات صحيحة ولا يحوّل الشاشة المؤجلة إلى تصميم مكتمل.

**قرار البدء:** يمكن بدء **نقل تدريجي** للأساس المرئي والمكونات المشتركة والشاشات الثلاث إلى Micro عند بدء مسار التنفيذ المنفصل، مع تحقق بصري ووظيفي أثناء الدمج. لا تنسخ الاستوديو تطبيقًا إنتاجيًا كاملًا.

## ما فُحص في هذه المراجعة المستقلة

| البند | النتيجة وحدها |
|---|---|
| المصدر | اطلعنا على آخر Commit والملفات النشطة للنسخة V2؛ العمل في مستودع التسليم فقط؛ لم يُعدّل تطبيق Micro الإنتاجي. |
| البناء | `npm ci` ثم `npm run build` نجحا محليًا: فحص TypeScript و79 وحدة Vite. |
| اللون | `#D97757` هو لون الهوية؛ `#A94630` لون إجراء داكن متباين؛ لا آثار Indigo/Citrus في الملفات المرئية النشطة التي فُحصت. لا تعيد نسخ ألوان V1 من التاريخ. |
| الشاشات | `OVR-NOW` ست حالات، `OPS-SALE-CREATE` تسع، `FIN-OVERVIEW` ست؛ `OVR-SNAPSHOT-ALL` عرض مرجعي بأربع حالات؛ `STUDIO-COMPONENTS` أداة مراجعة وليست شاشة Micro. |
| التكبير | أظهرت لقطة ZAI عند `360/200%` تضييق وصف بعض الصفوف إلى عمود رفيع؛ عُدّل تخطيط `OpenRow` و`TransactionRow` عند 200% ليظهر الوصف بعرض أوسع ثم القيمة تحته. البناء بعد التصحيح نجح؛ يجب إعادة التقاط لقطات 320/360 عند 200% قبل اعتماد صلاحية العرض النهائي على الجهاز. |
| بيانات المبالغ | كان `TransactionRow` يطبع `40.00/15.00/25.00` حرفيًا لكل عملية جزئية؛ أصبح يعرض الأصل والمدفوع والمتبقي من بيانات الصف مع إبقاء الرقم والوحدة متلازمين. |
| أدلة ZAI | تقريره يقول 47/47 فحصًا في بيئته وصور V2 موجودة؛ سكربت الفحص الأصلي ونتائجه الخام خارج المستودع، لذا لا يجوز تسميتها فحوصًا مستقلة معادة من هذا المصدر. |
| المعاينة العامة | Workflow للبناء نجح لكن GitHub Pages تعذر تهيئته بخطأ صلاحية GitHub App؛ تشغيل الاستوديو محليًا متاح، والرابط العام غير مثبت. هذا قيد معاينة، لا عيب في التصميم نفسه. |

## عقد النقل إلى Micro

1. ابدأ بـ`src/foundations/` ثم مكونات `core/` و`financial/` و`contextual/` والأيقونات والخطوط المرخصة؛ طابق السلوك المرئي والـRTL وإشارات الصدق المالي. لا تعتمد ترتيب الملفات أو React كشرط على معمارية Micro؛ انقل العقود والتوكنز لا تطبيق الاستوديو كله.
2. طبّق `OVR-NOW` و`OPS-SALE-CREATE` و`FIN-OVERVIEW` على منطق Micro الحقيقي. اربط العمليات والمحافظ والنتيجة والتحصيل بالبيانات الواقعية بعد التحقق من شروط الحساب؛ راجع خصوصًا «أثر العملية» في البيع لأنه مثال عرض بقيم Fixtures فقط.
3. أنشئ كل شاشة أخرى بناءً على مهمة المستخدم وبيانات المنتج الحالية قبل تنفيذها؛ `DeferredScreen` ليس شاشة جاهزة. احتفظ بالسوق تبويبًا خامسًا دون افتراض محتواه، وبالمنتجات والخدمات شاشة سياقية لا تبويبًا سادسًا.
4. استبعد من الإنتاج `StudioBar` و`ReviewControls` و`StudioHome` و`STUDIO-COMPONENTS` و`fixtures/` وشارة Fixtures والمحاكاة البصرية لشريط نظام الهاتف؛ صمم بديل بيانات حقيقية وتسميات حالة صادقة حيث يلزم.
5. في نقل كل شاشة افحص العرض 320/360/390/412 والتكبير 150/200% واتجاه الرجوع والسحب والطبقات المنبثقة واللمس ولوحة المفاتيح وقراءة الشاشة على جهاز حقيقي. لا تصف ذلك بأنه ناجح قبل تنفيذه.
6. لا تستورد أي شكل أو لون من واجهة Micro القديمة بوصفه مرجعًا تصميميًا؛ راجع الوظائف والقيود فقط. لا تولّد شاشة تكرارًا ميكانيكيًا لنموذج واحدة من الثلاث.

## فصل ما يمكن تأجيله

- أصول Micro Signal الرباعية والغليفات المالية نهائية من حيث الدور والتجربة المعروضة، لكن ملف الرسم النهائي وهوية العلامة المستقلة يحتاجان تحقق أصول قبل اعتمادهما كعلامة إنتاج رسمية.
- أجهزة Android فعلية، TalkBack، مناطق Safe Area، ضوء الشمس وأداء جهاز متوسط، واختبار أصحاب مشاريع أردنيين: **لم تُفحص مستقلًا** هنا؛ تحقق أثناء التنفيذ وقبل إصدار الإنتاج.
- ارتفاع شريط التبويبات عند تكبير 200%، صيغة صفحة «كل البطاقات» لو أصبحت صفحة منتج كاملة، ودور لون الحفظ المحلي: تحسينات مستقبلية مسجلة؛ لا توقف نقل المكونات والشاشات المنفذة.
- روابط المعاينة العامة تحتاج تمكين Pages أو وسيلة استضافة مصرح بها، ولا تعطل الوصول إلى المصدر عبر المستودع.

## قاعدة اعتماد التنفيذ

نجاح `npm run build` واعتماد المالك **يجيزان بدء نقل المرجع المرئي**، ولا يمثلان قبول إصدار Micro الإنتاجي. اجعل فحص كل شاشة منقولة وبخاصة 200% وحالات نقص البيانات بوابة قبل توسعة تنفيذ الشاشات الأخرى. أي اختلاف بصري مثبت عن V2 الموافق عليه يوثق كعيب، وأي تطوير جمالي لاحق يوضع في سجل تحسين منفصل.
