# Micro V2 — جاهزية نقل معايير التصميم

**قرار المالك:** الاتجاه البصري V2 المعروض معتمد مرجعًا واحدًا. **قرار التنفيذ في هذه المرحلة:** نقل الأساسات والمكونات المشتركة أولًا عبر مستودع Micro عند صدور تكليف التنفيذ؛ لا تبدأ بنسخ الشاشات الثلاث دفعة واحدة ولا تعمم تكوينها على باقي الصفحات.

## ما يؤخذ من هذا المستودع

| الطبقة | المصدر | ما ينقل إلى Micro |
|---|---|---|
| الألوان | `COLOR-STANDARD-V2.md` و`src/foundations/tokens.css` | أدوار Brand/Action/Canvas/Surface/Ink/Semantic مع أزواج التباين؛ `#D97757` هوية، وليس لون كل إجراء أو نتيجة. |
| الخط والشكل والحركة | `DESIGN-DECISIONS-V2.md` و`src/foundations/` | Alexandria/Noto، مسافات وحواف وحدود واستجابة الضغط والحركة المخفضة؛ تكييفها مع بنية Micro الحالية. |
| المكونات | `COMPONENT-CATALOG.md` و`src/components/` | عقود الزر والصف والحقل وSheets والمال والإشارة والتنقل وحالاتها؛ تحديث المكونات المشتركة مرة واحدة. |
| اللغة السياقية | `STATE-MATRIX.md` و`RTL-ACCESSIBILITY.md` | حالة الصفر الحقيقي/غير معروف/جزئي/دون اتصال والمبلغ والعملة والأرقام الإنجليزية والتركيز والرجوع. |
| مرجع الشاشة | `SCREEN-CATALOG.md` و`SCREEN-COVERAGE-V2.csv` | مثال ملموس للتحقق من لغة التصميم في 3 شاشات، وليس قالبًا يُكرر على الشاشات الأخرى. |

جميع مسارات `src/` والكتالوجات أعلاه داخل `interactive-design-studio/` ما لم يذكر خلاف ذلك.

## خطوات نقل المعايير

1. جرد مواضع الاستخدام الحالية في Micro: Canvas/Surface، الخطوط، قيم المال، الأزرار، الحقول، الصفوف، التنقل، الإشعارات والطبقات. احمِ الوظائف والبيانات والقيود المالية الحالية.
2. أنشئ ترجمة واضحة من الدور المرئي V2 إلى مكونات Micro. استبدل التوكنز ثم المكونات المشتركة؛ حافظ على التمييز الدلالي بين الربح والكاش والديون والنقص والخطأ. لا تغيّر منطق حساب أو صلاحية بسبب لون.
3. افحص الشاشات الحالية بعد انتقال الأساس: قد يختل ترتيب أو حد أو التفاف نص؛ أصلح التكيف في سياقه بدل نسخ تخطيط `OVR-NOW` إلى صفحة طلبات أو مخزون.
4. بعد اعتماد أثر المعايير على الواجهات، نفذ تركيب كل شاشة مخصصًا لمهمتها. أول ثلاث شاشات نماذج مرئية معتمدة، أما بقية الجرد فغير مصمم كاملًا.
5. استبعد من Micro الإنتاجي `StudioBar` و`ReviewControls` و`StudioComponents` و`fixtures/` وشارة Fixtures وشريط حالة الهاتف المحاكى. اربط الواجهة ببيانات حقيقية؛ «أثر العملية» في البيع مجرد قيم عرض، وليس معادلة مالية.
6. افحص RTL و320/360/390/412 وتكبير 150/200% والأرقام `0–9` و`د.أ` وFocus والطبقات والسحب ولوحة الهاتف. اختبر حالات نقص البيانات على الجهاز قبل وصفها بالمنجزة.

## ما فُحص وما بقي

نجح البناء `npm ci && npm run build` للنسخة بعد تصحيح تخطيط `OpenRow` و`TransactionRow` عند تكبير 200% وربط مبالغ العملية الجزئية ببياناتها. تقرير ZAI الأصلي يذكر 47/47 فحصًا قبل هذين التصحيحين؛ سكربت المتصفح ونتائجه الخام لم يُسلّما في المستودع. **لم يُعاد اختبار شكل التصحيح في متصفح أو هاتف فعلي**؛ الصور القديمة عند 200% أزيلت من الشجرة النشطة حتى لا تُعامل كدليل عليه.

GitHub Actions يبني الحزمة، لكن الاستضافة العامة Pages فشلت بصلاحية التكامل. نجاح البناء لا يغلق اختبارات TalkBack/Android واللمس والضوء وأصحاب المشاريع الأردنيين؛ وثّقها أثناء نقل Micro وقبل الإنتاج.

## تحسينات مستقبلية منفصلة

الأصل النهائي لعلامة Micro، دور الحفظ المحلي، ارتفاع التبويبات عند تكبير كبير، وشكل صفحة كل البطاقات كصفحة منتج: مواضيع تراجع وفق حاجتها. لا تعيد فتح هوية V2 المعتمدة بسببها، ولا تعتبرها اكتمالًا لشاشات لم تبنَ.
