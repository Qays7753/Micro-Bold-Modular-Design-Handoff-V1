# Micro — Visual Design Decisions V1

**نوع الوثيقة:** سجل قرارات UI/UX والهوية البصرية  
**الإصدار:** V1  
**التاريخ:** 2026-09-23
**الحالة:** مرجع مكتمل للقرارات المعتمدة؛ اعتماد §21.5 على مرحلتين: تسليم التصميم ثم التحقق الواقعي  
**النطاق:** ما يراه المستخدم ويتفاعل معه على الواجهة فقط

---

## 1. وظيفة هذه الوثيقة

هذه الوثيقة هي المرجع الحالي للقرارات البصرية المعتمدة لنظام **Micro**. هدفها منع تضارب التوجيهات، ومنع الرجوع إلى التصاميم أو الافتراضات القديمة، وتجهيز أساس واضح لأي مصمم أو Agent سيعمل لاحقًا على الواجهات.

لا تُعد هذه الوثيقة أمر تنفيذ ولا مواصفات لكود Micro الإنتاجي. تحدد §21 عقد إنشاء استوديو تصميم تفاعلي مستقل عند تكليف المنفذ. الحزمة المرافقة تحوّلها إلى تعليمات تصميم عند توجيه Agent إليها صراحة، ولا تمنح صلاحية تعديل النظام الحالي أو إطلاق منتج أو إعلان قبول نهائي نيابة عن المالك.

### حالات القرار

- **مغلق:** قرار معتمد لا يُغيّر إلا بقرار جديد صريح.
- **مفتوح:** لم يُحسم بعد ولا يجوز افتراضه.
- **مرفوض:** اتجاه جُرّب أو نوقش وتم استبعاده.

---

## 2. حدود المسار

### داخل النطاق

- Shape Language.
- Typography.
- UI / Visual Identity.
- Interaction Design المرئي.
- Component UI.
- Iconography.
- Contextual UI.
- Visual UX.
- Interaction UI.
- Form UI.
- Financial UI.
- Navigation UI.
- الحالات، التغذية الراجعة، الحركة، والانتقالات المرتبطة مباشرة بما يراه المستخدم.

### خارج النطاق

- Business Logic.
- المعادلات والحسابات المالية.
- السياسات المحاسبية.
- قواعد البيانات.
- منطق التخزين والمزامنة.
- Backend وAPIs.
- الصلاحيات والمنطق الأمني.
- Routes أو Information Architecture التقنية.
- إضافة وظائف جديدة.
- تغيير كود Micro الإنتاجي أو منطق المنتج؛ يسمح §21 ببناء كود استوديو التصميم المستقل للمراجعة فقط.

---

## 3. القاعدة الحاكمة للتصميم

### قرار مغلق: لا قوالب جاهزة ولا Vibe Coding

يُرفض أي حل يبدو كواجهة مولّدة من UI Kit عام أو Dashboard جاهز، حتى لو كان نظيفًا تقنيًا. يمكن استخدام الأنماط المألوفة عندما تخدم سهولة الاستخدام، لكن يجب إعادة صياغتها داخل لغة Micro البصرية، لا نسخها كما هي.

علامات الرفض تشمل:

- نفس البطاقة مكررة لكل نوع محتوى.
- صفوف Chips أو Pills في كل مكان.
- تدرجات لونية وزجاجية وظلال زخرفية بلا وظيفة.
- أيقونة داخل مربع ملون لكل عنصر.
- Toast بعد كل تفاعل.
- Bottom Sheet لكل اختيار بسيط.
- توزيع Dashboard عام لا يعكس طبيعة المهمة.
- اختلاف الألوان فقط مع بقاء الفلسفة والشكل والتكوين كما هما.

#### قاعدة عامة ملزمة لكل الوثيقة وكل عمل لاحق

منع القوالب الجاهزة و`Vibe Coding` ليس تفضيلًا بصريًا يطبق عند الإمكان، بل **شرط قبول حاكم** لكل:

- قرار أو اقتراح داخل هذه الوثيقة.
- Component أو حالة أو شاشة أو انتقال.
- مثال بصري أو ملف تسليم أو Design System.
- عمل يصدر لاحقًا عن مصمم أو Agent أو أداة توليد.

لا يكفي أن يطابق الحل الوظيفة أو قواعد الوصول. يجب أن يثبت أيضًا أنه صيغ داخل لغة Micro البصرية والسلوكية. استخدام Pattern مألوف مسموح عندما يحمي قابلية الفهم، لكن تمثيله العام أو نسخه من مكتبة كما هو مرفوض.

القواعد الملزمة هي:

1. **المكوّن يملك حالاته:** Loading وEmpty وPartial وError وOffline وغيرها تحولات داخل المكوّن المضيف كلما أمكن، لا Cards أو Alerts مستقلة تضاف فوقه.
2. **السياق قبل المكوّن:** ترتبط الحالة بمصدرها، وتنكشف منه، ويظهر التعافي في المكان نفسه وفق `Anchor → Reveal → Recover`.
3. **ثبات المكان:** لا تتغير بنية الشاشة أو تقفز العناصر لمجرد تغير الحالة؛ يحافظ المستخدم على ذاكرته المكانية.
4. **ميزانية الانتباه:** لا يتنافس أكثر من عنصر واحد عالي الحضور داخل مساحة العرض، وتبقى الحالات الأخرى مدمجة قرب مصادرها.
5. **التصعيد بحسب الأثر:** قوة الشكل يحددها أثر الحالة على المهمة، لا مجرد تصنيفها كمعلومة أو تحذير أو خطأ.
6. **لا قالب بصري متكرر:** يمنع تكرار بنية `أيقونة داخل دائرة + عنوان + وصف رمادي + زر` بوصفها حلًا عامًا.
7. **لا خصوصية شكلية زائفة:** تغيير اللون أو الاسم أو Radius أو الأيقونة لا يحول المكوّن الجاهز إلى مكوّن خاص بـMicro.
8. **قابلية الرفض:** أي مخرج يبدو كـUI Kit عام أو Generated Dashboard يرفض حتى لو نفذ المتطلبات حرفيًا، ويعاد تكوينه قبل المراجعة الجمالية التفصيلية.

لا يُستثنى أي محور لاحق من هذه القاعدة إلا بقرار صريح موثق يثبت أن النمط القياسي ضروري للفهم أو الوصول.

#### قاعدة السلوك المألوف والتعبير الخاص بـMicro

يعتمد النظام قاعدتين متلازمتين:

1. `Behavioral Familiarity Lock`: سلوك المنصة والأنماط المعروفة لا يعاد اختراعه.
2. `Micro Visual Expression`: الشكل والتكوين والهوية والحالات لا تنسخ من القوالب الجاهزة.

تبقى مألوفة بالكامل: Back، Scroll، Swipe المباشر، Keyboard، Focus، Checkbox، Radio، Toggle، Dialog، Sheet، System Gestures، Safe Areas، Permission Dialogs، وتكبير النص وقارئ الشاشة. يمكن تخصيص هندستها وألوانها وتكوينها، لكن لا يتغير معناها أو اتجاهها أو النتيجة المتوقعة منها.

إذا تعارضت الهوية مع توقع المستخدم أو سلوك المنصة، يفوز السلوك المألوف. ويمنع في المقابل نسخ الشكل الافتراضي للمكتبة بحجة الحفاظ على المألوفية. خصوصية Micro تأتي من Shape Language واللون والهرمية والتكوين والحالات والإيقاع الحركي، لا من اختراع Gestures أو Controls جديدة.

### مرجعية التصميم

- تُبنى الرؤية البصرية الجديدة بمعزل عن واجهات Micro الحالية.
- الشيفرة الحالية والتصاميم السابقة ليست مصدرًا بصريًا ملزمًا.
- تحافظ الأنماط المألوفة على قابلية التعلم، بينما تأتي شخصية Micro من التكوين، والهرمية، والهندسة، والإيقاع، والحركة، واللغة البصرية المتكاملة.

---

## 4. الاتجاه العام

### قرار مغلق: Active Clarity

يجب أن تبدو Micro:

- نشطة ومتزنة.
- مهنية من دون برود.
- قريبة من صاحب المشروع من دون طفولية.
- واضحة وموثوقة من دون مظهر مصرفي تقليدي.
- حيوية من دون ضوضاء لونية.
- غنية بصريًا من دون زخرفة فارغة.

الهدف النفسي ليس تهدئة المستخدم إلى حد الخمول، ولا إثارة القلق. المطلوب واجهة تساعد صاحب المشروع المستعجل على فهم وضعه واتخاذ الإجراء التالي بثقة، مع إبقائه يقظًا ومتفاعلًا.

---

## 5. Screen Composition

**الحالة: مغلق**  
**اسم الاتجاه:** `Hierarchical Modular Layout`

### البنية الأساسية

تُبنى الشاشة من وحدات متفاوتة في الحجم والأهمية، بدل شبكة بطاقات متساوية:

1. وحدة رئيسية كبيرة عند وجود معلومة أو مهمة تستحق القيادة.
2. وحدات متوسطة للمعلومات أو الإجراءات المهمة التالية.
3. بطاقات صغيرة قابلة للتمرير الأفقي للمحتوى الثانوي أو الأدوات.
4. صفوف مفتوحة للقوائم والعمليات المتكررة.
5. كتلة تحليلية كبيرة فقط عندما يكون التحليل هو غرض الشاشة.

### قواعد التكوين

- لا تستخدم الشاشة القالب نفسه بصرف النظر عن وظيفتها.
- لا تتحول كل معلومة إلى بطاقة.
- تقل شبكة البطاقات المتساوية إلى الحد الأدنى.
- لا تُخفى معلومة أساسية داخل Carousel.
- عند استخدام التمرير الأفقي يظهر جزء من العنصر التالي ليكشف إمكانية التمرير.
- يبدأ التمرير الأفقي من اليمين في RTL.
- تُستخدم الأحجام المختلفة لتوضيح الأولوية، لا للزينة.
- العنصر الأكبر يعني أهمية وظيفية أعلى، وليس مجرد تنويع بصري.

---

## 6. Shape Language

**الحالة: مغلق**  
**اسم اللغة:** `Structured Soft Geometry`

### الشخصية الهندسية

- هندسة لينة لكنها منضبطة.
- ليست حادة أو مصرفية.
- ليست مستديرة أكثر من اللازم أو طفولية.
- الحدود والمساحات والارتفاعات توضح البنية، ولا تُستخدم كزخرفة.

### أنصاف الأقطار

| العنصر | نصف القطر المعتمد |
|---|---:|
| البطاقات الرئيسية | `20–24px` |
| البطاقات المتوسطة والصغيرة | `14–18px` |
| الأزرار | `14–16px` |
| الحقول | `12–16px` |

### قواعد الاستخدام

- الحدود تظهر عندما توجد حدود وظيفية فعلية.
- الظل خفيف ويستخدم فقط عندما يوجد ارتفاع حقيقي أو طبقة فوق أخرى.
- القوائم المتكررة تُعرض كصفوف مفتوحة، لا كبطاقة لكل صف.
- `Pills` محصورة في حالات أو اختيارات قصيرة محددة؛ ولا تمثل نظام الفلاتر الرئيسي.

### مرفوض

- الزوايا المقطوعة.
- أنصاف أقطار عشوائية.
- الفقاعات الضخمة.
- Glassmorphism.
- ظل حول كل عنصر.
- بطاقات داخل بطاقات.

---

## 7. Typography and Numbers

**الحالة: مغلق**

### الخطوط

- الخط الأساسي: **Alexandria**.
- الأوزان الأساسية: `Regular`، `SemiBold`، `Bold`.
- خط fallback: **Noto Sans Arabic**.
- لا يوجد خط Mono منفصل للأرقام.
- لا تُخلط عدة عائلات خطية داخل الواجهة.

### السلم الطباعي

| الاستخدام | الحجم | الوزن |
|---|---:|---|
| Hero | `28–32px` | Bold |
| عنوان الشاشة | `22–26px` | Bold |
| عنوان القسم | `18–20px` | SemiBold |
| عنوان البطاقة | `15–17px` | SemiBold |
| النص الأساسي | `14–16px` | Regular |
| النص المساند | `12–14px` | Regular / Medium |
| نص الزر | `15–16px` | SemiBold |
| القيمة المالية الرئيسية | `28–36px` | Bold |
| قيمة داخل قائمة | `15–18px` | SemiBold |
| تسمية التنقل | `11–13px` | Medium، مع بقاء تصميم التنقل مفتوحًا |

### الأرقام والتنسيق المالي

- تُستخدم الأرقام الإنجليزية فقط: `0–9`.
- تُمنع الأرقام الهندية: `٠–٩`.
- مثال معتمد: `1,250.50 د.أ`.
- فاصلة الآلاف: `,`.
- الفاصلة العشرية: `.`.
- تُعزل الأرقام باتجاه `LTR` داخل الواجهة العربية `RTL`.
- تطبق القاعدة على المبالغ، النسب، الكميات، التواريخ، الوقت، أرقام الطلبات والفواتير، وأرقام الهواتف.

### مرفوض

- Bold على كل النصوص.
- النصوص الصغيرة المخفية.
- الخطوط الزخرفية أو الشبيهة بالكتابة اليدوية.
- إضافة خط رقمي مختلف لمجرد إعطاء مظهر تقني.

---

## 8. Visual Identity and Color System

**الحالة: مغلق**  
**الوضع المعتمد حاليًا:** Light Mode فقط

### قرار Dark Mode

لا يوجد Dark Mode في المرحلة الحالية، ولا يُصمم له أو يُفترض وجوده ضمن المخرجات القادمة.

### الألوان الأساسية

| الدور | الاسم | القيمة |
|---|---|---|
| النص والحبر الرئيسي | Deep Ink | `#171923` |
| خلفية التطبيق | Canvas Cool Cloud | `#F4F6FA` |
| السطح الرئيسي | Primary Surface White | `#FFFFFF` |
| السطح الثانوي | Secondary Surface | `#EEF1F7` |
| اللون الأساسي | Electric Indigo | `#4F46E5` |
| Primary Pressed | Pressed Indigo | `#3F37C9` |
| Primary Soft | Soft Indigo | `#E8E7FF` |
| اللون الحيوي | Citrus Lime | `#D9F43B` |
| Citrus Soft | Soft Citrus | `#F3FBCB` |
| اللون الدافئ المساند | Terracotta | `#C45F46` |
| Terracotta Soft | Soft Terracotta | `#F7E4DE` |
| النص الثانوي | Secondary Text | `#5E6472` |
| النص والعنصر غير المتاح | Disabled Text | `#7D8596` |
| العنصر الزخرفي الخافت / Skeleton | Decorative Muted | `#9AA1AE` |
| الحدود الزخرفية | Border | `#D8DDE8` |
| الحدود التفاعلية الضرورية | Interactive Boundary | `#7A8292` |
| طبقة التعتيم | Overlay | `rgba(15,17,23,0.55)` |

### أسطح مساندة

| الاسم | القيمة |
|---|---|
| Soft Blue | `#DFE9FF` |
| Soft Lavender | `#ECE7FF` |
| Soft Mint | `#DFF5E7` |
| Soft Terracotta | `#F7E4DE` |

### الألوان الدلالية

| الحالة | اللون الرئيسي | السطح الخفيف |
|---|---|---|
| نجاح | `#137A55` | `#DCF4E8` |
| تحذير | `#A65A00` | `#FFF0D6` |
| خطر / خطأ | `#C2354B` | `#FDE5E9` |
| معلومات | `#1D64D8` | `#E0ECFF` |
| جزئي | `#6E5AA8` | `#EEE9FA` |
| غير معروف | `#697386` | `#EEF0F4` |
| محفوظ محليًا | `#52657A` | `#E7EDF3` |

### لوحة الرسوم البيانية عند الحاجة

`#4F46E5` — `#008C8C` — `#D97706` — `#B83B75` — `#657C25`

لا تُستخدم عدة ألوان في الرسم إلا عندما توجد سلاسل بيانات أو مقارنات حقيقية.

### قواعد Canvas وتوزيع اللون

- لا تستخدم Beige أو Cream كخلفية أساسية.
- البطاقات الرئيسية بيضاء، والأقسام المساندة تستخدم درجات باردة خفيفة.
- يوجد عادة عنصر لوني قوي واحد في مساحة العرض، لا منافسة بين عدة كتل صاخبة.
- التوزيع الإرشادي: `70%` محايد، `20%` أساسي أو مساند، `10%` Accent أو Semantic.
- اللون الدلالي يشرح معنى، ولا يُستخدم لمجرد التنويع.

### Micro Contrast Composition

- التباين يصنع الهرمية ولا يحول كل عنصر إلى صندوق ذي Border.
- Deep Ink للمعلومة الرئيسية، وSecondary Text للمساند، واللون الدلالي للإشارة أو كلمة الحالة.
- `Interactive Boundary` تستخدم فقط عندما تكون الحدود ضرورية لفهم الإدخال أو الاختيار أو Focus، ولا توضع حول كل Card أو صف أو قسم.
- `Border` الحالية للـDividers والتنظيم الزخرفي فقط، ولا تكون الحد الوحيد لعنصر تفاعلي.
- الأسطح الدلالية تندمج داخل `Micro Context Seam` و`Micro Reveal Row` وحالات المكوّن، ولا تتحول إلى Alert Cards.
- يبقى الاتجاه C والكتل اللونية القوية معتمدًا؛ الوصول لا يحول Micro إلى واجهة رمادية أو Wireframe.

#### أزواج اللون المعتمدة

- `On Indigo = White`.
- `On Citrus = Deep Ink`.
- `On Soft Semantic = Deep Ink` للنص الأساسي وSecondary Text للمساند.
- `On Canvas = Deep Ink / Secondary Text`.
- `On White = Deep Ink / Secondary Text`.
- `Focus on Light = Indigo`.
- `Focus on Indigo = Citrus`.

لا يركب المنفذ Foreground وBackground عشوائيًا من ألوان ناجحة منفردة؛ تستخدم الأزواج الموثقة وتفحص النسبة النهائية.

### دور Terracotta

Terracotta محفوظ كلمسة دافئة مساندة فقط:

- حضوره المستهدف نحو `5–8%` كحد تقريبي.
- لا يستخدم كخلفية Canvas.
- لا يستخدم للزر الأساسي.
- لا يستخدم لحالة الاختيار في التنقل.
- لا يحمل دلالات الربح أو الخسارة أو النجاح أو الخطأ أو التحذير.
- لا ينافس Electric Indigo أو Citrus Lime.
- لا يستخدم كنص عادي فوق الأبيض أو Soft Terracotta؛ يكون النص Deep Ink أو Secondary Text، ويقتصر Terracotta على الأثر الهندسي والإشارة والأيقونة والتفاصيل الكبيرة.

### الأزرار حسب اللون

- Primary: خلفية Indigo ونص أبيض.
- Accent: Citrus ونص Deep Ink، للفرص والاكتشافات غير الحساسة فقط.
- Destructive: اللون الدلالي الأحمر.
- Terracotta لا يستخدم زرًا أساسيًا.

### مرفوض

- Beige Canvas.
- Clay أو Terracotta كلون قيادة.
- Teal كهوية أساسية.
- Sage كهوية أساسية.
- الرماديات الدافئة القديمة.
- Gradients كلغة عامة.
- Glassmorphism.
- تلوين كل بطاقة.
- الأخضر لكل رقم إيجابي أو الأحمر لكل دين أو مصروف دون معنى دلالي دقيق.

---

## 9. Iconography

**الحالة: مغلق**

### المكتبة والأسلوب

- المكتبة الأساسية: **Phosphor Icons**.
- `Regular` هو الأسلوب الافتراضي.
- `Fill` للحالة المحددة.
- `Bold` للإجراء الرئيسي عند الحاجة.
- `Duotone` محصور في الشروحات الكبيرة أو حالات الفراغ.
- تُصمم أيقونات Micro مخصصة للمفاهيم المالية التي قد تلتبس بأيقونات عامة.

### الأحجام

| الاستخدام | الحجم |
|---|---:|
| التنقل | `22–24px`، مع بقاء شكل التنقل مفتوحًا |
| إجراءات أعلى الشاشة | `20–22px` |
| داخل بطاقة | `20–24px` |
| إجراء رئيسي | `24–28px` |
| حالة صغيرة | `16–18px` |
| حالة فراغ | `40–56px` |

### قواعد الاستخدام

- الأيقونة المنفردة بلا نص مقبولة فقط للإجراءات المفهومة عالميًا، مثل الرجوع والإغلاق والبحث والتنبيهات والقائمة السياقية، مع Accessible Label.
- الإجراءات المالية أو عالية التأثير تستخدم أيقونة مع تسمية صريحة.
- لا توضع كل أيقونة داخل دائرة أو مربع ملون.
- يمكن استخدام مربع مستدير للإجراءات السريعة عند وجود مبرر.
- الدائرة محصورة في إجراء مركزي مستقل عند وجوده.
- حجم الأيقونة لا يختصر مساحة اللمس المطلوبة.

### RTL

- سهم الرجوع يشير إلى اليمين.
- الأسهم الاتجاهية تعكس حسب المعنى.
- أيقونات الوسائط والرفع والتنزيل والأيقونات غير الاتجاهية لا تُعكس تلقائيًا.

### الأيقونات المخصصة المطلوبة

- بيع مقابل تحصيل.
- مصروف مقابل دفعة مورد.
- دين لنا مقابل دين علينا.
- عربون.
- أموال المالك.
- تصحيح مقابل عكس عملية.
- جزئي.
- غير معروف.
- محفوظ محليًا فقط.
- غير موزع.

### مرفوض

- Lucide كمكتبة أساس.
- Material Symbols كلغة ثانية.
- Emoji أو رموز نصية بدل الأيقونات.
- خلط مكتبات متعددة.

---

## 10. Component UI

**الحالة العامة: مغلق للمكونات التالية**

### 10.1 Buttons and Actions

#### الأنواع

- Primary.
- Accent.
- Secondary.
- Tertiary.
- Destructive.
- Icon Button.

#### المواصفات

- ارتفاع الزر الرئيسي: `48px`.
- الارتفاع المضغوط: `44px`.
- نصف القطر: `14–16px`.
- النص: `15–16px SemiBold`.
- يوجد إجراء رئيسي واحد فقط داخل المنطقة الواحدة.
- توضع الأيقونة قبل النص من جهة اليمين في RTL.
- يوضع السهم الاتجاهي في الطرف المقابل.
- التسمية فعل + مفعول واضح: `تسجيل التحصيل`، وليس `حفظ` فقط.

#### الحالات

- Default.
- Pressed.
- Focus.
- Loading.
- Disabled.
- Success confirmation.
- Error recovery.

#### قواعد خاصة

- أثناء التحميل يحافظ الزر على هندسته وحجمه ويمنع التكرار، مع بقاء وصف العملية مفهومًا.
- Citrus لا يستخدم لتأكيد مالي، أو تحصيل، أو دفع، أو حذف، أو تصحيح، أو عكس عملية.
- الإجراء المدمر يوضح أثره ويطلب قرارًا مناسبًا؛ لا يظهر افتراضيًا كزر أحمر ضخم.

#### مرفوض

- Gradient.
- ظل كبير.
- Full Pill.
- زران متساويان في القوة يتنافسان على القرار نفسه.

### 10.2 Cards, Containers and Rows

#### البطاقة الرئيسية

- بطاقة واحدة تقود المنطقة.
- نصف القطر: `20–24px`.
- Padding: `18–20px`.
- غرض رئيسي واحد.

#### البطاقة المتوسطة

- نصف القطر: `16–18px`.
- Padding: `14–16px`.
- Border أو سطح مساند، وليس الاثنين للزخرفة.
- إجراء رئيسي واحد كحد أقصى.

#### البطاقة الصغيرة

- نصف القطر: `14–16px`.
- Icon + Title + Value/Status.
- نص قصير فقط.

#### البطاقات الأفقية

- البطاقة المتوسطة تشغل تقريبًا `72–80%` من العرض.
- البطاقة المضغوطة تشغل تقريبًا `42–48%` من العرض.
- يظهر جزء من البطاقة التالية.
- يبدأ التمرير من اليمين.
- يستخدم Scroll Snap.
- لا توضع المعلومة الأساسية الوحيدة داخل التمرير.
- يمنع Horizontal Scroll داخل Horizontal Scroll.

#### الصفوف

- أيقونة أو علامة على اليمين.
- العنوان والمعلومة المساندة في الوسط.
- القيمة أو الحالة أو السهم على اليسار.
- الارتفاع الأدنى: نحو `56px`.
- Divider داخلي لا يمتد بلا داعٍ إلى الحواف.
- لا ظل ولا بطاقة مستقلة لكل صف.
- إذا كان للصف وجهة واحدة، تكون مساحة الصف كاملة قابلة للنقر.

#### الحاويات والأقسام المفتوحة

- تجمع الحاوية صفوفًا مرتبطة تحت حد خارجي واحد وفواصل داخلية.
- لا توجد بطاقات متداخلة داخل الحاوية.
- القسم المفتوح يستخدم عنوانًا، وربما `عرض الكل`، ثم محتواه مباشرة.

#### المسافات

| العلاقة | المسافة |
|---|---:|
| داخل العناصر | `8–12px` |
| بين البطاقات | `12–16px` |
| بين الأقسام | `24–32px` |
| Padding الشاشة | `16–20px` |

#### مرفوض

- بطاقة لكل معلومة.
- بطاقات متساوية القوة في كل الشاشة.
- بطاقات داخل بطاقات.
- ظل حول كل بطاقة.
- عدة كتل مهيمنة في الشاشة نفسها.
- ألوان بطاقات عشوائية.

### 10.3 Tabs, Filters and Chips

#### Tabs

- Tabs نصية مباشرة على الـCanvas.
- المحدد: Indigo + SemiBold + خط سفلي `3px`.
- غير المحدد: Secondary Text.
- مساحة لمس `44px` على الأقل.
- يظهر `2–4` Tabs عادة.
- عند الزيادة، يسمح بالتمرير الأفقي.
- لا تستخدم Pills ولا بطاقة خلف مجموعة Tabs.
- Tabs للتنقل بين Views، وليست Actions.

#### Filter Command Button

لا يستخدم Micro صف Chips للفلاتر الرئيسية. يستخدم زر أمر واضح:

- أيقونة Phosphor Funnel.
- تسمية `تصفية`.
- Badge صغير بلون Indigo لعدد الفلاتر النشطة.
- الارتفاع `40–44px`.
- نصف القطر `12px`.
- سطح أبيض وحد واضح.
- لا ظل إلا عند فتح الطبقة المرتبطة.

#### Contextual Filter Panel

- يرتبط بصريًا بزر التصفية ويظهر أسفله.
- سطح أبيض.
- نصف القطر `16–18px`.
- Border وظل قصير يوضح الطبقة.
- هامش شاشة `16px`.
- ليس Full Screen.
- تعرض الصفوف اسم الفلتر، والقيمة الحالية، وChevron.
- الخيارات الفرعية تُفتح داخل اللوحة.
- Footer: `إعادة تعيين` كنص + زر رئيسي مثل `عرض 24 نتيجة`.
- يمكن الإغلاق بالنقر خارج اللوحة عندما لا يسبب ذلك فقدان قرار حساس.

#### الاختيارات البسيطة والمعقدة

- اختيار واحد بسيط، مثل الفترة أو الترتيب، يستخدم Custom Dropdown Button.
- لا يستخدم Native Browser Select.
- Bottom Sheet يستخدم فقط لنطاق تاريخ معقد، أو Multi-select، أو قائمة طويلة.
- الترتيب منفصل بصريًا ووظيفيًا عن التصفية.

#### ملخص الفلاتر

- يظهر كسطر واحد، مثل: `العرض الحالي: هذا الشهر · غير محصل · عميل واحد`.
- يرافقه `تعديل` أو `مسح`.
- إذا طال الملخص، يستخدم وصف مثل: `3 فلاتر مطبقة`.

#### Chips

Chips ليست نظام الفلاتر الرئيسي، وتُحصر في:

- Status Chip غير تفاعلي.
- Selection Chip داخل سياق مركز أو Form.
- Removable Chip.

#### Segmented Control

- يستخدم فقط لـ`2–3` Views متبادلة، مثل يوم / أسبوع / شهر.
- داخل حاوية واحدة.
- نصف القطر `12–14px`.
- لا يستخدم كتنقل رئيسي.
- لا يتكرر مع Tabs للغرض نفسه.

#### مرفوض

- Pills في كل مكان.
- الشكل نفسه للـTabs والفلاتر والحالات.
- Bottom Sheet لاختيار تافه.
- Native Dropdown.
- بطاقات ضخمة للفلاتر.
- زر Filter بلا نص.
- تقليد Material أو iOS حرفيًا.

### 10.4 Status and Feedback

#### Micro Signal System

علامة خاصة بـMicro مكوّنة من أربع وحدات، وتظهر مع نص الحالة:

| الحالة | تمثيل الإشارة |
|---|---|
| مكتمل | أربع وحدات ممتلئة |
| جزئي | وحدتان ممتلئتان ووحدتان Outline |
| قيد التنفيذ | وحدة نشطة وثلاث هادئة |
| غير معروف | أربع وحدات Dashed / Outline |
| مسودة | وحدة ممتلئة والباقي فارغ |
| خطأ | اختلال في انتظام الوحدات مع `×` صغيرة |
| محفوظ محليًا | الوحدات داخل Outline جهاز |

#### مواضع الاستخدام

- داخل القائمة: الإشارة + نص الحالة، بلا خلفية أو Pill أو بطاقة.
- داخل البطاقة: تندمج الحالة في بنيتها، مع تغير خفيف في السطح أو الحافة فقط.
- الحالة المهمة تظهر داخل Header المحتوى، لا كـAlert Card منفصل بلا داعٍ.

#### Action Echo

التغذية الراجعة تحدث عند مصدر الفعل:

1. الزر يتفاعل ويدخل Loading.
2. العنصر المتأثر يتغير في موقعه الحقيقي.
3. تتحدث Micro Signal.
4. تبرز القيمة أو الصف الذي تغير لفترة قصيرة.
5. يظهر نص قصير مثل `تم التحديث` عند الحاجة.

لا يستخدم Toast إلا عندما يوجد Undo أو عندما يقع أثر العملية خارج الشاشة.

#### الحركة

- Press: `80–120ms` مع تغير خفيف في السطح أو الحد، دون Bounce.
- Success: تنتهي العملية، تمتلئ الوحدات بالتتابع، تظهر النتيجة، يبرز العنصر المتغير ثم يستقر؛ المجموع `280–420ms`.
- Partial: تمتلئ بعض الوحدات ويظهر المنجز والمتبقي، دون احتفال.
- Unknown: لا حركة اكتمال؛ تظهر الإشارة المتقطعة ثم السبب والإجراء المتاح.
- Error: يتوقف التحميل، تبقى المدخلات، وتظهر علامة الانقطاع قرب المشكلة؛ لا Shake.
- Local-only: يظهر أثر الحفظ ثم إطار الجهاز حول الإشارة؛ لا نجاح عام يوحي بالمزامنة.
- Dropdown: `180–220ms` وينفتح من مصدره.
- Bottom Sheet: `220–280ms`.
- إضافة صف: يظهر في موقعه الحقيقي وتتحرك الصفوف المحيطة.
- حذف صف: ينهار في مكانه مع Undo عندما يكون ممكنًا.
- انتقال الشاشة باتجاه RTL: `240–320ms`.
- إيقاع الاختيار: `140–180ms`.
- Stagger: `40–60ms`.
- الخروج أسرع من الدخول.

#### مرفوض

- Confetti.
- علامة نجاح ضخمة.
- Toast بعد كل عملية.
- Shake أو Bounce.
- قفزة رقمية استعراضية.
- Lottie جاهز.
- حركة Loop مستمرة.
- Full-screen Fade كحل عام.
- Status Pills كهوية الحالة الأساسية.
- مربع ملون خلف كل أيقونة حالة.

### 10.5 Loading, Empty and Error

#### Loading

- يظهر Shell والـCanvas والعناصر الثابتة فورًا.
- Skeleton يطابق هندسة المحتوى الحقيقي ويقتصر على المنطقة المنتظرة.
- يحافظ Layout على ثباته.
- تستخدم إشارة Micro ذات الوحدات الأربع للتحميل الصغير.
- في Reduced Motion تظهر حالة ثابتة مع نص واضح.

مرفوض: Fake Progress، Shimmer قوي، Skeleton عام لا يطابق المحتوى، Spinner كبير في منتصف الشاشة، أو إخفاء المحتوى القديم قبل وصول الجديد.

#### Empty States

تُفصل الحالات التالية:

- First-use empty.
- Natural empty.
- No results.
- Completed / cleared.

القواعد:

- First-use: تكوين Micro خفيف أو أيقونة سياقية `48–72px` + عنوان واضح + سطر واحد + إجراء رئيسي واحد.
- Natural empty: هادئة، ولا تفرض CTA غير ضروري.
- No results: تبقي الفلاتر ظاهرة وتقدم `مسح الفلاتر`، ولا تعرض إنشاء عنصر جديد كحل افتراضي.
- Completed: تستخدم إشارة Micro المكتملة، دون Confetti أو أولوية مصطنعة.
- تظهر الحالة في مكان المحتوى المتوقع، لا في بطاقة عامة منفصلة.

مرفوض: Illustration عامة ضخمة، شخصيات كرتونية جاهزة، أو نص `لا توجد بيانات` بلا سياق.

#### Errors

- الخطأ Inline عند مصدره أولًا.
- خطأ القسم يبقى داخل القسم مع Retry.
- الخطأ Blocking لا يستخدم إلا إذا تعذر الاستمرار.
- تُحفظ المدخلات.
- يوضح النص ما فشل، وهل حُفظت البيانات، وما الإجراء التالي.
- يُفرق بين غير محفوظ، محفوظ محليًا، وغير مرسل.

مرفوض: شاشة حمراء كاملة، `حدث خطأ` بلا تفسير، بطاقة حمراء لكل خطأ، أو Shake.

#### انتقالات الحالة

- Skeleton إلى المحتوى في مكانه: Fade خفيف `160–220ms`.
- Empty إلى أول عنصر: `240–320ms`.
- Retry: تتحول الإشارة إلى Loading ثم يظهر المحتوى في المنطقة نفسها.

### 10.6 Sheets, Dialogs, Menus and Notices

#### Contextual Sheet

- يستخدم لـMulti-select، أو تاريخ مخصص، أو مهمة سياقية قصيرة.
- يدخل من الأسفل.
- سطح أبيض ونصف قطر علوي `24px`.
- الحد الأعلى نحو `85%` من الشاشة، مع تفضيل ارتفاع المحتوى.
- العنوان يمينًا والإغلاق يسارًا.
- محتوى مفتوح، لا بطاقات متداخلة.
- Footer ثابت عندما يوجد قرار يجب تأكيده.
- يراعي Safe Area ولوحة المفاتيح.
- السحب للأسفل متاح فقط عندما لا يسبب فقدانًا خطرًا.
- عند وجود تعديلات غير محفوظة يظهر قرار مناسب.
- زر Back يغلق الـSheet أولًا.
- يمنع Sheet فوق Sheet.

المهام الطويلة، متعددة الخطوات، أو الحساسة ماليًا تستخدم Full-screen Layer بدل Sheet متضخم.

#### Dialog

- يستخدم للقرارات أو العوائق فقط: حذف، عكس، تجاهل، تغييرات غير محفوظة، أو فعل غير قابل للتراجع.
- العرض `88–92%` من الشاشة.
- نصف القطر `20px`.
- لا أيقونة ضخمة داخل دائرة.
- العنوان يوضح القرار، والنص يوضح النتيجة.
- لا تستخدم تسميات `نعم / لا`؛ تسمى الأفعال صراحة.
- الإجراء الآمن والمدمر غير متساويين بصريًا.

#### Menus and Popovers

- ترتبط بمصدرها.
- نصف القطر `14–16px`.
- ارتفاع الصف `44–48px`.
- يظهر Check بجوار الاختيار الحالي.
- يفصل الإجراء المدمر عن بقية الخيارات.

#### Contextual Notice

- يندمج مع القسم.
- يستخدم Micro Signal + عنوان + شرح قصير + إجراء اختياري.
- يستخدم سطحًا دلاليًا خفيفًا أو Divider، لا Border + Shadow بلا ضرورة.
- لا يمكن إغلاق إشعار ضروري ماليًا قبل معالجة سببه.

#### Global Notice

- نادر ومخصص للحالة العامة التي تؤثر على النظام كله.
- يندمج أسفل App Bar.
- يحتوي إجراء واحدًا.
- يبقى حتى حل السبب.
- لا يستخدم للعروض أو المحتوى الترويجي.

#### Overlay and Accessibility

- Scrim: `rgba(15,17,23,0.55)`.
- لا Blur قوي.
- الإغلاق خارج الطبقة متاح فقط عندما يكون آمنًا.
- ينتقل Focus إلى الطبقة ويعود إلى العنصر الذي فتحها.

#### الحركة

- Sheet: `220–280ms` من الأسفل مع Scrim تدريجي؛ الإغلاق أسرع.
- Dialog: `160–220ms` مع Fade وScale خفيف من `98%` إلى `100%`، بلا Bounce.
- Popover: `160–200ms` وينفتح من مصدره.
- أثناء القرار، يحمل الزر في مكانه وتبقى الطبقة حتى ظهور النتيجة.
- عند النجاح تغلق الطبقة ثم يحدث Action Echo في الشاشة الأصلية.
- عند الخطأ تبقى الطبقة وتحفظ المدخلات.

---

## 11. Navigation UI

**الحالة: مغلق بالكامل**

### 11.1 البنية الرئيسية

يتكون التنقل الرئيسي من خمسة تبويبات ثابتة ومتساوية:

1. مشروعي الآن.
2. العمل.
3. المالية.
4. أدواتي.
5. السوق.

`منتجاتي وخدماتي` شاشة سياقية يمكن الوصول إليها من أكثر من موضع، وليست تبويبًا سادسًا.

### 11.2 الشريط السفلي

- شريط مدمج مع حافة الشاشة، وليس Floating Pill.
- خلفية بيضاء متصلة بالحافة السفلية.
- Divider علوي خفيف بدل الظل الكبير.
- الارتفاع البصري: `68–74px` إضافة إلى Safe Area.
- التبويبات الخمسة متساوية، ولا يوجد تبويب أكبر من البقية.
- لا يوجد زر مركزي بارز أو دائرة عائمة.
- جميع التسميات ظاهرة دائمًا.
- لا توضع الأيقونات داخل دوائر أو كبسولات.
- مساحة اللمس لكل تبويب لا تقل عن `48×48px`.

#### حالة الاختيار

- الأيقونة النشطة تستخدم `Fill` بحجم `24px` وبلون Indigo.
- التسمية النشطة تستخدم `11–12px SemiBold` وIndigo.
- تظهر علامة Micro الرباعية كإشارة اختيار خاصة بالهوية.
- حركة الاختيار `140–180ms` بلا Bounce أو تغيير استعراضي في الحجم.
- الأيقونة غير النشطة تستخدم `Regular` بحجم `24px`.
- التسمية غير النشطة تستخدم `11–12px Medium` وSecondary Text.
- لا تعتمد الحالة النشطة على اللون وحده؛ تختلف الأيقونة والوزن وعلامة Micro أيضًا.

### 11.3 أيقونات التبويبات

| التبويب | أيقونة Phosphor | المعنى |
|---|---|---|
| مشروعي الآن | `Gauge` | قراءة الوضع الحالي بسرعة |
| العمل | `Briefcase` | العمليات اليومية وإدارة العمل |
| المالية | `Wallet` | الكاش والديون والحركة المالية |
| أدواتي | `Toolbox` | الأدوات المساندة |
| السوق | `Storefront` | السوق والفرص والخدمات |

### 11.4 سلوك التبويبات

- الضغط على تبويب آخر ينقل إلى الشاشة الرئيسية لذلك القسم.
- الضغط على التبويب النشط أثناء وجود المستخدم في شاشة فرعية يعيده إلى أصل القسم.
- الضغط عليه وهو في أصل القسم يصعد إلى أعلى الصفحة.
- يحتفظ كل قسم مؤقتًا بموضع التمرير والفلاتر وحالته عند الانتقال بين التبويبات.
- لا يستخدم Double Tap أو Long Press لأي وظيفة أساسية.
- لا يظهر Loading داخل أيقونة التبويب؛ يظهر داخل محتوى الشاشة.

### 11.5 Badges

- تستخدم فقط لعدد واضح يحتاج انتباهًا أو إجراءً.
- لا تعرض مبالغ مالية.
- لا تستخدم نقطة ملونة غامضة.
- الحد الأعلى الظاهر `99+`.
- لا يوضع Badge على أكثر من تبويبين عادةً؛ تعدد الـBadges مؤشر على مشكلة في الأولويات.
- يعلن قارئ الشاشة معنى العدد، لا الرقم وحده.

### 11.6 السوق

- يبقى `السوق` تبويبًا خامسًا ثابتًا وظاهرًا.
- لا يظهر Disabled أو باهتًا.
- يفتح كشاشة طبيعية.
- يجب أن يقدم محتوى مفيدًا عند الإطلاق، حتى إن بقيت بعض خصائصه المستقبلية غير متاحة.
- لا تكون شاشة السوق مجرد رسالة `قريبًا`.
- الخصائص غير المتاحة تشرح داخل الشاشة، لا على أيقونة التبويب.

### 11.7 المنطقة العلوية العالمية

#### التكوين

- لا يظهر اسم الصفحة داخل المنطقة العلوية.
- لا يظهر شعار Micro أو اسمه.
- يمينًا: زر الحساب والنظام.
- يسارًا: `التوصيل` و`اسأل Micro`.
- لا يوجد Hamburger أو Chevron.
- عنوان الصفحة يظهر داخل المحتوى أسفل المنطقة العلوية.
- لا تضاف أيقونات البحث أو التنبيهات أو الإعدادات عشوائيًا إلى الشريط.

#### زر الحساب والنظام

- الأيقونة: Phosphor `UserCircleGear`.
- الحجم البصري: `26px`.
- مساحة اللمس: `44×44px` على الأقل.
- اللون الطبيعي: Deep Ink.
- عند فتح اللوحة: Indigo مع خلفية Soft Indigo محدودة ونصف قطر `12px`.
- Accessible Label: `الحساب وإعدادات النظام`.
- العنصر ليس شعارًا؛ هو مشغل صريح للحساب والمشروع والنظام.

اختير `UserCircleGear` بدل البدائل للأسباب التالية:

- شعار Micro لا يوضح وجود قائمة.
- Gear منفرد يوحي بالإعدادات فقط.
- أربعة خطوط تبدو كـHamburger وتنقل جانبي.
- أربعة مربعات توحي بقائمة تطبيقات.
- صورة المستخدم لا تناسب الحسابات غير المكتملة دائمًا.

#### التوصيل

- أيقونة Phosphor `Truck` مع النص `التوصيل`.
- اللون Deep Ink.
- زر نصي مدمج بلا Pill أو خلفية دائمة.

#### اسأل Micro

- علامة Micro الرباعية مع النص `اسأل Micro`.
- اللون Indigo لإعطائه أولوية أعلى قليلًا.
- لا تستخدم Sparkles أو Robot.
- لا يظهر كزر Gradient أو كبسولة كبيرة.

### 11.8 الاندماج مع Status Bar

- يستخدم التصميم `Edge-to-Edge Unified Header`.
- يمتد لون الـCanvas خلف Status Bar.
- يظهر Status Bar شفافًا بصريًا مع أيقونات نظام داكنة.
- يبدأ المحتوى بعد Safe Area ولا يدخل تحت الكاميرا أو Notch.
- الارتفاع الكلي: `Safe Area العلوية + 56px`.
- لا يوجد اختلاف لون أو خط فاصل بين شريط الهاتف ومنطقة Micro في الوضع الطبيعي.
- لا توجد خلفية بطاقة أو زوايا أو ظل.
- عند التمرير تتحول المنطقة العلوية وStatus Bar معًا من Canvas Cool Cloud إلى الأبيض.
- يظهر Divider خفيف أسفل منطقة Micro فقط.
- مدة الانتقال `160–200ms`.
- لا Blur ولا Glassmorphism.
- Light Mode فقط.

### 11.9 Header-Connected Account Panel

لوحة الحساب ليست Side Drawer ولا Bottom Sheet ولا Popover عائمًا. إنها امتداد طبيعي للمنطقة العلوية.

#### الشكل

- عند الضغط على `UserCircleGear` تتحول المنطقة العلوية إلى الأبيض.
- يبقى الزر في مكانه ويتحول إلى Indigo.
- تتمدد اللوحة مباشرة من أسفل المنطقة العلوية.
- تشغل عرض الشاشة كاملًا.
- لا توجد زوايا علوية لأنها متصلة بالـHeader.
- الزوايا السفلية فقط: `24px`.
- الارتفاع حسب المحتوى، عادةً `280–360px`.
- يظهر Overlay خلفها وتبقى الشاشة الأصلية مرئية.
- لا تغطي Status Bar أو المنطقة العلوية.

#### تعريف المشروع

- مساحة مفتوحة وليست بطاقة داخلية.
- يظهر اسم المشروع.
- يظهر نوع النشاط عند توفره.
- تظهر Micro Signal وحالة اكتمال الحساب.
- عند عدم الاكتمال يظهر `بيانات المشروع غير مكتملة` وإجراء `إكمال البيانات`.
- لا يستخدم اللون الأحمر؛ عدم الاكتمال ليس خطأ.
- لا تعرض نسبة اكتمال مصطنعة.
- إذا لم يوجد اسم مشروع، يستخدم `مشروعي` مؤقتًا بدل `مستخدم جديد`.

#### عناصر القائمة

| العنصر | الأيقونة |
|---|---|
| حسابي | `UserCircle` |
| بيانات المشروع | `Storefront` |
| إعدادات النظام | `SlidersHorizontal` |

- تظهر العناصر كصفوف مفتوحة داخل مجموعة واحدة.
- الأيقونة يمينًا بحجم `22px`.
- العنوان والوصف القصير في الوسط.
- Chevron يسارًا.
- ارتفاع الصف `52–56px`.
- الصف كامل قابل للنقر.
- لا توضع الأيقونات داخل مربعات ملونة.
- تستخدم الفواصل بين المجموعات، لا بطاقات مستقلة لكل عنصر.
- إعدادات الحساب تبقى ظاهرة وفعالة حتى إذا لم يكتمل الحساب.
- لا يوجد Dark Mode Toggle.

#### تسجيل الخروج

- يظهر منفصلًا أسفل المجموعة بعد Divider ومسافة واضحة.
- الأيقونة `SignOut`.
- النص بلون Danger دون خلفية حمراء.
- لا يظهر كزر رئيسي.
- يفتح Dialog تأكيد صريح ولا ينفذ فورًا.

#### الحركة والإغلاق

- تتمدد اللوحة من أسفل الـHeader خلال `220–260ms`.
- يدخل المحتوى بعد بداية التمدد بفارق `40–60ms`.
- الإغلاق أسرع: `180–220ms`.
- الزر نفسه يغلق اللوحة.
- الضغط خارجها أو زر Back يغلقها عندما يكون ذلك آمنًا.
- لا يوجد زر `×` إضافي.
- يعود Focus إلى زر الحساب بعد الإغلاق.

### 11.10 مستويات التنقل

#### المستوى الأول: الشاشات الرئيسية

- يظهر الشريط العلوي العالمي.
- يظهر الشريط السفلي.
- عنوان الشاشة داخل المحتوى.

#### المستوى الثاني: شاشات التفاصيل

- يستبدل الشريط العالمي مؤقتًا بشريط سياقي بسيط.
- يظهر يمينًا `ArrowRight + رجوع`.
- لا يظهر اسم الصفحة داخل الشريط.
- يظهر عنوان الصفحة داخل المحتوى.
- الإجراءات السياقية الضرورية فقط تظهر يسارًا.
- يختفي الشريط السفلي لمنع خلط المستوى الفرعي بالتنقل الرئيسي.

#### المستوى الثالث: المهمة المركزة

- لا يظهر الشريط السفلي.
- يظهر يمينًا `إلغاء` أو الإغلاق المناسب.
- عنوان المهمة داخل المحتوى.
- الإجراء النهائي في موقع ثابت وواضح، لا داخل الشريط العلوي.
- الخروج مع بيانات غير محفوظة يفتح Dialog يشرح النتيجة.

### 11.11 قواعد الرجوع

- مساحة زر الرجوع لا تقل عن `44×44px`.
- السهم يشير إلى اليمين في RTL.
- زر Back في الهاتف ينفذ السلوك نفسه.
- الرجوع يعيد موضع التمرير والفلاتر والعنصر الذي فتح التفاصيل.
- لا يعيد المستخدم إلى بداية القائمة بلا سبب.
- لا تستخدم Breadcrumbs على الهاتف.
- لا يظهر أكثر من زر رجوع أو إغلاق في الشاشة نفسها.

### 11.12 Swipe بين الصفحات الرئيسية

Swipe معتمد بين التبويبات الخمسة الرئيسية فقط:

`مشروعي الآن ← العمل ← المالية ← أدواتي ← السوق`

#### السلوك

- يتحرك المحتوى مع الإصبع: السحب يسارًا يحركه يسارًا، والسحب يمينًا يحركه يمينًا.
- الصفحة التي تظهر تتحدد بموقعها المرئي في Pager العربي وسلوك المنصة؛ لا نعتمد قاعدة ثابتة تربط `يسار` بكلمة `التالي`. المرجع التفصيلي في `16.1`.
- عند أول وآخر تبويب توجد مقاومة خفيفة ولا يحدث Loop.
- يتحرك المحتوى مع الإصبع بدل انتظار انتهاء السحب.
- تتغير حالة الشريط السفلي بعد اكتمال الانتقال.
- إذا لم يكتمل السحب، تعود الصفحة إلى موقعها بهدوء.
- يستقر الانتقال خلال `180–260ms` حسب سرعة السحب والمسافة.
- Swipe وسيلة إضافية، وليس بديلًا عن الضغط على التبويبات.
- لا Swipe أثناء Loading أو وجود طبقة منبثقة.

#### مناطق تعطيل Swipe

- البطاقات الأفقية.
- الرسوم البيانية التفاعلية.
- Sliders.
- الصور القابلة للسحب.
- لوحات الفلاتر.
- Dialogs وSheets.
- النماذج والمهام المالية.
- شاشات التفاصيل والمستويات الفرعية.

لا توجد حركة Bounce قوية أو Overscroll ملون.

### 11.13 نظام الانتقالات

#### انتقالات الشاشات

- الدخول إلى مستوى أعمق يتبع اتجاه RTL.
- الرجوع يعكس الحركة نفسها.
- المدة `240–320ms`.
- لا يستخدم Full-screen Fade كحل عام.
- لا Bounce أو انتقال ثلاثي الأبعاد.
- يمكن إبراز العنصر الذي فتح التفاصيل بخفة عند الرجوع إليه.

#### انتقالات الطبقات المنبثقة

| العنصر | طريقة الظهور | المدة |
|---|---|---:|
| قائمة صغيرة مرتبطة بزر | تتمدد من موقع الزر مع Fade | `160–200ms` |
| Dropdown | ينفتح أسفل مصدره باتجاه المحتوى | `180–220ms` |
| لوحة الفلاتر | تتمدد من زر التصفية مع Fade خفيف | `180–220ms` |
| لوحة الحساب العلوية | تتمدد من أسفل الـHeader | `220–260ms` |
| Bottom Sheet | يصعد من الأسفل مع Scrim تدريجي | `220–280ms` |
| Dialog | Fade مع Scale من `98%` إلى `100%` | `160–220ms` |
| Full-screen Layer | انتقال شاشة باتجاه RTL | `240–320ms` |
| إشعار داخل المحتوى | يتمدد داخل موقعه ويدفع المحتوى بهدوء | `180–240ms` |
| Undo Message | يدخل من أسفل فوق الشريط السفلي | `180–220ms` |

- الإغلاق أسرع من الفتح بنحو `30–50ms`.
- الحركة تشرح مصدر العنصر: الزر، المنتصف، الأسفل، مستوى جديد، أو موضع المحتوى.
- لا يظهر عنصر منبثق من مكان لا يرتبط بمصدره أو وظيفته.

#### الخلفية والتفاعل

- يظهر Scrim تدريجيًا مع Dialog وSheet ولوحة الحساب.
- لا Blur.
- Popover صغير لا يحتاج تعتيمًا قويًا.
- عند فتح طبقة، تتوقف العناصر خلفها عن استقبال اللمس.
- يبقى Bottom Navigation مرئيًا خلف التعتيم لكنه غير قابل للتفاعل.
- الضغط خارج الطبقة يغلقها فقط عندما لا توجد بيانات أو قرارات قد تضيع.

#### منع تكديس الطبقات

- لا Dialog فوق Dialog.
- لا Sheet فوق Sheet.
- المهمة الأكبر تتحول إلى Full-screen Layer بدل تكديس الطبقات.
- فتح عنصر منبثق جديد يغلق العنصر السياقي السابق.
- لوحة المفاتيح لا تغطي الإجراء النهائي.

#### النجاح والخطأ

- عند الخطأ لا تغلق الطبقة، وتحفظ المدخلات، ويظهر الخطأ قرب مصدره بلا Shake.
- عند النجاح يحمل الزر في مكانه، ثم تغلق الطبقة، ثم يظهر الأثر في الشاشة الأصلية باستخدام Action Echo.

#### Reduced Motion

- تزال الحركة الاتجاهية والـScale.
- يبقى Fade قصيرًا `100–150ms`.
- لا تختفي معلومات الحالة أو التغذية الراجعة.

### 11.14 الوصول والاستخدام على الهاتف

- إجراءات المنطقة العلوية لا تقل عن `44×44px`.
- عناصر الشريط السفلي لا تقل عن `48×48px`.
- الترتيب البصري والقرائي يبدأ من اليمين.
- يعلن قارئ الشاشة اسم التبويب وحالته وعدد العناصر المهمة.
- مثال: `العمل، محدد، 3 عناصر بحاجة للمراجعة`.
- Safe Area مطبقة أعلى وأسفل الشاشة.
- لا يلامس الشريط السفلي مؤشر تنقل الهاتف.
- التصميم الأساسي Portrait.
- يبقى Landscape قابلًا للاستخدام دون قص الأزرار أو إخفاء الإجراءات.

#### تكبير النص

- في الوضع الطبيعي تبقى التسميات في سطر واحد.
- عند تكبير النص من إعدادات الهاتف يسمح بزيادة ارتفاع الشريط.
- عند المستويات الكبيرة جدًا يمكن للتسمية أن تصبح سطرين بدل قصها.
- لا تستبدل النصوص بأيقونات فقط عند التكبير.

### 11.15 مرفوض في Navigation UI

- Floating Pill للشريط السفلي.
- زر مركزي أكبر من التبويبات.
- تنقل يعتمد على الأيقونات وحدها.
- Hamburger أو Side Drawer يكرر التنقل الرئيسي.
- استخدام شعار Micro كزر قائمة غامض.
- اسم الصفحة داخل الشريط العلوي.
- Disabled Tab للسوق.
- Swipe داخل النماذج أو المكونات الأفقية.
- Double Tap أو Long Press لوظيفة أساسية.
- الحركة نفسها لكل الطبقات.
- Bounce وZoom كبير ودوران وBlur متحرك.
- تكديس Dialogs وSheets.

---

## 12. Form UI

**الحالة: مغلق بالكامل**  
**اسم الاتجاه:** `Guided Open Forms`

### 12.1 بنية النماذج

تعتمد Micro ثلاثة أنماط فقط:

#### النموذج السريع

- للبيع والمصروف والتحصيل والعمليات المتكررة.
- شاشة واحدة قصيرة تتضمن `3–5` مدخلات أساسية ظاهرة.
- التفاصيل غير الضرورية تحت `تفاصيل إضافية`.
- لا خطوات متعددة لعملية بسيطة.
- الإجراء النهائي ثابت فوق Safe Area، ويبقى ظاهرًا مع لوحة المفاتيح.

#### النموذج القياسي

- تمرير رأسي واحد.
- أقسام مفتوحة بعناوين واضحة، لا بطاقة حول كل قسم.
- المسافة بين الأقسام `24–32px`.
- الحقول المرتبطة متجاورة.
- القسم الواحد يحتوي عادةً `3–6` حقول.

#### النموذج الطويل أو المعتمد على خطوات

- يستخدم فقط عندما تعتمد المعلومات على بعضها أو تكون المهمة معقدة.
- `2–4` خطوات حقيقية.
- يظهر نص مثل `الخطوة 2 من 4`.
- زر `التالي` أساسي و`رجوع` نصي.
- تحفظ القيم عند التنقل بين الخطوات.
- لا Stepper ضخم أو دوائر مرقمة أعلى الشاشة.

#### ترتيب المعلومات

1. المطلوب لإكمال المهمة.
2. المعلومة المالية أو التشغيلية الأساسية.
3. الخيارات المساندة.
4. الملاحظات والتفاصيل الإضافية.
5. الأثر قبل التأكيد عندما تكون العملية مالية.

تظهر الحقول المشروطة فقط عندما تصبح مرتبطة باختيار المستخدم.

### 12.2 شريط الإجراء السفلي

- سطح أبيض مدمج مع أسفل الشاشة.
- Divider علوي خفيف.
- Padding `12–16px`.
- زر رئيسي واحد باسم النتيجة، وإجراء ثانوي نصي عند الحاجة.
- لا Floating Pill ولا بطاقة حول الشريط.
- يرتفع فوق لوحة المفاتيح.
- يحتوي النموذج Bottom Padding يعادل ارتفاعه حتى لا يغطي آخر حقل.
- Citrus لا يستخدم لتأكيد عملية مالية.

### 12.3 بنية الحقل وشكله

يتكون الحقل من عنوان ثابت، مساحة الإدخال، ثم توضيح أو رسالة خطأ عند الحاجة.

| الخاصية | القرار |
|---|---|
| الارتفاع | `52–56px` |
| نصف القطر | `14px` |
| Border | `1px` |
| Padding أفقي | `14–16px` |
| النص المدخل | `16px Regular` |
| عنوان الحقل | `13–14px SemiBold` |
| النص المساعد | `12–13px Regular` |
| المسافة بين العنوان والحقل | `6–8px` |
| المسافة بين الحقول | `16–20px` |

- الخلفية بيضاء والحد `#D8DDE8` والنص Deep Ink.
- Placeholder ثانوي واضح، وليس بديلًا عن العنوان.
- لا Shadow حول الحقول.
- الحقل يشغل العرض المتاح عادةً.

#### حالات الحقل

- **Default:** حد خفيف وسطح أبيض.
- **Focus:** Border Indigo بسماكة بصرية `2px` وحلقة Soft Indigo؛ لا يتغير الحجم.
- **Filled:** قريب من Default؛ لا يلون لمجرد وجود قيمة.
- **Error:** حد Danger + أيقونة صغيرة + رسالة محددة، بلا خلفية حمراء أو Shake.
- **Disabled:** Secondary Surface ونص Disabled وغير تفاعلي.
- **Read-only:** قيمة واضحة مع `للعرض فقط` أو قفل عند الحاجة؛ لا يبدو Disabled.
- **Success:** محصور في تحقق فعلي، لا بعد كل إدخال صحيح.

#### المطلوب والاختياري

- لا نجمة حمراء على معظم الحقول.
- الحقل غير الإلزامي يحمل `(اختياري)`.
- الحقل المطلوب الناقص يوضح خطأه بعد محاولة المتابعة.

#### الأيقونات والحقول المتجاورة

- الأيقونة تستخدم عندما تضيف معنى فقط.
- الأيقونة الدلالية يمينًا، والمسح أو إظهار كلمة المرور يسارًا.
- مساحة لمس الزر الداخلي `44×44px` على الأقل.
- الأصل حقل واحد في الصف.
- يسمح بحقلين قصيرين مرتبطين، مثل الكمية والوحدة أو التاريخ والوقت، ويعودان إلى صفين عند ضيق المساحة.

#### Text Area

- الارتفاع الأولي `96–112px`.
- يتمدد إلى حد معقول.
- عداد الأحرف يظهر فقط إذا وجد حد فعلي.
- لا يغطي الإجراء السفلي.

### 12.4 أنواع الإدخال والاختيار

#### النص والأرقام

- النص العربي يبدأ من اليمين.
- الرقم والهاتف والبريد والأكواد معزولة `LTR`.
- جميع الأرقام المعروضة `0–9`.
- زر المسح يظهر بعد وجود قيمة.

#### المبالغ

- العملة `د.أ` داخل الطرف المقابل من الحقل.
- مثال `1,250.50 د.أ`.
- لوحة أرقام عشرية.
- تنسيق الآلاف بعد انتهاء الإدخال حتى لا يتحرك المؤشر.
- لا يظهر رمز العملة داخل Placeholder.
- دقة المنازل النهائية يحسمها `Financial UI`.

#### اختيار واحد

- خياران قصيران متكرران: Segmented Control، مثل `نقدي / آجل`.
- القرار الذي يحتاج شرحًا: Radio Rows.
- `3–5` خيارات قصيرة: Radio Rows مفتوحة، والصف كامل قابل للنقر.
- لا Chips للقرارات الأساسية.

#### القوائم

- قائمة قصيرة حتى `5–7` خيارات: Custom Dropdown مرتبط بالحقل، والخيار الحالي يحمل Check.
- لا Native Browser Select ولا Bottom Sheet لقائمة قصيرة.
- القوائم الطويلة مثل العملاء والمنتجات والموردين تفتح Bottom Sheet مرتفعًا أو Full-screen Selection مع بحث ثابت وصفوف مفتوحة.
- عند العودة يظهر الاختيار في الحقل الأصلي.
- لا Dropdown طويل يتجاوز الشاشة ولا Nested Scroll.

#### التاريخ والوقت

- Picker مناسب للنظام؛ لا كتابة يدوية افتراضية.
- التاريخ `DD/MM/YYYY` بأرقام إنجليزية.
- الوقت يتبع إعداد الهاتف مع أرقام إنجليزية.
- نطاق التاريخ المعقد يستخدم واجهة مخصصة.

#### متعدد الاختيار وToggle

- متعدد الاختيار يستخدم Checkbox Rows، ويعرض ملخصًا مثل `3 عناصر محددة`.
- Toggle محصور في إعداد يحدث فورًا.
- لا Toggle لطريقة دفع أو قرار مالي أو نعم/لا يحتاج حفظًا.

#### الحركة

- Dropdown: `180–220ms`.
- Radio وCheckbox: `140–180ms`.
- الحقل المشروط: `180–240ms` داخل موقعه.
- لا Bounce أو قفزة في الشاشة.

### 12.5 التحقق والأخطاء

#### توقيت التحقق

- لا خطأ أثناء كتابة الحرف الأول.
- الحقل المطلوب الفارغ: بعد محاولة المتابعة أو الحفظ.
- التنسيق الخاطئ: بعد مغادرة الحقل.
- تجاوز حد واضح: أثناء الكتابة عند بلوغه.
- التحقق الخارجي: بعد انتهائه.
- يختفي الخطأ عند تصحيح القيمة.

#### شكل ورسالة الخطأ

- Border Danger + أيقونة + رسالة أسفل الحقل.
- الرسالة محددة وقابلة للتصرف، مثل `أدخل مبلغًا أكبر من 0`.
- لا `قيمة غير صالحة` العامة، ولا خلفية حمراء أو Shake أو Toast.

#### عدة أخطاء

- تحفظ كل القيم.
- ينتقل العرض وFocus إلى أول خطأ.
- يظهر ملخص قصير أعلى النموذج، مثل `راجع 3 حقول لإكمال العملية`.
- تبقى الرسائل موضعية بجانب الحقول.

#### الخطأ والتحذير

- Error يمنع المتابعة عندما يتعذر الحفظ الصحيح.
- Warning يشرح نتيجة محتملة ويسمح بالمتابعة.
- التحذير يستخدم سطح Warning خفيفًا ونصًا واضحًا؛ لا Dialog إلا للأثر العالي أو صعب التراجع.

#### التحقق الجاري والفشل

- Micro Signal صغير قرب الحقل، لا Spinner كبير.
- عند فشل الحفظ يبقى النموذج والقيم، ويظهر الخطأ قرب منطقة الإجراء مع `إعادة المحاولة`.
- تفرق الرسالة بين: لم يحفظ، محفوظ محليًا، أو لم يرسل.
- لا نجاح عام إذا لم تكتمل المزامنة.

كل رسالة تجيب عن اثنين على الأقل: ما المشكلة؟ ماذا أفعل؟ ماذا حدث لبياناتي؟

### 12.6 لوحة المفاتيح وسلوك الإدخال

| الحقل | لوحة المفاتيح |
|---|---|
| الاسم والوصف | العربية أو لغة الجهاز |
| المبلغ | أرقام عشرية |
| الكمية الصحيحة | أرقام فقط |
| الهاتف | هاتف / أرقام |
| البريد | Email |
| البحث | Search |
| الملاحظات | نص متعدد الأسطر |

- إذا أدخلت لوحة الهاتف أرقامًا هندية، تحول فورًا إلى الإنجليزية دون خطأ.
- `التالي` ينقل إلى الحقل المنطقي التالي، و`تم` يغلق لوحة المفاتيح، و`بحث` للبحث فقط.
- Enter لا يحفظ عملية مالية أو يرسل نموذجًا حساسًا.
- Text Area يسمح بسطر جديد.
- الشاشة تحافظ على الحقل وعنوانه ورسالته والإجراء النهائي ظاهرين.
- في الشاشات الصغيرة ينكمش شريط الإجراء إلى زر رئيسي واحد.
- ترتيب Focus من أعلى لأسفل، وداخل الصف من اليمين إلى اليسار.

#### المبلغ والهاتف

- لا يبدأ المبلغ بـ`0.00` يجب حذفه.
- لا يعاد تنسيقه أثناء الكتابة بطريقة تحرك المؤشر.
- رقم الهاتف الأردني `LTR` وبأرقام إنجليزية، ويقبل الشكل المحلي `07XXXXXXXX`.
- يمكن تقسيمه بصريًا دون تغيير القيمة، ورمز الدولة منفصل عند الحاجة.

#### Autofocus

- يستخدم فقط عندما يكون أول فعل واضحًا مثل البحث أو إدخال مبلغ سريع.
- لا يستخدم إذا كان على المستخدم قراءة شرح، أو إذا أخفى معلومات مهمة، أو كان المدخل الأول اختيارًا.

### 12.7 الحفظ والإلغاء والخروج

#### الإجراء النهائي

- التسمية فعل + نتيجة: `تسجيل البيع`، `حفظ بيانات العميل`، `تحديث المنتج`.
- لا `حفظ` أو `موافق` وحدهما عندما يكون المعنى غير واضح.
- يبقى الزر قابلًا للضغط ليكشف الأخطاء، ولا يعطل بلا تفسير.
- أثناء التنفيذ يبقى الحجم ثابتًا، وتظهر Micro Loading Signal وتسمية مثل `جارٍ تسجيل البيع`، ويمنع التكرار.

#### النجاح والفشل

- لا شاشة نجاح لعملية بسيطة.
- عند انتهاء المهمة يعود المستخدم للسياق، ويتغير العنصر الحقيقي ويظهر Action Echo ونص دقيق.
- عند الفشل يبقى النموذج والقيم ويظهر `إعادة المحاولة` وحالة البيانات.

#### الخروج

- دون تغييرات: خروج مباشر بلا Dialog.
- مع تغييرات: Dialog بعنوان `هل تريد تجاهل التغييرات؟` وإجراءين `متابعة التعديل` و`تجاهل التغييرات`.
- لا `نعم / لا`.
- زر Back يتبع القاعدة نفسها، ويغلق طبقة الاختيار أولًا إن كانت مفتوحة.

#### المسودات

- للنماذج الطويلة فقط.
- تظهر `تم حفظ المسودة` دون إيحاء باكتمال المهمة.
- تعود القيم عند فتح النموذج.
- لا مسودة لعملية بسيطة يجب تسجيلها فورًا.

### 12.8 الحركة وRTL والوصول

- الحقول المشروطة تظهر في موضعها خلال `180–240ms` وتحرك ما تحتها بهدوء.
- `تفاصيل إضافية` تحفظ القيم، ويمكن أن تعرض عددها، وتتحرك `200–240ms`.
- انتقال الخطوات باتجاه RTL خلال `240–300ms`، والرجوع يعيد القيم.
- Reduced Motion يحول الحركة إلى Fade `100–150ms`.
- العربية يمينًا، والأرقام والهاتف والبريد والأكواد `LTR`.
- الأيقونة الدلالية يمينًا والمسح/إظهار كلمة المرور يسارًا.
- لا تعكس الأيقونات غير الاتجاهية.
- مناطق اللمس `44×44px` على الأقل، والضغط على عنوان Radio/Checkbox يحدد الخيار.
- قارئ الشاشة يقرأ: الاسم، النوع، القيمة، الاختيارية، ثم المساعدة أو الخطأ.
- Focus واضح ومنطقي، ويعود إلى المصدر بعد إغلاق الطبقة.
- تكبير النص يزيد الارتفاع وينقل الحقول المتجاورة إلى صفين؛ لا Ellipsis لعنوان أو خطأ.
- تستخدم دلالات Autofill الصحيحة ولا يمنع Password Manager.

### 12.9 مرفوض في Form UI

- بطاقة لكل مجموعة أو حقل.
- نموذج طويل بلا تقسيم أو Wizard لعملية بسيطة.
- إخفاء الأساسي داخل Accordion.
- Floating Label أو Underline-only أو Shadow لكل حقل.
- Placeholder بدل العنوان.
- حقول بعمودين بلا حاجة.
- كل الخيارات المتقدمة ظاهرة من البداية.
- أخضر لكل إدخال صحيح أو أحمر أثناء الكتابة الأولى.
- Toast لأخطاء الحقول أو `حدث خطأ` العامة.
- حذف القيم بعد الفشل.
- زر Disabled بلا تفسير أو Loading يتغير عرضه.
- حفظ مالي بزر Enter.
- رفض الأرقام الهندية بدل تحويلها.
- Autofocus في كل شاشة.
- منع Autofill أو استخدام لوحة حساسة مخصصة.

---

## 13. Financial UI

**الحالة: مغلق بالكامل**

### 13.1 حد النطاق

يقرر هذا القسم فقط ما يراه المستخدم: الترتيب، المكون، اللون، الحجم، الحالة، الحركة، والشرح المرئي.

لا يقرر:

- طريقة الحساب.
- السياسة المحاسبية.
- ما تنشئه العملية في قاعدة البيانات.
- صلاحيات التصحيح أو العكس أو الحذف.
- أثر العملية المحاسبي الفعلي.

أي مثال للأثر في هذا القسم يوضح **طريقة العرض** فقط، ويستقبل قيمه ومعناه من منطق المنتج المعتمد خارجه.

### 13.2 Question-First Financial Hierarchy

الاتجاه المرئي المعتمد هو `Question-First Financial UI`، وتدعمه داخليًا `Financial Truth Layers` كقاعدة نزاهة تمنع خلط المعاني.

يرى المستخدم إجابات مرتبة على أسئلته:

1. كم معي الآن؟
2. ماذا دخل وماذا خرج؟
3. ما لي وما عليّ؟
4. هل أنا ربحان أم خسران؟
5. هل النتيجة مكتملة؟

طبقات النزاهة الداخلية هي: المتاح الآن، حركة المشروع، الالتزامات، النتيجة، ودرجة اكتمالها.

- لا تظهر الأسئلة الخمسة كبطاقات متساوية.
- السؤال الأهم يقود الشاشة بكتلة قوية، والأسئلة المساندة بوحدات متوسطة أو صفوف.
- تختلف الأولوية حسب غرض الشاشة.
- لا يستخدم `الرصيد` دون تحديد نوعه.
- لا يقدم الربح كحقيقة مؤكدة عند نقص البيانات.

### 13.3 عرض المبالغ

- أرقام إنجليزية فقط، والعملة `د.أ`، والوحدة الرقمية `LTR`.
- مثال افتراضي `1,250.50 د.أ`.
- تستخدم Tabular Numbers للمحاذاة.
- منزلتان افتراضيًا، وتظهر الثالثة إذا كانت قيمة حقيقية مثل `1,250.125 د.أ`؛ لا تعرض `.000` بلا حاجة ولا تخفى قيمة ثالثة.
- الصفر الحقيقي `0.00 د.أ`، وغير المعروف `—` مع تفسير.
- علامة السالب الصحيحة `−` تستخدم في الصافي أو التغير، لا بدل اسم نوع الحركة.
- لا تختصر المبالغ المهمة إلى `1.2K`؛ يمكن اختصار محاور الرسم مع إتاحة القيمة الدقيقة.
- عنوان الصف يمينًا والقيمة يسارًا، والفترة قرب العنوان.
- Deep Ink هو لون الرقم الافتراضي؛ لا أخضر لكل دخل ولا أحمر لكل مصروف.
- التقدير يحمل نص `نتيجة تقديرية` أو `تقريبًا` وMicro Signal، لا الرمز `≈` وحده.

### 13.4 Financial Snapshot Deck

- بطاقة رقمية كبيرة واحدة ظاهرة في اللحظة نفسها.
- Swipe بين البطاقات، مع طرف/إشارة صغيرة توحي بالبطاقة التالية دون إظهار نصفها.
- مؤشر واضح مثل `1 من 4`، ويمكن دمجه مع وحدات Micro الأربع.
- رأس القسم: `نظرة مالية` يمينًا و`عرض الكل` يسارًا؛ لا يخفى داخل ثلاث نقاط.
- مثال ترتيب: الكاش المتاح، نتيجة المشروع، ديون لك، ديون عليك.
- كل بطاقة تجيب عن سؤال واحد.
- الحالات العاجلة ومشكلات البيانات لا تخفى داخل Swipe.

#### شكل البطاقة

- Radius `24px` وPadding `20px`.
- القيمة `32–36px Bold`، العنوان `15–16px SemiBold`، والمساند `12–14px`.
- Electric Indigo يمكن أن يكون الكتلة القوية مع نص أبيض.
- الحالة تظهر بالنص وMicro Signal، لا بتحويل اللون آليًا إلى أخضر أو أحمر.
- عنصر لوني قوي واحد عادةً في مساحة العرض.
- Citrus للفرصة أو Insight غير الحساس، وTerracotta مساند فقط؛ لا يمثلان ربحًا أو خسارة.

#### عرض الكل والتفاعل

- `عرض الكل` يفتح شاشة كاملة ذات هرمية مالية، لا Bottom Sheet صغيرًا ولا شبكة بطاقات ملونة.
- Swipe داخل البطاقة يوقف Swipe الصفحات في منطقتها.
- لا Auto-play ولا Loop من الأخيرة إلى الأولى.
- تحفظ البطاقة خلال الجلسة، ويبدأ الدخول الجديد من البطاقة الأساسية.
- الاستقرار `180–240ms`، وقارئ الشاشة يعلن الاسم و`البطاقة 1 من 4`.

### 13.5 سجل الحركات المالية

- صفوف مفتوحة، لا بطاقة لكل عملية.
- اليمين: أيقونة `20–22px` بلا مربع ملون.
- الوسط: نوع العملية، الطرف المرتبط، والتاريخ/المعلومة المساندة.
- اليسار: المبلغ `LTR`، الحالة عند الحاجة، وChevron للتفاصيل.
- الارتفاع `64–72px`، Padding `16px`، وDivider داخلي.
- التمييز بالاسم والأيقونة وMicro Signal، لا بالأحمر والأخضر.
- في قائمة مختلطة يستخدم وصف مثل `دخل` أو `خرج` مع المبلغ عند الحاجة؛ لا تعتمد `+` و`−` وحدهما.
- التصحيح أو العكس، إن وجدا في المنتج، يظهران باسم صريح لا كرقم سالب غامض.
- تجمع القائمة: اليوم، أمس، ثم تاريخ واضح.
- الحالات الممكن عرضها: مكتمل، جزئي، مسودة، محفوظ محليًا، غير معروف، معكوس؛ بالنص والإشارة لا Pill.
- الضغط على الصف يفتح التفاصيل، ولا Swipe Actions ولا Long Press.
- الإجراء المدمر لا يظهر مباشرة.
- الرجوع يعيد إلى الصف نفسه.
- الصف الجديد يظهر في موقعه `240–320ms` ويبرز ثم يستقر.

### 13.6 واجهة الديون والتحصيل

- فصل مرئي بين `لك` و`عليك` باستخدام Segmented Control.
- لا رقم وحيد باسم `صافي الديون` بوصفه العرض الأساسي.
- ملخص كل عرض يوضح الإجمالي وعدد الأطراف والمستحق/المتأخر عند توفر بياناته.
- صف الشخص: الاسم والدور وآخر حركة/استحقاق يمينًا، والمتبقي والحالة وإجراء `تحصيل` أو `دفع` يسارًا.
- الصف كامل يفتح دفتر الشخص، والإجراء النصي ينفذ المسار المباشر إن كان موجودًا.
- الحالات: غير مستحق، مستحق اليوم، متأخر، جزئي، مكتمل، بلا تاريخ، أو غير مؤكد.
- الدين الطبيعي Deep Ink؛ المستحق Warning، والمتأخر Danger، والجزئي Partial، وبلا تاريخ محايد.
- عند الجزء المدفوع تعرض القيم الأصلية والمدفوعة والمتبقية؛ يجوز Progress رفيع إذا كان الأصل معروفًا، مع بقاء الأرقام مكتوبة.
- قبل واجهة تأكيد التحصيل/الدفع يظهر ملخص مرئي: قبل، مبلغ العملية، بعد، والمحفظة المتأثرة؛ القيم تأتي من منطق المنتج.
- دفتر الشخص: ملخص، مبلغ حالي، إجراء أساسي، وسجل صفوف زمنية.
- الاسم المتشابه يرافقه هاتف أو معرف مساعد.
- الربط غير المؤكد يظهر `بحاجة للمراجعة` و`—`، لا `0.00`.

### 13.7 الكاش والمحافظ والتوزيع

- الكتلة الرئيسية تجيب: `الكاش المتاح الآن`، المبلغ، عدد المحافظ، `عرض التوزيع`، و`نقل بين المحافظ` إذا كانت الوظيفة موجودة.
- المحافظ صفوف مفتوحة: نوع، اسم، آخر حركة، رصيد، وChevron؛ لا بطاقة أو لون لكل محفظة.
- `غير موزع` لا يظهر كمحفظة عادية؛ الاسم المرئي `بانتظار تحديد المحفظة` داخل Contextual Notice مع المبلغ والسبب وإجراء `تحديد المحفظة`.
- القيمة السالبة غير المفسرة لا تعرض كـ`غير موزع −20`؛ تظهر كحالة `مصدر دفع غير محدد` مع شرح وإجراء، إذا كانت هذه هي الحالة التي يقررها منطق المنتج.
- شاشة التوزيع تعرض المطلوب، الموزع، والمتبقي، ثم المحافظ والقيم المخصصة، مع حالات واضحة للاكتمال والنقص والتجاوز.
- واجهة النقل تعرض المصدر والوجهة والمبلغ وأرصدة قبل/بعد التي يوفرها النظام، وتوضح بصريًا أن النقل بين أماكن المال.
- الصفر الحقيقي `0.00`، وغير المحسوب `—`، وعدم وجود حركات `لا توجد حركات بعد`.
- بعد الإجراء تتحدث الأرصدة في مواقعها وتبرز، بلا Animation لنقود طائرة.

### 13.8 Financial Impact Preview

مكوّن مرئي داخل النموذج وفوق الإجراء النهائي بعنوان `أثر العملية`، يجيب: ماذا سيظهر أنه تغير بعد التنفيذ؟

- قسم مفتوح، لا بطاقة مراجعة ضخمة.
- Micro Signal + سطح Secondary خفيف عند الحاجة.
- يعرض فقط القيم التي يحدد منطق المنتج أنها متأثرة.
- كل أثر صف واضح بصيغة قبل/بعد أو وصف نصي، مثل `100.00 ← 115.00 د.أ` مع عنوان يفسره.
- لا تعتمد `+` و`−` أو اللون وحدهما.
- القيم غير المحسوبة تعرض `—` وPartial Signal والسبب والإجراء الممكن.
- لا شاشة تأكيد إضافية لكل عملية يومية؛ Dialog أو مراجعة إضافية فقط عندما يقرر المنتج أن الأثر عاليًا أو صعب التراجع.
- القيم تتحدث `160–220ms` دون عدادات أو قفزات، وبعد الحفظ يظهر Action Echo في السياق الأصلي.

الأمثلة مثل البيع والتحصيل والمصروف ودفعة المورد ومال المالك والنقل **أمثلة على تنسيق العرض فقط**؛ تحديد أثرها المالي من اختصاص منطق المنتج والسياسة المالية خارج هذه الوثيقة.

### 13.9 أنواع الحركات وMicro Financial Glyphs

تبدأ واجهة الاختيار بالسؤال `ماذا حدث؟` بدل مطالبة المستخدم بتصنيف محاسبي.

- تعرض الأنواع الموجودة فعلًا كمجموعات صفوف مفتوحة، لا شبكة مربعات ملونة.
- الصف يحتوي أيقونة واسمًا ووصفًا من سطر واحد، وكله قابل للنقر.
- أمثلة عناوين مرئية: بيع، مصروف، شراء من مورد، تحصيل دين، دفع لمورد، نقل بين المحافظ، عربون، إضافة مال للمشروع، سحب مال من المشروع، تصحيح، عكس.
- لا `مدين / دائن`، ولا زر عام غامض `دخل / خرج` لاختيار الحدث.
- الأوصاف تشرح الحدث بلغة المستخدم؛ القواعد والآثار الفعلية ليست قرار UI.

#### الأيقونات والهوية

- Phosphor للمفاهيم العامة.
- المفاهيم المالية الملتبسة تستخدم `Micro Financial Glyphs` مخصصة.
- هندسة النسخة النهائية للشعار توجه نهايات الخطوط والزوايا والفراغات دون تحويل الأيقونة إلى شعار غامض.
- يمكن استخدام عناصر الشعار في Micro Signal، حركة الامتلاء، مؤشر Snapshot Deck، علامات صغيرة، ونقاط بيانات الرسوم.
- لا شعار كامل داخل كل بطاقة، ولا أربعة مربعات لكل أيقونة، ولا خلفية شعار متكررة.
- التطبيق الدقيق لا يغلق قبل مراجعة الشعار النهائي.

### 13.10 Financial Transaction Detail UI

**هذا القرار مرئي فقط ولا يقرر وجود وظائف التصحيح أو العكس أو الحذف.**

ترتيب الشاشة:

1. نوع العملية وحالتها.
2. المبلغ الرئيسي.
3. التاريخ والوقت.
4. الأثر المالي المعروض من النظام.
5. الطرف المرتبط والمحفظة.
6. التفاصيل الإضافية.
7. سجل التغييرات إن كان موجودًا.
8. الإجراءات المتاحة فعليًا.

- الرأس يحتوي الأيقونة والنوع وMicro Signal والمبلغ والتاريخ والحالة.
- الأثر يعرض القيم المتأثرة؛ عدم التأثر يظهر فقط إذا كان مهمًا للفهم.
- المعلومات كصفوف مفتوحة، والمعرفات التقنية تحت `معلومات إضافية` لا في الأعلى.
- سجل التغييرات يظهر فقط إذا وفره النظام، كتسلسل زمني مفهوم.
- الإجراءات الموجودة ترتب بصريًا؛ الثانوي في قائمة سياقية، والمدمر غير مهيمن.
- إذا وفر النظام مقارنة قبل/بعد، تعرض القيمتان والأثر بوضوح.
- لا فاتورة ضخمة داخل بطاقة واحدة، ولا زر أحمر ضخم، ولا تفاصيل تقنية تقود الشاشة.

### 13.11 الربح والخسارة واكتمال النتيجة

يعتمد العرض مزيجًا من:

- `Result + Completeness` في الملخص والبطاقة الرئيسية.
- `Result Story` داخل شاشة التفاصيل.

#### النتيجة المكتملة

- العنوان: `نتيجة هذا الشهر` أو الفترة المختارة.
- القيمة: `ربح 420.00 د.أ` أو `خسارة 80.00 د.أ`.
- Micro Signal مكتملة.
- النص: `بحسب جميع البيانات المسجلة`.
- الإجراء: `عرض التفاصيل`.

#### النتيجة الجزئية

- العنوان: `نتيجة تقديرية`.
- تظهر القيمة فقط إذا وفرها النظام.
- Micro Signal جزئية.
- يوضح النص البيانات الناقصة، مثل `تنقص تكاليف 3 منتجات`.
- يظهر إجراء مرتبط بالمعلومة الناقصة.
- لا تستخدم كلمة `ربح` وحدها وكأن النتيجة مؤكدة.

#### تعذر الحساب

- العنوان: `النتيجة غير متاحة بعد`.
- القيمة `—`، لا `0.00 د.أ`.
- Micro Signal غير معروفة.
- السبب والإجراء التالي واضحان.

#### اللون والتفاصيل

- البطاقة Indigo أو Deep Ink، ولا تتحول كلها إلى الأخضر أو الأحمر.
- Partial يستخدم Lavender، وUnknown سطحًا محايدًا.
- شاشة التفاصيل تعرض القيم التي يوفرها النظام بتسلسل مفهوم، ثم ما اكتمل وما ينقص.
- هذا القسم يحدد العرض فقط، ولا يقرر معادلة الربح أو مكوناتها المحاسبية.

### 13.12 الفترات والمقارنات

#### اختيار الفترة

- زر واحد واضح مثل `هذا الشهر ▾` داخل رأس القسم.
- القائمة: اليوم، هذا الأسبوع، هذا الشهر، الشهر الماضي، وفترة مخصصة.
- لا صف Period Chips.
- الفترة المخصصة تعرض `من` و`إلى` ومعاينة وزر `عرض النتائج`.
- التاريخ `DD/MM/YYYY` بأرقام إنجليزية.

#### المقارنة

- تعرض القيمة الحالية، مقدار التغير، واسم الفترة المقارنة.
- مثال: `زادت المبيعات 12% مقارنة بالشهر الماضي`.
- لا سهم أو نسبة أو لون بلا تفسير.
- الارتفاع لا يعد إيجابيًا تلقائيًا؛ يشرح المعنى الذي يقدمه النظام.
- عند نقص إحدى الفترتين تظهر Partial Signal وتعرض القيمتان دون حكم دقيق.
- لا `0%` عند غياب البيانات؛ يظهر `لا توجد بيانات كافية للمقارنة`.
- شاشة الهاتف لا تقارن أكثر من فترتين في الوقت نفسه.

#### الحركة

- يبقى الهيكل ثابتًا، ويتغير الجزء المعني فقط.
- Loading محلي داخل المنطقة.
- الانتقال `180–220ms` بلا Count-up أو قفز أرقام أو إعادة الصفحة إلى الأعلى.
- لا Swipe بين الفترات لتجنب تعارضه مع Swipe الصفحات والبطاقات.

### 13.13 Micro Chart System

الرسم يستخدم فقط عندما يشرح تغيرًا أو مقارنة أو تركيبًا أفضل من الرقم والنص.

#### عائلة الرسوم المعتمدة

1. `Micro Line Chart` للاتجاه عبر الزمن.
2. `Micro Rounded Bar` للمقارنات والفئات.
3. `Micro Stacked Bar` لتوزيع أو تركيب قابل للمقارنة.
4. `Micro Half-Ring` كمكوّن مميز للهاتف لعرض أجزاء من مجموع.
5. `Micro Segmented Ring` الكامل، باستخدام محدود.

#### الشكل العام

- الارتفاع المعتاد للرسم الخطي أو الأعمدة `180–220px`.
- يظهر في قسم مفتوح، لا بطاقة مزخرفة.
- العنوان والوصف يمينًا، والفترة أو `عرض البيانات` في الجهة المقابلة.
- Gridlines أفقية خفيفة فقط، بلا إطار أو Shadow.
- خط الرسم `2–3px`، والنقاط تظهر عند التفاعل أو إذا كانت قليلة.
- خط الصفر واضح للقيم الموجبة والسالبة.
- لا أكثر من رسم رئيسي في مساحة عرض واحدة.

#### الألوان

- لوحة الرسم: `#4F46E5`، `#008C8C`، `#D97706`، `#B83B75`، `#657C25`.
- Indigo للسلسلة الأولى.
- لا ألوان متعددة لسلسلة واحدة ولا Gradient تحت الخط.
- اللون لا يحمل المعنى وحده؛ الاسم أو نمط الخط يرافقه.

#### الاتجاه والتفاعل

- واجهة الرسم والعنوان والLegend RTL.
- محور الزمن `LTR`: الأقدم يسارًا والأحدث يمينًا، على أن يختبر لاحقًا مع مستخدمين أردنيين.
- الرسم الفئوي يرتب حسب المعنى أو القيمة ولا يعكس آليًا.
- الضغط يظهر Tooltip بالتاريخ والتسمية والقيمة الدقيقة، ومساحة الالتقاط أكبر من العنصر المرئي.
- لا Hover بوصفه الوسيلة الوحيدة، ولا Pinch Zoom أو تمرير أفقي أو Swipe لتغيير الفترة.
- `عرض البيانات` إلزامي ويفتح القيم كقائمة واضحة.

#### المحاور والبيانات الناقصة

- الأرقام إنجليزية، ومحور الزمن يعرض نحو `4–6` علامات على الهاتف.
- اختصار القيمة مسموح على المحور فقط، وتظهر كاملة في Tooltip وعرض البيانات.
- لا يصل الخط عبر بيانات مفقودة ولا تعامل كصفر؛ تظهر فجوة وتفسير.
- Partial Signal قرب العنوان إذا كانت البيانات جزئية.

#### الحركة

- الدخول أو تغير الفترة `200–280ms`.
- لا Line Drawing استعراضي ولا Count-up ولا Morph بين أنواع مختلفة.
- Reduced Motion يعرض Fade قصيرًا.

### 13.14 Micro Half-Ring

نصف دائرة علوي مفتوح من الأسفل، مصمم للهاتف لتوفير الارتفاع وإعطاء الرقم مساحة واضحة.

#### البنية

- نهايات مستديرة وفراغات بين الأجزاء.
- `2–3` أجزاء مفضلة و`4` كحد أقصى.
- الرقم الإجمالي داخل المساحة المفتوحة، والعنوان أسفله.
- قائمة بالأسماء والقيم والنسب أسفل الرسم.
- مناسب لتوزيع الكاش أو المصاريف أو المبيعات على فئات قليلة تمثل أجزاء من مجموع.
- غير مناسب للربح والخسارة أو الاتجاه الزمني أو دين منفرد أو رقم لا يحتوي أجزاء.

#### التفاعل

- الضغط على جزء يبرزه `4–6px`.
- تتغير القيمة الوسطية إلى قيمة الجزء ويظهر الاسم والنسبة.
- الضغط مرة أخرى أو خارج الرسم يعيد الإجمالي.
- الحركة `160–220ms` بلا Bounce.

#### منع تحوله إلى Gauge

- لا Needle.
- لا Min/Max أو تدريجات عداد.
- لا مناطق خضراء وحمراء.
- لا Sweep Animation أو دوران.
- لا تسميات خارجية بخطوط طويلة.

`Micro Half-Ring` هو الخيار الأساسي للتركيب على الهاتف، و`Micro Segmented Ring` الكامل يستخدم عند الحاجة إلى مساحة أو عرض تفصيلي أكبر. المستخدم لا يختار بينهما؛ Design System يحدد الشكل.

### 13.15 Micro Segmented Ring

- مستوحى من Pie ذي الفراغات والنهايات المستديرة، لكنه مكوّن Micro مخصص.
- من `2–4` أجزاء فقط؛ الفئات الزائدة تجمع تحت `أخرى` أو تتحول إلى Bar.
- الإجمالي في الوسط، والقائمة الدقيقة أسفل الرسم.
- الضغط يبرز الجزء `4–6px` ويعرض الاسم والقيمة والنسبة داخل المركز أو تحته.
- لا خطوط تسمية طويلة أو ازدحام حول الدائرة.
- يظل `عرض البيانات` إلزاميًا.
- يمكن ربط الفراغات والنهايات المستديرة بعناصر الشعار والوحدات الأربع دون إجبار البيانات على أربعة أجزاء.

### 13.16 أنماط الرسوم المرفوضة أو المحدودة

- `Biaxial Bar Chart` مرفوض في الواجهة الأساسية لأن المحورين قد يخلقان مقارنة مضللة؛ يستخدم رسمان أو مقياس واحد صالح بدلًا منه.
- `Straight Angle Pie` ليس رسمًا ماليًا أساسيًا؛ يؤخذ منه الإلهام الهندسي فقط، بينما ينفذ نصف الدائرة وفق قواعد Micro Half-Ring.
- `Pie Chart With Customized Label` لا يستخدم بتسميات خارجية مزدحمة؛ تستبدل بالقيمة الوسطية وLegend/قائمة.
- `Custom Active Shape Pie` يؤخذ منه إبراز الجزء باللمس فقط بعد تبسيطه.
- Pie التقليدي وDonut غير المنضبط وGauge وSpeedometer و3D وArea الكثيف والرسوم داخل كل بطاقة مرفوضة.
- لا رسم للزينة، ولا خمسة ألوان لسلسلة واحدة، ولا إخفاء للقيمة الدقيقة.

### 13.17 مكتبة الرسوم — توصية التنفيذ

هذا توجيه تقني مشروط وليس قرارًا في الهوية أو تفويضًا باختيار مكتبة نيابة عن فريق التنفيذ:

- في تنفيذ React Web أو PWA يمكن تقييم `Recharts` أو بديل مناسب بعد التحقق من النسخة الحالية والترخيص والأداء والوصول في بيئة المشروع.
- لا تستخدم إعدادات المكتبة البصرية الافتراضية؛ تبنى طبقة `Micro Chart System` تضبط الألوان والخط والمحاور والـTooltip والحركة والحالات وبديل `عرض البيانات`.
- القرار النهائي لاختيار المكتبة يتخذه منفذ التقنية وفق المنصة الفعلية؛ لا يتغير شكل Micro أو معنى الرسم باختلاف المكتبة.

### 13.18 تحكم المستخدم في الرسم

المستخدم يستطيع تغيير:

- الفترة.
- المؤشر المعروض.
- المقارنة.
- `الرسم / عرض البيانات`.

لا يختار نوع الرسم بحرية في الشاشة الأساسية؛ نوع الرسم جزء من معنى البيانات ومسؤولية Design System.

الاستثناء الوحيد داخل تحليل متقدم: `خطي / أعمدة` عندما يكون النوعان صحيحين للبيانات نفسها، بخيارين فقط ودون Pie أو Gauge.

### 13.19 Micro Guidance Layer — Insights المالية وأولوية الإجراء

اسم النظام الداخلي هو `Micro Guidance Layer`، أما العنوان الذي يراه المستخدم فهو `الأهم الآن`.

#### الوظيفة وحدود الاستخدام

- `الأهم الآن` مخصص فقط للمعلومة التي تتطلب انتباهًا أو قرارًا أو إجراءً من المستخدم.
- المعلومة المحايدة لا تُرفع إلى هذه الكتلة؛ توضع قرب الرقم أو الرسم أو القسم المرتبط بها بلا Badge تصنيفي.
- لا تُصنع Insight لمجرد ملء المساحة، ولا يكرر المكوّن رقمًا ظاهرًا من دون تفسير سبب أهميته أو ما يستطيع المستخدم فعله.
- لا يدّعي النظام اليقين عندما تكون البيانات جزئية أو ناقصة.
- النظام المالي هو الذي يساعد المستخدم على فهم وضع مشروعه؛ لا تُقدَّم المعلومة بوصفها حديثًا مع نموذج ذكاء اصطناعي.

#### البنية المعتمدة: Evidence → Action

كل Insight قابلة للإجراء تتكون، بحسب المتاح، من:

1. **الخلاصة:** ماذا حدث، بصياغة تبدأ بالنتيجة.
2. **الدليل:** الرقم والفترة أو المقارنة التي تبرر الخلاصة.
3. **اكتمال البيانات:** هل النتيجة مكتملة أم جزئية أم يتعذر الجزم بها.
4. **الإجراء:** خطوة واحدة محددة يستطيع المستخدم اتخاذها.

يمكن إضافة `لماذا ظهر هذا؟` عند الحاجة إلى شرح إضافي، ولا يوضع زر شكلي إذا لم يوجد إجراء حقيقي.

#### مستويات المعنى الظاهرة

- `فرصة`.
- `يحتاج انتباه`.
- `يحتاج إجراء الآن`.
- `بيانات ناقصة`.
- المعلومة المحايدة تظهر Inline بلا تصنيف أو Badge.

لا يعتمد أي مستوى على اللون وحده؛ يكتب المعنى بوضوح وترافقه Micro Signal المناسبة.

#### الترتيب والموضع

- توجد Insight رئيسية واحدة فقط في المساحة الظاهرة.
- يمكن إظهار `2–3` ملاحظات إضافية كصفوف، وما زاد ينتقل إلى `عرض الكل`.
- لا تُخفى حالة حرجة خلف `عرض الكل` أو داخل Swipe.
- في `مشروعي الآن`: Insight واحدة قابلة للإجراء تحت الملخص الرئيسي.
- في `المالية`: Insight رئيسية، ثم رابط مثل `3 ملاحظات أخرى`، وتعرض البقية كصفوف لا كبطاقات ملونة متساوية.
- Insight السياقية تظهر قرب الدين أو المحفظة أو الرسم المرتبط بها، ولا تُكرر كنسخة كاملة في مواضع متعددة.
- إذا لم توجد أولوية قابلة للإجراء، يختفي القسم أو يظهر نص هادئ مثل `لا توجد أمور تحتاج إجراءً الآن`؛ لا تستخدم بطاقة نجاح خضراء كبيرة.

#### الشكل المرئي

- Radius `18px` وPadding بين `16–18px`.
- سطح دلالي خفيف بلا Shadow وبلا مربع أيقونة ملون.
- Micro Signal في اليمين، ثم تصنيف قصير اختياري، ثم عنوان يبدأ بالخلاصة، وسطر دليل واحد، وحالة اكتمال البيانات، وإجراء واحد.
- اللون الدلالي محدود ولا يحول المكوّن إلى إعلان.
- يمكن استخدام Soft Citrus للفرصة، بلا Gradient أو Glow.
- لا Sparkles ولا Robot ولا رموز AI ولا بطاقة Gradient تحمل عبارة مثل `اكتشفنا لك` أو `الذكاء الاصطناعي ينصحك`.

#### اللغة والحركة والوصول

- اللغة مباشرة، قريبة، وغير لومّية؛ لا تخويف ولا مصطلحات محاسبية ثقيلة.
- تحديث المعلومة يحدث في المكان نفسه خلال `180–240ms`.
- بعد تنفيذ الإجراء يظهر Action Echo في سياقه الحقيقي.
- لا Pulse أو Loop أو Confetti؛ وفي Reduced Motion يستخدم Fade بين `100–150ms`.
- يعلن قارئ الشاشة التصنيف والعنوان والدليل وحالة البيانات والإجراء.
- هدف الإجراء لا يقل عن `44×44px`، ولا تختفي المعلومة تلقائيًا أثناء قراءتها.

#### الأنماط المرفوضة

- Feed طويل من Insight Cards.
- Carousel للتنبيهات أو النصائح.
- أكثر من إجراء رئيسي داخل Insight واحدة.
- توصية بلا دليل أو بلا بيان اكتمال البيانات.
- تحويل كل تغير إلى لون أحمر أو حالة خطر.
- لغة اللوم أو التخويف.
- إخفاء المعلومة الحرجة خلف `عرض الكل`.
- تكرار رقم ظاهر من دون تفسير أو إجراء.

### 13.20 حالات القيم والحفظ والإرسال

هذا القسم يحدد **طريقة العرض المرئي فقط** ولا يقرر قواعد التخزين أو المزامنة أو الحساب.

#### محور معنى القيمة

- `0.00` يظهر فقط عندما تكون القيمة صفرًا حقيقيًا ومعلومًا.
- `غير مسجل`: القيمة أو البيانات المطلوبة لم تُسجل.
- `لا يمكن حسابه`: المدخلات أو الشروط اللازمة للحساب غير متاحة.
- `نتيجة تقديرية`: توجد نتيجة، لكنها مبنية على بيانات غير مكتملة.
- عند غياب قيمة رقمية يستخدم النص المناسب أو `—`؛ لا يستبدل الغياب بصفر.
- لا يستخدم `غير معروف` بوصفه وصفًا ماليًا عامًا لأنه فضفاض وقد يخلط بين نقص التسجيل وتعذر الحساب.

#### محور الحفظ والإرسال

- `محفوظ على هذا الهاتف`: العملية محفوظة محليًا فقط.
- لا تظهر حالة دائمة عند اكتمال الحفظ والإرسال بصورة طبيعية؛ النجاح الطبيعي لا يحتاج شارة مستمرة.
- `لم يتم الإرسال`: العملية لم تصل إلى الوجهة المطلوبة. يمكن إظهار `إعادة المحاولة`، ولا يقال إنها محفوظة محليًا إلا إذا أكد النظام ذلك فعلًا.
- `لم تُحفظ العملية`: الحفظ نفسه لم ينجح، وتظهر الحالة قرب مصدر الفعل. لا يقال إن بيانات النموذج باقية أو قابلة للاستعادة إلا إذا كان ذلك صحيحًا في النظام.

#### قواعد العرض

- حالة معنى الرقم تظهر بجوار الرقم أو ضمن كتلته.
- حالة الحفظ أو الإرسال تظهر قرب العملية أو القسم المرتبط بها.
- المحفوظ محليًا يستخدم Micro Signal مرتبطة بالجهاز، ولا يعرض نجاحًا كاملًا يوحي بالمزامنة.
- لا Banner عام بسبب مشكلة تخص صفًا واحدًا، ولا لون أحمر تلقائي لكل تعثر إرسال.
- لا عبارة عامة مثل `حدث خطأ` عندما يمكن تسمية ما لم يُحفظ أو لم يُرسل.
- يبقى محور معنى الرقم منفصلًا بصريًا ولغويًا عن محور حفظ العملية أو إرسالها.

### 13.21 المراجعة النهائية وإغلاق Financial UI

اجتاز المحور مراجعة الاتساق وفق القرارات التالية:

- يبدأ من أسئلة المستخدم، لا من تصنيفات محاسبية.
- يفصل الكاش والمبيعات والديون والنتيجة بدل دمجها في رقم مضلل.
- يميز الصفر الحقيقي عن غير المسجل وعن المتعذر حسابه.
- لا يقدم النتيجة الجزئية كحقيقة مؤكدة.
- يعرض بطاقة مالية رئيسية واحدة في اللحظة نفسها، ولا يخفي حالة حرجة داخل Swipe.
- يستخدم اللون للهرمية والدلالة المساندة، لا بديلًا عن النص والمعنى.
- يقصر `الأهم الآن` على Insights القابلة للإجراء.
- يستخدم رسومًا محدودة ومخصصة للمعنى، مع قيمة دقيقة و`عرض البيانات`.
- لا يقرر منطقًا محاسبيًا أو سياسة حفظ أو مزامنة من داخل مواصفات الواجهة.

#### مكونات Financial UI المعتمدة

1. `Question-First Financial Hierarchy`.
2. `Financial Snapshot Deck`.
3. `Financial Activity Rows`.
4. `Financial Impact Preview`.
5. `Micro Financial Glyphs`.
6. `Micro Guidance Layer`.
7. `Micro Line Chart`.
8. `Micro Rounded Bar`.
9. `Micro Stacked Bar`.
10. `Micro Half-Ring`.
11. `Micro Segmented Ring`.
12. نظام حالات القيم والحفظ والإرسال.

#### فرضيات تحقق باختبار المستخدم — لا تعيد فتح القرار

1. هل يكتشف المستخدم Swipe في `Financial Snapshot Deck`؟
2. هل يفهم `Micro Half-Ring` كتوزيع لأجزاء من مجموع، لا كعداد تقدم أو Gauge؟
3. هل يبقى محور الزمن `LTR` واضحًا داخل الواجهة العربية؟
4. هل يميز المستخدم بين `غير مسجل` و`لا يمكن حسابه` و`نتيجة تقديرية`؟
5. هل يفهم `الأهم الآن` بوصفه مساحة لما يحتاج انتباهًا أو إجراءً؟

تظل هندسة `Micro Financial Glyphs` الدقيقة مرتبطة بمراجعة الشعار النهائي؛ حالة أصل الشعار والأيقونات النهائية `Asset Gap` إلى أن يقدم ويعتمد، ولا يجوز اختراع هندسة نهائية أو الادعاء بتسليم الأصول من هذا النص.

---

## 14. Contextual UI

**الحالة: مغلق بالكامل**  
**القاعدة الحاكمة:** الحالات السياقية تحولات داخل بنية الواجهة، وليست مجموعة Alerts أو Cards جاهزة.

### 14.1 Contextual Hierarchy

**الحالة: مغلق**

تعتمد Micro خمسة مستويات للمعنى والأولوية، وليست خمسة Components ثابتة:

1. **سياق هادئ:** معلومة تفسيرية قرب العنصر، بلا Container أو إجراء افتراضي.
2. **ملاحظة سياقية:** معلومة يجب ملاحظتها لكنها لا تعطل المهمة.
3. **يحتاج انتباهًا:** حالة قد تؤثر على الدقة مع إمكان المتابعة.
4. **يحتاج إجراءً:** حالة لا ينبغي تجاهلها ويوجد لها إجراء مباشر.
5. **حالة مانعة:** لا يمكن إكمال الجزء المتأثر قبل معالجة الحالة.

#### قواعد الاختيار

- إذا احتاج المستخدم إلى فهم إضافي فقط، تعرض الحالة كسياق هادئ.
- إذا وجب ملاحظتها من دون أثر مباشر على المهمة، تستخدم ملاحظة سياقية.
- إذا أثرت على الدقة مع بقاء المتابعة ممكنة، تستخدم حالة تحتاج انتباهًا.
- إذا تطلبت معالجة قريبة، تستخدم حالة تحتاج إجراءً.
- إذا منعت المهمة، تستبدل المنطقة المتأثرة بحالة مانعة.

#### نظام النضج المعتمد

- `Component-Owned States`: كل مكوّن يملك حالاته الطبيعية والتحميل والفراغ والجزئية والفشل والحفظ المحلي وعدم الإتاحة.
- `Anchor → Reveal → Recover`: ترتبط الحالة بمصدرها، تنكشف منه، ثم يعود المكوّن إلى وضعه الطبيعي بعد المعالجة.
- ثبات المكان والشكل الأساسي إلزامي؛ لا تقفز العناصر ولا تستبدل الشاشة بلا ضرورة.
- لا يظهر أكثر من عنصر سياقي واحد عالي الحضور في مساحة العرض.
- قوة الشكل تتبع أثر المشكلة الفعلي.
- الاسترجاع والإجراء التالي أهم من مجرد إعلان الخطأ.
- يعالج السبب في أقرب مكان إليه؛ لا Banner عام لمشكلة داخل صف واحد، ولا Modal لمجرد لفت الانتباه.

`Micro Guidance Layer` يفسر بيانات المشروع ويوجه المستخدم ماليًا، بينما `Contextual UI` يشرح حالة الواجهة أو المهمة الحالية. لا يدمج النظامان في مكوّن واحد.

### 14.2 Contextual Notices

**الحالة: مغلق**

تعتمد اللغة السياقية الأشكال التالية بوصفها تحولات متكيفة مع العنصر المضيف:

#### Micro Context Trace

- أثر نصي صغير ملاصق للمعلومة أو القيمة.
- Micro Signal على خط النص عند الحاجة، بلا أيقونة منفصلة أو Container.
- يستخدم للسياق الهادئ مثل وقت التحديث أو اكتمال البيانات.

#### Micro Reveal Row

- الصف نفسه يتوسع من الأسفل لعرض السبب والإجراء.
- لا Card داخل الصف ولا Radius داخلي.
- ينتقل Divider إلى نهاية الامتداد.
- بعد المعالجة ينغلق الامتداد ويعود الصف إلى حالته الطبيعية.

#### Micro Context Seam

- طبقة سياقية تمتد داخل القسم بين محتويين، ولا تطفو كبطاقة.
- تستخدم تغيرًا خفيفًا في سطح Canvas، بلا Shadow أو Border كامل أو قالب رأس ثابت.
- تتكون بحسب الحالة من خلاصة وسبب أو دليل وإجراء واحد.

#### Micro System Ribbon

- للحالات العامة التي تؤثر على أكثر من شاشة مثل Offline أو عمليات متعددة بانتظار الإرسال.
- يندمج مع المنطقة العلوية للهاتف والتطبيق، بلا Pill أو Card عائمة.
- يمكن أن يتسع لعرض إجراء ثم يعود إلى حالته المختصرة.
- لا يستخدم لمشكلة تخص عنصرًا واحدًا.

#### Micro Recovery Stage

- تستبدل المنطقة المتعطلة فقط مع إبقاء بنية الصفحة الصالحة ظاهرة.
- تعرض ما لم يكتمل، والسبب إذا كان معروفًا، وإجراء استرجاع واحدًا.
- لا Illustration جاهزة أو أيقونة ضخمة أو بطاقة مركزية.
- تستخدم الشاشة الكاملة فقط إذا كانت الصفحة كلها غير قابلة للاستخدام.

#### الحركة والوصول

- توسع الصف أو الطبقة بين `180–240ms`، وReduced Motion يستخدم Fade بين `100–150ms`.
- لا Bounce أو حركة زخرفية.
- تقرأ الحالة بعد العنصر المرتبطة به، ولا يعيد قارئ الشاشة إعلانها مع كل تحديث غير جوهري.
- الحالات الحرجة فقط تعلن فورًا، وأهداف الإجراءات لا تقل عن `44×44px`.

#### مرفوض

- مكوّن عام باسم `Alert Card` يستخدم لكل الحالات.
- أيقونة داخل دائرة Pastel، ثم عنوان ووصف وزر.
- Border جانبي ملون، Card داخل Card، أو Toast يحمل شرحًا يجب الرجوع إليه.
- Pop-up لمعلومة عادية؛ Dialog أو Bottom Sheet محجوزان لقرار لازم أو خطر فقد أو أثر صعب التراجع.

### 14.3 Empty States

**الحالة: مغلق**

الفراغ ليس حالة واحدة، ويقسم إلى خمسة أنماط ذات معنى مختلف:

#### Micro First Move

- لبداية الاستخدام قبل تسجيل أول عنصر.
- يبقى عنوان الشاشة وبنيتها، وتظهر الدعوة قرب المكان الذي سيظهر فيه أول محتوى.
- عنوان يوضح الفائدة، شرح قصير، وإجراء أساسي واحد.
- لا تستخدم عبارات تسويقية طفولية أو `لا توجد بيانات`.

#### Micro Empty Continuation

- لقسم مستخدم لا يحتوي عناصر حاليًا، مثل عدم وجود طلبات قيد التنفيذ.
- تبقى أدوات القسم ورأس القائمة، وتظهر الحالة في بداية منطقة الصفوف.
- Micro Signal ونص قصير، ولا يضاف إجراء إذا كانت الحالة طبيعية.

#### Micro Search Reset

- لبحث أو فلترة لم ينتجا نتائج، وليس لغياب البيانات الأصلية.
- تبقى أدوات البحث والفلاتر ظاهرة، ويذكر الشرط المؤثر عند الإمكان.
- الإجراء هو تعديل البحث أو مسح الفلاتر، لا إضافة عنصر جديد تلقائيًا.

#### Micro Period Gap

- لغياب البيانات في الفترة المختارة.
- يبقى عنوان القسم واختيار الفترة، وتستبدل منطقة الرسم برسالة مدمجة.
- لا تعرض محاور فارغة أو `0.00` بدل البيانات الغائبة.
- الإجراء يغير الفترة أو يعرض فترة تحتوي بيانات إذا كانت المعلومة متاحة فعلًا.

#### Micro Clear State

- لحالة إيجابية هادئة مثل عدم وجود ديون متأخرة أو أمور تحتاج إجراءً.
- تظهر كسطر هادئ أو مساحة صغيرة، بلا بطاقة نجاح خضراء أو علامة صح ضخمة أو احتفال.

#### قواعد التكوين والحركة

- تظهر الحالة عند بداية منطقة المحتوى وتحافظ على الهيكل المحيط؛ لا توضع افتراضيًا في منتصف الشاشة.
- النص من سطر إلى سطرين، وإجراء واحد كحد أقصى.
- لا Stock Illustrations أو شخصيات ثلاثية الأبعاد أو Lottie جاهزة أو رموز ضخمة لملء الفراغ.
- يسمح بأثر هندسي صغير من Shape Language فقط إذا أدى وظيفة توجيهية.
- بعد إضافة أول عنصر يتحول موضع الحالة إلى المحتوى الجديد خلال `180–240ms` مع Action Echo قصير، من دون إعادة تحميل الشاشة كاملة.
- فشل التحميل، Offline، غياب الصلاحية، نقص البيانات المانع، وانتظار النتائج ليست Empty States.

### 14.4 Loading & Updating States

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Loading Choreography`

التحميل حالة داخل المكوّن لا شاشة مستقلة، ويعتمد `Content-Shaped Loading + Priority Reveal + مؤشر متحرك واحد + تحديث في المكان نفسه`.

#### الأنماط المعتمدة

1. `Micro Structural Load`: بنية الشاشة الحقيقية تظهر أولًا، ويقتصر Skeleton على المناطق التي تنتظر بيانات.
2. `Micro Loading Signal`: إشارة صغيرة من وحدات Micro تستخدم داخل موضع الانتظار بدل Spinner عام.
3. `Micro Quiet Refresh`: تبقى البيانات السابقة ظاهرة مع `جارٍ التحديث` ووقت آخر نسخة عند الحاجة.
4. `Micro Action Hold`: يبقى الزر في موضعه وعرضه، ويتحول نصه إلى فعل مستمر مثل `جارٍ الحفظ` مع منع الضغط المتكرر.
5. `Micro Value Update`: تتحدث القيمة في مكانها بلا Count-up أو قفزة أو أخضر/أحمر تلقائي.
6. `Micro Continuation Load`: يظهر تحميل المزيد في نهاية القائمة من دون إعادة القائمة للأعلى.
7. `Micro Progress Stage`: للعملية الطويلة فعلًا؛ يعرض نسبة حقيقية أو اسم المرحلة، ولا يخترع تقدمًا غير معلوم.

#### التكوين والإيقاع

- لا صفحة Loading أو Overlay عام أو Grid من Skeleton Cards.
- يحاكي Skeleton إيقاع المحتوى وأبعاده التقريبية، لا يحول الشاشة إلى مستطيلات رمادية متساوية.
- ترتيب الكشف: هيكل الشاشة، الإجابة أو الرقم الرئيسي، الإجراءات الأساسية، المعلومات المساندة، ثم القوائم والرسوم الثانوية.
- يسمح بمؤشر متحرك واحد فقط داخل مساحة العرض؛ تبقى بقية حالات الانتظار ثابتة.
- البيانات القديمة الصالحة لا تمحى أثناء Refresh، وتوسم بوضوح إذا أصبحت قديمة بصورة مؤثرة.
- بعد وصول البيانات تستخدم Crossfade محلية بين `160–220ms`، بلا Line Drawing أو Count-up أو Flash.
- أقل من `300ms` لا يحتاج مؤشرًا لتجنب الوميض؛ بعده يظهر مؤشر محلي، وبعد انتظار ملحوظ يظهر وصف لما يجري.
- Reduced Motion يلغي التتابع ويستخدم تغير Opacity هادئًا، ولا يتغير موضع التركيز.

#### مرفوض

- Spinner كبير في منتصف الشاشة، Shimmer سريع، Skeleton كامل في كل تحديث، زر يتحول إلى دائرة بلا نص، أو أكثر من Loader متحرك.
- تعطيل الصفحة كاملة بسبب تحديث جزء صغير.
- مؤشرات تقدم أو نسب غير حقيقية.
- Loader أعيد تلوينه أو أربعة مربعات تحركت بلا علاقة بهرمية المحتوى.

### 14.5 Success & Action Echo

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Causal Feedback`

لا يوجد `Success Component` عام. النجاح انتقال سببي: الفعل، تأكيد تنفيذه، ظهور النتيجة، ثم استقرار الواجهة.

#### الأنماط المعتمدة

1. `Micro Source Echo`: يتحدث العنصر نفسه عندما تكون النتيجة داخله.
2. `Micro Destination Echo`: تظهر النتيجة في موضعها الفعلي، مع `عرض العملية` إذا لم يكن الموضع ظاهرًا.
3. `Micro Impact Echo`: يعرض الأثر الأساسي لإجراء مالي مهم من دون Receipt Card ضخمة.
4. `Micro Insert Echo`: يدخل الصف الجديد في موقعه الصحيح ويستقر خلال `240–320ms`.
5. `Micro Completion State`: لمسار كبير أو متعدد الخطوات فقط، لا لكل حفظ يومي.

#### Causal Feedback

- الإجراء البسيط ذو النتيجة الظاهرة لا يحتاج رسالة نجاح.
- الإجراء غير الظاهر يحتاج تأكيدًا محددًا وربطًا بالنتيجة.
- الإجراء المالي المهم يترك دليلًا دائمًا في السجل أو الحالة أو التفاصيل، ولا يعتمد على رسالة مؤقتة.
- العملية المحلية تعرض `محفوظ على هذا الهاتف` ولا تحصل على نجاح كامل قبل الإرسال.
- ينتهي `Micro Action Hold`، تكتمل Micro Signal، تتحدث النتيجة الأساسية، يظهر أثر واحد، ثم تستقر الشاشة.
- نختار نقطة Echo رئيسية واحدة؛ لا نبرز الزر والرقم والبطاقة والقائمة معًا.
- الرسالة المؤقتة تستخدم فقط عندما يختفي المصدر أو لا توجد نتيجة مرئية أو يوجد `تراجع` حقيقي.
- اللغة محددة مثل `تم تسجيل البيع`، ولا تستخدم `نجاح!` أو `تمت العملية بنجاح` أو لغة احتفالية.

#### مرفوض

- Toast بعد كل فعل، بطاقة نجاح خضراء، Checkmark ضخمة، Confetti، شاشة نجاح يومية، أو Animation منفصلة عن النتيجة.
- نجاح كامل لعملية محفوظة محليًا فقط.
- تحريك عدة نتائج أو أرقام في الوقت نفسه.

### 14.6 Error & Recovery

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Recovery Choreography`

لا يوجد `Error Component` عام. التسلسل هو `Preserve → Expose → Recover → Resolve`.

#### الأنماط المعتمدة

1. `Micro Local Recovery`: فشل داخل عنصر واحد ينكشف من العنصر نفسه.
2. `Micro Action Recovery`: تبقى المدخلات ويعود الإجراء بصيغة مثل `إعادة الحفظ`.
3. `Micro Partial Recovery`: يفصل ما اكتمل عما لم يكتمل والإجراء المتاح.
4. `Micro Stale Recovery`: تبقى آخر بيانات صالحة مع وقتها وإعادة المحاولة.
5. `Micro Blocking Recovery`: تستبدل المنطقة المتعطلة فقط بـ`Micro Recovery Stage`.

#### التكوين والسلوك

- `Micro Recovery Seam` امتداد للبنية الأصلية؛ لا Radius داخلي أو Card جديدة أو خلفية حمراء كاملة.
- `Micro Incomplete Signal` تظهر كبنية Micro غير مكتملة مع نص واضح، ولا تستبدل المعنى المكتوب.
- لا تمسح الحقول أو تغلق الطبقة أو تعيد الشاشة للأعلى أو تفقد الفلاتر وموضع القائمة.
- عند تعدد الأخطاء تظهر حالة مختصرة في العناصر المتأثرة وContext Seam واحدة للقسم عند الحاجة، لا Error Cards متكررة.
- Dialog يستخدم فقط لخطر فقد مدخلات أو تعارض أو قرار مانع؛ كسر السلوك المألوف بحجة التميز مرفوض.
- الخطأ غير المعروف يستخدم عبارة محددة مثل `لم نتمكن من إكمال العملية` مع مسار حقيقي، ويمكن إخفاء المرجع التقني تحت التفاصيل.
- لا Shake أو Flash أحمر؛ تتحول الحالة في موضعها خلال `160–220ms` ولا تختفي قبل المعالجة.

#### مرفوض

- Banner أحمر لكل مشكلة، أيقونة ضخمة، Illustration حزينة، زر `حسنًا` لا يحل شيئًا، أو إعادة تحميل كاملة لمعالجة خطأ محلي.
- ادعاء حفظ البيانات من دون تأكيد النظام.
- عرض نجاح كامل في حالة النجاح الجزئي.

### 14.7 Offline, Local & Sync States

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Continuity Thread`

تفصل الواجهة بين الاتصال والحفظ والإرسال والتعارض؛ Offline لا يعني الفشل، والحفظ المحلي لا يعني الإرسال.

#### الحالات

- الاتصال العام: `تعمل الآن دون اتصال` داخل المنطقة العلوية، مع وصف إمكان المتابعة فقط إذا دعمه النظام.
- العملية: `محفوظ على هذا الهاتف`، `بانتظار الإرسال`، `جارٍ الإرسال`، `لم يتم الإرسال`، أو `لم تُحفظ العملية`.
- حالة الإرسال المكتمل لا تحتاج شارة دائمة عادةً.
- عند تعدد العناصر يظهر ملخص مثل `3 عمليات بانتظار الإرسال` ويمكن فتح قائمة صفوف عادية إذا كانت الوظيفة موجودة.
- عودة الاتصال تتدرج داخل الموضع نفسه: عاد الاتصال، جارٍ إرسال العمليات، ثم تأكيد هادئ واختفاء الحالة.
- التعارض الحقيقي يفتح سطح مراجعة يعرض النسختين أو الفروق المتاحة ولا يحله UI تلقائيًا.

#### Continuity Thread

- `Micro System Ribbon` حالة للمنطقة العلوية نفسها؛ تتمدد كسطر مدمج بلا Card أو Pill أو Shadow.
- لا تكرر Offline في الصفوف؛ يعرض كل صف حالته الخاصة فقط.
- `Micro Sync Trace` جزء من بيانات الصف الثانوية بجانب الوقت أو المعلومة المساندة، لا Badge أو Chip.
- Micro Signal بنية واحدة متغيرة للحفظ المحلي والانتظار والإرسال والفشل، لا مجموعة أيقونات سحابة.
- الانتقال يحافظ على الموضع: محفوظ محليًا، بانتظار الإرسال، جارٍ الإرسال، ثم الحالة الطبيعية أو Recovery Seam.
- مؤشر إرسال متحرك واحد فقط؛ لا Spinner في كل عملية.
- Offline ليس Danger افتراضيًا، واللون لا يستبدل وصف الحالة.

#### مرفوض

- نقطة Online خضراء دائمة، Banner أصفر أو أحمر جاهز، Badges للحفظ، Sync Dashboard تقني، Toast لكل عملية، أو نسبة تقدم وهمية.
- دمج `غير محفوظ` مع `غير مرسل`، إخفاء العملية المعلقة، أو وعد بالإرسال التلقائي من دون ضمان النظام.

### 14.8 Blocked, Locked & Unavailable States

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Availability Choreography`

الواجهة تعرض ما يمكن فعله الآن، وتفصل بين المتطلب الناقص والصلاحية ووضع القراءة وقفل العملية وعدم صلة الوظيفة بالسياق.

#### التكوينات المعتمدة

1. **Action Substitution:** يحل الإجراء الممكن الآن محل الإجراء المعطل؛ مثل `إضافة التكاليف` بدل زر `عرض النتيجة` المقفل.
2. **Scope-Level Availability:** تظهر حالة الصلاحية أو القراءة مرة واحدة على مستوى القسم، لا تحت كل عنصر.
3. **Micro Read Surface:** بيانات للقراءة كعناوين وقيم وصفوف مفتوحة، لا Inputs رمادية معطلة.
4. **Micro Gate Trace:** يبقى موضع النتيجة معروفًا وتظهر `—` والسبب والإجراء الذي يفتحها.
5. **Contextual Withdrawal:** تسحب الوظيفة غير المفيدة من التكوين ويعاد توزيع المساحة.

#### قواعد الظهور

- يخفى العنصر إذا لم يرتبط بالمستخدم أو المهمة ولن يستطيع استخدامه في السياق.
- يبقى ظاهرًا إذا كان متوقعًا أو مؤقتًا أو توجد خطوة تفتحه.
- تعرض القيمة كبنية قراءة عندما يحتاج فهمها من دون تعديلها.
- الزر Disabled الصامت محجوز لحالة مؤقتة وواضحة جدًا مثل التنفيذ؛ غير ذلك يستبدل أو يشرح أو يسحب.
- الوظائف المستقبلية لا تملأ المسارات اليومية بعبارة `قريبًا`؛ تظهر فقط في موضع اكتشاف يخدم هدفًا واضحًا.
- عند اكتمال المتطلب يصبح العنصر متاحًا في مكانه خلال `160–220ms` بلا Glow أو Bounce.

#### مرفوض

- حقول ونماذج رمادية، أقفال متكررة، Tooltip يعتمد على Hover، `غير متاح` بلا سبب، أو قائمة معظم خياراتها معطلة.
- نفس الشاشة لكل الصلاحيات مع تغيير Opacity.
- زر Disabled بجانب زر حل المتطلب.

### 14.9 Contextual Actions and Final Review

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Action Topology`

الحالة السياقية لا تتحول تلقائيًا إلى CTA. الإجراء يملك موضعًا ثابتًا داخل بنية العنصر، ويستبدل بحسب السياق.

#### أنواع الإجراء

- `Resolve`: يحل السبب مباشرة مثل `إضافة التكاليف` أو `تحديد المحفظة`.
- `Inspect`: يفتح الفهم مثل `عرض التفاصيل` من دون منافسة الإجراء المباشر.
- `Retry`: يعيد العملية فقط عندما يكون ذلك آمنًا ومتاحًا فعلًا.
- `Dismiss`: للمعلومة الاختيارية غير الحرجة فقط.
- `No Action`: لا زر وهمي إذا لم يستطع المستخدم فعل شيء.

#### Action Topology

- لكل مكوّن `Action Slot`: في الطرف المقابل للصف، قرب القيمة، بعد سبب القسم، داخل System Ribbon، أو في موضع الإجراء الأساسي داخل Recovery Stage.
- الإجراء الجديد يحل محل السابق؛ لا يبقى زر Disabled إلى جوار الإجراء الذي يفتح المتطلب.
- الصف الكامل يفتح التفاصيل عندما يكون ذلك متوقعًا، والفعل النصي المنفصل للمعالجة المباشرة.
- داخل النطاق إجراء مباشر واحد ورابط تفاصيل واحد عند الضرورة؛ الخيارات الأخرى تنتقل إلى التفاصيل.
- لا Footer أزرار جاهز داخل Notice، ولا تكرار للإجراء في الحالة وأسفل الشاشة.
- التسميات أفعال محددة مثل `إضافة التكاليف` و`إعادة الإرسال`، لا `حسنًا` أو `متابعة` أو `إدارة` أو `اضغط هنا`.
- الإجراء الحساس ينتقل إلى مراجعة أو تأكيد مناسب، ولا ينفذ مباشرة من Notice.
- بعد الضغط يبدأ `Micro Action Hold`، ثم `Micro Causal Feedback` عند النجاح أو `Micro Recovery Choreography` عند الفشل، مع ثبات Action Slot.

#### قائمة المكونات النهائية

1. `Contextual Hierarchy`.
2. `Micro Context Trace`.
3. `Micro Reveal Row`.
4. `Micro Context Seam`.
5. `Micro System Ribbon`.
6. `Micro Recovery Stage`.
7. نظام Empty States الخمسة.
8. `Micro Loading Choreography`.
9. `Micro Causal Feedback`.
10. `Micro Recovery Choreography`.
11. `Micro Continuity Thread`.
12. `Micro Availability Choreography`.
13. `Micro Action Topology`.

#### إغلاق المحور

يعتمد Contextual UI التسلسل العام: تظهر الحالة من مصدرها، تكشف معناها، تقدم الإجراء المناسب، تعرض النتيجة في مكانها، ثم تعود إلى بنيتها الطبيعية. أي مخرج يعيد هذه القرارات إلى Alerts أو Cards أو Banners أو Badges أو Loading Templates جاهزة يرفض وفق القاعدة العامة في القسم `3`.

---

## 15. Interaction Design

**الحالة: مغلق بالكامل**  
**النطاق:** كيفية استجابة الواجهة منذ لحظة اللمس حتى ظهور النتيجة واستقرارها، من دون تغيير منطق المنتج.

### 15.1 Interaction Contract

**الأنظمة المعتمدة:** `Micro Response Chain` و`Micro Motion DNA`

كل تفاعل يتبع السلسلة:

1. `Discoverable`: يبدو العنصر تفاعليًا من شكله وموقعه ودلالته.
2. `Acknowledge`: يستجيب للمس خلال نحو `50–100ms` من دون انتظار الشبكة.
3. `Commit`: يبدأ التنفيذ أو الانتقال، ويستخدم `Micro Action Hold` إذا احتاج وقتًا.
4. `Resolve`: تظهر النتيجة في موضعها أو يبدأ الاسترجاع عند الفشل.
5. `Settle`: تعود الواجهة إلى السكون وتحفظ موضع المستخدم وتركيزه.

#### Motion DNA

- `Rooted Motion`: كل حركة مرتبطة بمصدر بصري واضح.
- `Compression`: ضغط سطحي محدود بدل Ripple أو Bounce.
- `Seam Reveal`: التفاصيل تنكشف من حدود المكوّن لا داخل Card جديدة.
- `Priority Motion`: مركز حركة رئيسي واحد في مساحة العرض.
- `Spatial Memory`: يحافظ التفاعل على موضع المستخدم والمصدر والنتيجة.
- Micro Signal هي الحركة الهووية المتكررة الوحيدة؛ بقية الحركة بنيوية ووظيفية.
- الشخصية مباشرة وثابتة ويقظة: بداية سريعة، مسار قابل للتتبع، ونهاية بلا Overshoot أو Spring مرئي.

#### التوقيت المرجعي

| الحالة | التوقيت |
|---|---:|
| استجابة اللمس | `50–100ms` |
| تغير محلي | `120–180ms` |
| توسع مكوّن | `180–240ms` |
| انتقال شاشة | `220–320ms` |
| ظهور نتيجة مركبة | `240–320ms` |
| بدء مؤشر انتظار | بعد نحو `300ms` |

لا تستخدم Presets كما هي، ولا حركة زخرفية أو Loader لفتح محتوى محلي أو Toast لكل ضغطة.

### 15.2 Tap, Press, Selection and Focus

**الأنظمة المعتمدة:** `Micro Surface Response` و`Micro Interaction Material`

تتبع الاستجابة طبيعة العنصر:

- `Compression` للفعل.
- `Surface Response` للفتح والتنقل.
- `Selection Lock` للاختيار.
- `Focus Contour` للتركيز.
- `Micro Action Hold` عند بدء التنفيذ.

#### مواد التفاعل

1. `Ink`: يتعمق وزن أو لون الأيقونة أو الإجراء النصي من دون خلفية Tile أو حركة استعراضية.
2. `Surface`: تتغير نبرة السطح، ويمكن تحريك المحتوى `1px` داخل الزر فقط، مع ثبات الحجم الخارجي.
3. `Seam`: يتحرك Divider وتنفتح بنية الصف أو القسم من موضعها.
4. `Contour`: يوضح Focus والاختيار حول Shape الحقيقي، بلا Glow أو اعتماد على اللون وحده.

#### قواعد المكونات

- الزر يستجيب فورًا ويعود خلال `100–140ms` بلا Ripple أو Bounce.
- الصف القابل للفتح يغير سطحه ولا يصغر أو يرتفع؛ الضغط على الإجراء الداخلي لا ينفذ فتح الصف أيضًا.
- البطاقة ليست قابلة للنقر تلقائيًا؛ تحتاج دلالة واضحة ولا تستخدم Hover Lift أو Scale.
- الاختيار حالة ثابتة تختلف عن Press، وتظهر من السطح والوزن والمؤشر والنص الدلالي.
- Focus Contour بسمك يقارب `2px` وOffset واضح، لا تقطع بسبب Overflow ولا تظهر بعد كل لمس عادي.
- أهداف اللمس `44×44px` على الأقل، ولا وظيفة أساسية خلف Long Press أو Double Tap.
- Haptics تحسين اختياري محدود، وليست قناة أساسية أو استجابة لكل Tap.
- اجتماع Focus وSelection وLoading وError لا يمحو حالة أخرى أو تسمية العنصر.

### 15.3 Motion and Spatial Continuity

**الأنظمة المعتمدة:** `Micro Spatial Grammar` و`Micro Anchor Map` و`Anchor Persistence`

#### القواعد المكانية

- المحور الأفقي للتنقل الهرمي، مع اتجاه RTL والرجوع العكسي.
- المحور العمودي للطبقات المرتبطة بالحواف.
- `Seam Expansion` للمحتوى السياقي داخل المكوّن.
- `In-place Transition` لتغير القيمة أو الحالة.
- `Direct Manipulation` للحركة التي يقودها الإصبع.

#### نقاط الارتكاز

1. `Header Anchor`: لوحة الحساب وSystem Ribbon.
2. `Tab Anchor`: التنقل وSwipe بين التبويبات الرئيسية.
3. `Source Anchor`: الصف أو البطاقة أو القيمة التي بدأت منها المهمة.
4. `Seam Anchor`: امتداد الصف أو القسم والسياق والاسترجاع.
5. `Action Anchor`: الحفظ والتنفيذ والنجاح والفشل وإعادة المحاولة.

يجب أن يبقى عنصر واحد على الأقل مستقرًا خلال الانتقال. كل حركة توثق مصدرها ووجهتها والعنصر الثابت وطريق العودة.

#### عائلات الحركة

- `Root Transition` لمستويات التنقل.
- `Dock Transition` للطبقات الخارجة من حافة.
- `Fold Transition` للانكشاف من Seam.
- `Replace Transition` للحالة في الموضع نفسه.
- `Track Transition` للحركة المباشرة مع الإصبع.

لا تجمع الحركة Slide وFade وScale وRotation وBlur معًا. الإغلاق أسرع من الفتح بنحو `30–50ms`، والحركة قابلة للمقاطعة ولا تتراكم في Queue. الأداء يستهدف تجربة مستقرة قريبة من `60fps` على هاتف متوسط، بلا Blur متحرك أو Shadows ثقيلة أو Parallax أو Staggered Cards.

### 15.4 Gestures, Swipe and Scroll

**النظام المعتمد:** `Micro Gesture Map`

ترتيب ملكية Gesture:

1. نظام الهاتف.
2. Scroll رأسي.
3. العنصر المحلي الأفقي أو التفاعلي.
4. Swipe بين التبويبات الرئيسية.
5. تحسين Gesture ثانوي.

#### القواعد

- Swipe بين `مشروعي الآن، العمل، المالية، أدواتي، السوق` معتمد كبديل إضافي للضغط على التبويب.
- العنصر الأفقي المحلي مثل Snapshot Deck يملك Gesture داخل منطقته، ولا تتحرك معه الصفحة الرئيسية.
- تحسم الواجهة المحور مبكرًا لمنع الاهتزاز القطري؛ الحركة الرأسية الواضحة تعود إلى Scroll.
- الشريط العلوي الصغير المندمج مع Status Bar ثابت ولا يختفي مع التمرير؛ عنوان الصفحة داخل المحتوى يتحرك معه.
- يحفظ Scroll عند فتح التفاصيل والرجوع والتنقل المؤقت وفتح Sheet، ويعاد فقط عند تغير مجموعة البيانات جوهريًا.
- Pull to Refresh تحسين ثانوي لبيانات قابلة للتحديث، لا يستخدم داخل نموذج أو مهمة مالية أو مع بيانات غير محفوظة.
- القوائم الطويلة تحمل تدريجيًا بواسطة `Micro Continuation Load`، ولا تقفز بعناصر جديدة أثناء القراءة.
- Swipe Actions مرفوضة للإجراءات المهمة والمالية، وDrag لا يعتمد دون حاجة وHandle واضح وبديل وصول.
- لا Gesture أساسية من الحافة القصوى بما يتعارض مع نظام الهاتف، ولا Pinch Zoom للرسوم المالية الأساسية.
- لكل Gesture بديل ظاهر وقابل للوصول.

### 15.5 Overlays and Interruptions

**الأنظمة المعتمدة:** `Micro Interruption Ladder` و`Micro Task Gravity` و`Micro Layer Choreography`

#### سلم المقاطعة

1. Inline للحالة التي يمكن حلها داخل السياق.
2. Anchored Layer لقائمة أو اختيار قصير مرتبط بمصدر.
3. Contextual Sheet لمهمة قصيرة ذات خطوة واحدة أو اختيار متعدد.
4. Full-screen Layer لمهمة ممتدة أو حساسة أو كثيرة الكتابة.
5. Dialog لقرار مانع أو خطر فقد أو فعل صعب التراجع.

يحدد وزن المهمة ومدة التفاعل وأثر الخطأ والحاجة إلى لوحة المفاتيح الطبقة، لا سهولة التنفيذ أو شكل القالب.

#### Layer Choreography

- `Source Tether`: تستجيب نقطة المصدر أولًا، وتخرج الطبقة منها أو من حافتها الصحيحة، ثم تعود النتيجة إليها ويعود Focus.
- Anchored Layer تحاذي مصدرها وفق RTL ولا تظهر Card عائمة في المنتصف.
- Sheet امتداد للحافة السفلية، Radius علوي `24px`، Canvas مفتوح، وHandle فقط إذا كان السحب متاحًا.
- Full-screen Layer مهمة مستقلة بلا Scrim أو Radius خارجي، ولا تبدو Sheet مكبرة.
- Dialog هو `Decision Frame`: قرار، أثر، وأفعال صريحة، بلا Hero Icon أو Illustration أو Card داخلية.
- Menu للخيارات، Popover لمعلومة قصيرة، Sheet لمهمة واحدة، Full-screen لمسار، وDialog لقرار واحد.
- لا Layer فوق Layer من النوع نفسه؛ المهمة التي تحتاج طبقة كبيرة ثانية تتحول إلى Full-screen.
- الضغط خارج الطبقة وSwipe للأسفل والإغلاق مسموحة فقط إذا لم تفقد مدخلات أو قرارًا.
- Focus ينتقل إلى الطبقة ويعود للمصدر؛ لوحة المفاتيح لا تغطي الإجراء النهائي.
- النجاح يغلق الطبقة بعد تأكيد النتيجة ثم يظهر أثره في المصدر، والفشل يبقيها مفتوحة ويحفظ المدخلات.

### 15.6 Sensitive and Destructive Interactions

**الأنظمة المعتمدة:** `Micro Consequence Gate` و`Micro Decision Frame` و`Micro Consequence Preview`

تتبع قوة التأكيد أثر القرار وإمكان التراجع:

1. منخفض وقابل للتراجع: تنفيذ مباشر مع Undo حقيقي عند توفره.
2. مؤثر وقابل للتصحيح: مراجعة Inline أو Sheet قصيرة.
3. حساس أو صعب التراجع: Decision Frame.
4. أثر مالي مركب أو غير قابل للتراجع: شاشة مراجعة عند تعدد الآثار.

#### Consequence Preview

- يبدأ القرار بهوية العنصر: الاسم والنوع والقيمة والتاريخ أو المعرف المساعد عند الحاجة.
- تظهر العاقبة في سياقها: الحالة الحالية وما ستصبح عليه، أو التغييرات التي ستفقد، أو الأثر الذي يوفره النظام.
- شدة الخطر تأتي من التكوين وفصل الإجراء وتوضيح العاقبة، لا من إغراق Dialog بالأحمر.
- Danger Color يقتصر على الفعل الحساس والكلمات الحرجة، والإجراء الآمن واضح وغير مخفي.
- لا يستخدم Dialog حذف لعكس عملية مالية، ولا يخلط UI بين الحذف والعكس والتصحيح.
- الأفعال كاملة مثل `حذف المنتج` و`إلغاء الطلب` و`تجاهل التغييرات`، لا `نعم / لا / تأكيد`.
- لا Swipe للحذف، ولا Hold to Confirm افتراضيًا، ولا Type to Confirm في العمليات اليومية.
- لا Confirmation متسلسلة، ولا Dialog لكل تعديل؛ منع Confirmation Fatigue قرار ملزم.
- الإجراء الحساس يترك دليلًا دائمًا عندما يوفره النظام، ولا تعتمد الثقة على رسالة مؤقتة.

### 15.7 Interaction Accessibility and Final Review

**الأنظمة المعتمدة:** `Micro Interaction Accessibility Contract` و`Micro Inclusive Continuity`

- لا Accessibility Mode أو Theme منفصل؛ الواجهة الأساسية نفسها تعمل باللمس ولوحة المفاتيح وقارئ الشاشة وتكبير النص وReduced Motion.
- البنية الدلالية تطابق البنية البصرية: العنوان والتبويب والزر والحالة والقيمة تعرف بأدوارها الحقيقية.
- جميع وسائل الإدخال تؤدي إلى المهمة والنتيجة والاسترجاع نفسيهما.
- Focus Contour ضمن Shape Language وبـToken عالي التباين، من دون Glow أو خلط مع Selection وError.
- تكبير النص يعيد توزيع الصفوف والإجراءات إلى أسطر ويزيد الارتفاع بدل القص أو Horizontal Scroll، مع اختبار حتى `200%` حيث تنطبق معايير الويب.
- Reduced Motion يحافظ على `Ink` و`Surface` و`Seam` و`Contour`، ويستبدل الاتجاه والـScale بتغيرات محلية بين `100–150ms`.
- المعلومات الضرورية لا تختفي تلقائيًا، ولا Countdown للقرارات اليومية، ولا وظيفة تعتمد على الصوت أو Haptics أو اللون وحدها.
- RTL يحدد ترتيب القراءة والتفاعل واتجاه الرجوع والمحاذاة، بينما تبقى الأرقام ومحور الزمن ورموز التشغيل في اتجاهاتها الصحيحة.
- لا وظيفة أساسية خلف Gesture مخفية، ولا عدة Animations أو إجراءات رئيسية متنافسة.

#### Motion and Interaction Proof المطلوب لاحقًا

يجب التحقق على هاتف فعلي من: تبديل التبويبات، Swipe، فتح التفاصيل والعودة، Account Panel، الفلاتر، Sheet، Reveal Row، تحديث قيمة، فشل الحفظ، الحفظ المحلي والإرسال، Dialog حساس، تكبير النص، TalkBack، لوحة المفاتيح، Reduced Motion، الاستخدام بيد واحدة، والاتصال المتقطع.

#### قائمة الأنظمة النهائية

1. `Micro Response Chain`.
2. `Micro Motion DNA`.
3. `Micro Surface Response`.
4. `Micro Interaction Material`.
5. `Micro Spatial Grammar`.
6. `Micro Anchor Map`.
7. `Micro Gesture Map`.
8. `Micro Interruption Ladder`.
9. `Micro Task Gravity`.
10. `Micro Layer Choreography`.
11. `Micro Consequence Gate`.
12. `Micro Decision Frame`.
13. `Micro Consequence Preview`.
14. `Micro Interaction Accessibility Contract`.
15. `Micro Inclusive Continuity`.

أي تنفيذ يعتمد Default Ripple أو Preset Transitions أو Bottom Sheet لكل مهمة أو Dialog تحذير عام أو Gestures مخفية أو Accessibility ملصقة بعد البناء يرفض وفق القاعدة العامة في القسم `3`.

---

## 16. Accessibility and RTL Review

**الحالة: مغلق**  
**الوظيفة:** مراجعة اتساق للمحاور المغلقة، لا إعادة تصميم لها.

### 16.1 RTL Structural Direction

**الحالة: مغلق**  
**النظام المعتمد:** `Micro RTL Direction Matrix`

تقسم العناصر إلى: عناصر اتجاهية تعكس، عناصر عالمية تبقى كما هي، محتوى `LTR` داخل الواجهة العربية، وعناصر يتحدد اتجاهها حسب المحتوى.

#### البنية والتنقل

- الواجهة والجمل والعناوين والقوائم `RTL` وتبدأ من اليمين.
- التبويبات الرئيسية مرتبة يمينًا إلى يسار: مشروعي الآن، العمل، المالية، أدواتي، السوق.
- سهم الرجوع والـChevron والحركة الهرمية تتبع RTL وسلوك المنصة، بينما لا تعكس الأيقونات العالمية والصور والشعار والرسوم غير الاتجاهية.
- ترتيب القراءة والوصول يطابق ترتيب الفهم، ولا يعتمد على إعادة ترتيب بصري تخالف البنية الدلالية.
- Start وEnd يستخدمان للعلاقات التابعة للواجهة؛ Left وRight يحجزان للمواضع الفيزيائية المقصودة مثل محور زمني.

#### Swipe

- الحركة تتبع إصبع المستخدم مباشرة: سحب يسارًا يحرك المحتوى يسارًا وسحب يمينًا يحركه يمينًا.
- الصفحة المجاورة تتحدد وفق ترتيب التبويبات المرئي وسلوك Pager القياسي في RTL، لا بقاعدة يدوية جامدة مثل `يسار = التالي`.
- الضغط على التبويب يبقى البديل الأساسي، ويجب التحقق من السلوك على هاتف عربي فعلي.

#### العناصر التي تبقى LTR

- الأرقام الإنجليزية والمبالغ والنسب والهواتف والمعرفات والبريد والروابط والتاريخ والوقت الرقمي ومحور الزمن.
- تعزل السلسلة الرقمية أو التقنية حتى لا تنتقل العملة أو الإشارة أو الأقواس بسبب BiDi.
- الرسم وواجهته RTL، بينما محور الزمن يبقى الأقدم يسارًا والأحدث يمينًا، مع بقائه فرضية تحقق مع مستخدمين أردنيين.

#### النماذج والطبقات

- Labels ورسائل الخطأ RTL، بينما حقول الرقم والهاتف والمبلغ والتاريخ LTR.
- Prefix وSuffix والأيقونات توضع حسب الوظيفة لا بالعكس الآلي.
- Menus وDropdown وSheets وDialogs تحاذي مصادرها وترتيبها وفق RTL مع الحفاظ على السلوك المألوف.
- لا تقلب الصفحة أو جميع SVGs باستخدام Transform، ولا تستخدم مسافات يدوية لإصلاح النص المختلط.

### 16.2 Mixed Text, Numbers, Dates and Currency

**الحالة: مغلق**  
**الأنظمة المعتمدة:** `Micro Data Formatting Matrix` و`Currency Context Rule`

#### الأرقام والمبالغ

- أرقام إنجليزية فقط، والسلاسل الرقمية `LTR`، مع Tabular Numbers للمقارنة.
- الشكل الكامل للمبلغ: `1,250.50 د.أ`.
- منزلتان افتراضيًا، وتظهر الثالثة إذا كانت قيمة حقيقية؛ لا تعرض `.000` بلا حاجة ولا تخفى قيمة ثالثة.
- السالب يستخدم العلامة `−` ومعنى مكتوب، ولا تستخدم الأقواس المحاسبية أو اللون وحده.
- النسبة مثل `12%` مع تفسير، ولا تعرض `0%` عند غياب بيانات المقارنة.
- الصفر الحقيقي `0.00 د.أ`، والغياب أو التعذر أو التقدير يستخدم حالته النصية المعتمدة.

#### التاريخ والوقت والبيانات التقنية

- التاريخ الرقمي `DD/MM/YYYY` مثل `22/09/2026`، والتاريخ المكتوب مثل `22 سبتمبر 2026`.
- الوقت اليومي مثل `2:35 م` أو يتبع تفضيل الجهاز بين 12 و24 ساعة إذا دعمه النظام، مع اتساق الشاشة.
- الهاتف يعرض مثل `079 123 4567` أو `+962 79 123 4567` باتجاه LTR.
- المعرفات مثل `ORD-1042` والبريد والروابط تبقى LTR ومعزولة داخل الجملة العربية.
- لا تستخدم مسافات يدوية أو Monospace عامًا لإصلاح الاتجاه أو تمييز كل رقم.

#### Currency Context Rule

لأن Micro يعمل في الأردن فقط ولا توجد نية توسع حاليًا:

- لا Currency Selector أو شاشة إعداد عملة أو رموز متعددة.
- تستخدم الواجهة العربية `د.أ`، بينما `JOD` يحجز لمستند رسمي أو تصدير تقني عند الحاجة.
- تظهر `د.أ` دائمًا مع المبلغ الرئيسي والمستقل، وحقول الإدخال، والملخص، والتفاصيل، وImpact Preview، والقرار المالي الحساس، وTooltip.
- في قائمة مالية متجانسة يمكن إظهار `المبالغ بالدينار الأردني` مرة في عنوان القسم وحذف `د.أ` من كل صف لتقليل الضوضاء.
- إذا اختلطت المبالغ بالكميات أو النسب أو الأعداد، تكرر `د.أ` مع كل مبلغ لمنع الالتباس.
- في الرسم تذكر الوحدة في العنوان أو المحور، وتعرض القيمة الكاملة مع `د.أ` في Tooltip و`عرض البيانات`.
- قارئ الشاشة يقرأ الاختصار بمعنى `دينار أردني` ولا يقرأ الرموز الخام بلا معنى.

#### القص والتكبير

- لا يقص المبلغ الأساسي ولا تنفصل العملة عن القيمة.
- يمكن قص الاسم الطويل مع إتاحة النص كاملًا، بينما تبقى الحالة المهمة والقيمة ظاهرتين.
- ينتقل الإجراء إلى سطر جديد وتزيد ارتفاعات الصفوف قبل ضغط الرقم أو إخفائه.

### 16.3 Contrast and Non-Color Communication

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Contrast Composition`

#### حدود القبول

- النص العادي لا يقل عن `4.5:1`، والنص الكبير لا يقل عن `3:1`.
- الأيقونات والحدود والإشارات الضرورية لفهم عنصر تفاعلي لا تقل عن `3:1` مع اللون المجاور.
- لا يكتفى بالمرور عند الحد حسابيًا؛ تفحص النتيجة على الهاتف وفي الضوء القوي.

#### نتيجة مراجعة اللوحة

- Deep Ink وSecondary Text على White وCanvas معتمدان.
- White على Indigo وDeep Ink على Citrus معتمدان.
- Disabled Text القديم `#9AA1AE` ضعيف للنص؛ استبدل بـ`#7D8596`، وبقي القديم للزخارف وSkeleton فقط.
- Border `#D8DDE8` Divider زخرفي ولا يحدد عنصرًا تفاعليًا وحده؛ أضيف `Interactive Boundary #7A8292` للحاجة الوظيفية.
- Terracotta لا يستخدم كنص عادي، ويبقى Accent هندسيًا ومساندًا.
- النص الأساسي داخل Soft Semantic Surfaces هو Deep Ink، والمساند Secondary Text؛ اللون الدلالي للإشارة أو كلمة الحالة، لا لفقرة كاملة.
- `Unknown` على سطحه وDanger عند حده لا يستخدمان نص فقرة صغيرة؛ النص الداكن يحمل المعلومة.

#### التركيب البصري

- لا Border لكل Card أو صف أو قسم؛ تستخدم المساحة والسطح والحجم والخط وDividers الداخلية لبناء الهرمية.
- نقاط التباين العالية في مساحة العرض: المعلومة الرئيسية، الإجراء الرئيسي، والحالة الحرجة إن وجدت.
- الكتل Indigo وCitrus وSoft Terracotta وLavender وMint تبقى معتمدة ضمن أزواج Foreground/Background الموثقة.
- Focus يستخدم Indigo على الأسطح الفاتحة وCitrus فوق Indigo، بحد واضح يقارب `2px` ومن دون Glow.
- الأسطح الدلالية حالات داخل المكوّن أو Seam، وليست Alert Cards جاهزة.

#### عدم الاعتماد على اللون

- كل حالة تستخدم النص ومعه إشارة أو شكل أو موضع أو وزن أو Icon مفهومة.
- الربح والخسارة والنتيجة الجزئية تكتب صراحة، ولا يمثلها الأخضر والأحمر وLavender وحدها.
- Selected يختلف بالسطح والوزن والمؤشر والحالة الدلالية؛ Pressed مؤقت وFocus مستقل وError لا يمحو الحالتين.
- الحالة غير المتاحة تشرح سببها ولا تخفض Opacity للمكوّن كاملًا.

#### الرسوم والصور

- ألوان الرسم لا تستخدم كنص صغير لمجرد نجاحها كعنصر رسومي.
- السلسلة تعرف باسم وMarker أو نمط عند الحاجة، وTooltip و`عرض البيانات` إلزاميان.
- أجزاء Half-Ring تفصل بفراغات وحدود سطح، ولا يعتمد المستخدم على التمييز بين الأحمر والأخضر.
- لا نص فوق صورة أو Gradient متغير من دون Surface ثابتة مضمونة، ولا Blur لتحسين التباين.

#### الاختبار الواقعي

- يختبر النظام في ضوء الشمس، وسطوع متوسط، وشاشة هاتف متوسطة، ومع واقي شاشة ووضع توفير الطاقة.
- إذا انخفض الوضوح تزال الزخارف قبل المعلومات والتغذية الراجعة الوظيفية.
- نجاح النسبة لا يكفي إذا فقدت الواجهة `Active Clarity` أو أصبحت Wireframe أو Accessibility Template رمادية.

### 16.4 Touch, Focus, Keyboard and Screen Reader

**الحالة: مغلق**  
**الأنظمة المعتمدة:** `Micro Operability Contract` و`Micro Invisible Access Architecture`

هذه طبقة تشغيل مدمجة في مكونات Micro الأصلية، وليست Accessibility Theme أو Widget خارجيًا أو مجموعة مكونات عامة فوق التصميم. تبقى الواجهة معبّرة بصريًا، بينما تصبح مساحة التشغيل والمعنى والحالة ومسار الاسترجاع قابلة للاستخدام عبر اللمس ولوحة المفاتيح وقارئ الشاشة.

#### أهداف اللمس ومنع الخطأ

- مساحة التشغيل القياسية لأي عنصر قابل للنقر لا تقل عن `44×44px`، وتصل إلى `48×48px` في التنقل الأساسي والإجراءات المتكررة أو الحساسة.
- يمكن أن تبقى الأيقونة المرئية `20–24px`؛ لا تتحول مساحة التشغيل إلى مربع أو Border أو زر ضخم ظاهر.
- المسافة العملية الافتراضية بين هدفين متجاورين `8px` على الأقل، مع فصل أكبر عندما يكون أحدهما حذفًا أو إلغاءً أو إجراءً ماليًا حساسًا.
- الصف الكامل يصبح هدفًا واحدًا إذا كان له مقصد واحد واضح؛ لا توضع داخله أهداف صغيرة متداخلة أو مناطق لها نتائج مبهمة.
- يبدأ التنفيذ عند الإفلات لا بمجرد اللمس الأول، ويستطيع المستخدم إلغاءه بتحريك إصبعه بعيدًا قبل الإفلات حيث تسمح المنصة؛ هذا يمنع التنشيط العرضي.
- لا وظيفة يومية أساسية خلف Long Press أو Double Tap أو هدف دقيق، ولا تعتمد المهمة على حافة يصعب الوصول إليها أو على Gesture وحدها.
- تراعي الأهداف Safe Areas، ولوحة المفاتيح، والاستخدام بيد واحدة، ولا يلتصق هدف حساس بحافة الشاشة أو System Gesture Zone.

#### التركيز البصري وإدارته

- كل عنصر تفاعلي حقيقي قابل للتركيز وله Focus واضح؛ الزخارف والحاويات غير التفاعلية لا تدخل مسار التركيز.
- يظهر Focus Contour عند استخدام لوحة المفاتيح أو وسيلة إدخال تتطلبه، وليس Border دائمًا حول كل عنصر.
- يستخدم Indigo فوق الأسطح الفاتحة وCitrus فوق Indigo، بسماكة تقارب `2px` وOffset يحفظ الشكل، ومن دون Glow أو خلط مع Selection أو Error.
- لا يحجب العنصر المركّز Header أو Bottom Navigation أو Sheet أو لوحة المفاتيح؛ تتحرك مساحة العرض لإبقائه ظاهرًا من دون قفزة مربكة.
- ترتيب التركيز يطابق ترتيب الفهم والقراءة في RTL، لا ترتيبًا بصريًا مصطنعًا أو DOM مختلفًا.
- عند فتح Dialog أو Sheet ينتقل التركيز إلى عنوانها أو أول قرار منطقي، ويبقى داخلها حتى الإغلاق، ثم يعود إلى العنصر الذي فتحها.
- التحديثات التلقائية لا تسرق التركيز. إذا اختفى العنصر المركّز ينتقل التركيز إلى أقرب موضع منطقي محفوظ، لا إلى بداية الصفحة عشوائيًا.
- في Tabs وRadio Groups والقوائم المركبة يستخدم عنصر واحد نقطة دخول، ثم تتحرك الأسهم داخل المجموعة وفق النمط القياسي للمنصة؛ لا تتحول كل وحدة داخل المكوّن إلى محطة Tab منفصلة.

#### لوحة المفاتيح ووسائل الإدخال

- كل مهمة قابلة للإنجاز من دون Touch، وبالنتيجة والاسترجاع نفسيهما.
- `Enter` و`Space` ينفذان السلوك القياسي لنوع العنصر، و`Escape` يغلق الطبقة الحالية فقط عندما يكون الإغلاق آمنًا.
- الأسهم تعمل داخل Tabs وMenus وRadio Groups وSteppers وفق معنى المكوّن وسلوك المنصة؛ لا تعكس جميع المفاتيح آليًا لمجرد أن الواجهة RTL.
- لا اختصارات إلزامية أو تركيبات ذات توقيت دقيق، ولا تعارض مع اختصارات النظام أو المتصفح.
- لا يتم تحويل `div` أو بطاقة عامة إلى زر شكلي؛ يستخدم الدور الدلالي الصحيح مع الحفاظ على التكوين البصري الخاص بـMicro.

#### عقد قارئ الشاشة

كل عنصر تفاعلي يعلن بالترتيب المناسب: الاسم، النوع، الحالة، القيمة، ثم التعليمات الضرورية فقط.

- الاسم الظاهر يدخل في الاسم القابل للوصول؛ لا يسمع المستخدم تسمية مختلفة عما يراه.
- زر الأيقونة العلوية يعلن `فتح الحساب والإعدادات`، لا `أيقونة قائمة` أو اسم الأيقونة الداخلي.
- التبويب يعلن اسمه وحالة اختياره، والفلتر يعلن اسمه والقيمة الحالية وحالة الفتح، والعنصر القابل للتوسيع يعلن `مفتوح` أو `مطوي`.
- الحقول تعلن الاسم ونوع الإدخال والقيمة وهل هي مطلوبة أو غير صالحة، ثم رسالة المساعدة أو الخطأ المرتبطة بها مرة واحدة.
- حالات مثل `محفوظ محليًا` و`بانتظار المزامنة` و`فشل الحفظ` و`غير متاح` و`للقراءة فقط` تعلن بوصفها حالات ذات معنى، لا كنصوص تقنية.
- الأسماء البرمجية الداخلية والإنجليزية وأسماء مكتبات الأيقونات لا تصل إلى المستخدم العربي.
- لا تكرر الأيقونة المجاورة للنص الاسم نفسه، ولا تعلن زخرفة أو Badge بلا معنى مستقل.

#### قراءة المال والتاريخ والرسوم

- المبلغ يقرأ كقيمة مالية ومعه `دينار أردني`، وتقرأ الإشارة بمعناها مثل `ناقص` أو `مستحق` عندما يلزم، لا كسلسلة رموز خام.
- التاريخ والوقت يقرآن بصيغة طبيعية، بينما تبقى قيمتهما الرقمية المرئية وفق مصفوفة التنسيق المعتمدة.
- الرسم يملك عنوانًا وخلاصة نصية وقائمة أو جدول بيانات مختصرًا؛ لا يتنقل قارئ الشاشة بين كل Path أو Segment رسومي.
- Half-Ring يعلن القيمة، معناها، اتجاه التغير، وجودة البيانات عند الحاجة، مثل: `المصروفات 320 دينارًا أردنيًا، أعلى من الفترة السابقة بنسبة 8%`.
- Tooltip ليس المصدر الوحيد للمعلومة، و`عرض البيانات` هو البديل الصريح للرسم.

#### ترتيب القراءة والإعلانات الحية

- البنية الدلالية تطابق ترتيب العرض؛ لا تستخدم إعادة ترتيب CSS لإنتاج تسلسل يخالف قارئ الشاشة.
- صف العملية يقرأ: الاسم، المعلومة الثانوية، المبلغ، الحالة، ثم الإجراء عند وجوده.
- نجاح الحفظ والتحديثات غير الحرجة تعلن بهدوء، بينما يحجز الإعلان الفوري للأخطاء التي تمنع المهمة أو تحتاج تدخلًا مباشرًا.
- لا يعلن كل Skeleton أو انتقال أو تحديث عددي صغير. تجمع المزامنة الخلفية في نتيجة واحدة مفيدة لتجنب الضوضاء الصوتية.
- الرسالة المعلنة توضح النتيجة والخطوة التالية، ولا تكتفي بكود خطأ أو حالة تقنية.

#### الطبقات والنماذج

- عند فتح Dialog أو Sheet تصبح الخلفية غير تفاعلية لقارئ الشاشة ولوحة المفاتيح، ويكون للطبقة عنوان ووسيلة إغلاق معروفة.
- Swipe للإغلاق وسيلة إضافية فقط؛ يوجد زر أو إجراء قابل للوصول يحقق النتيجة نفسها.
- Labels دائمة وليست Placeholder فقط، والمطلوب لا يعرف بالنجمة أو اللون وحدهما.
- يرتبط الخطأ بالحقل، لا تمسح قيمة المستخدم، وينتقل التركيز بعد فشل الإرسال إلى ملخص الخطأ أو أول حقل يحتاج تصحيحًا وفق حجم النموذج.
- لا تكرر رسالة الخطأ بصريًا وصوتيًا بطريقة مزعجة، ولا تستخدم Toast لمعلومة يحتاج المستخدم الرجوع إليها.

#### الحماية من القوالب الجاهزة

- الوصول لا يفرض بطاقات رمادية أو Borders عامة أو أزرارًا مربعة أو أحجامًا مرئية ضخمة؛ مساحة التشغيل قد تكون أكبر من الرسم المرئي.
- لا Floating Accessibility Widget ولا Accessibility Mode ولا حزمة جاهزة تغير هوية Micro.
- الدلالات الأصلية للمنصة والسلوك المتوقع يحفظان، بينما الشكل والتكوين والحركة واللغة البصرية تتبع Micro.
- أي مكوّن جميل بصريًا لكنه يستخدم دورًا خاطئًا أو ترتيب تركيز مصطنع أو تسمية عامة يرفض، وكذلك أي مكوّن Accessible يبدو كعنصر جاهز غير مندمج في الهوية.

### 16.5 Text Scaling, Reflow and Small Screens

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Adaptive Reflow`

عند تكبير النص أو ضيق الشاشة يعاد تركيب المكوّن، ولا يصغر النص ولا تقص المعلومة المالية أو المهمة. إعادة التوزيع جزء من تصميم كل مكوّن وليست Stack آليًا يحول الواجهة إلى بطاقات عامة متشابهة.

#### حدود الدعم

- تدعم الواجهات تكبير النص حتى `200%` دون فقدان مهمة أو قيمة أو حالة أو إجراء أساسي.
- تصمم الشاشات للعمل ابتداءً من عرض `320px`، مع احترام Safe Areas ولوحة المفاتيح وشريط النظام.
- لا ارتفاع ثابت لمكوّن يحتوي نصًا أو أرقامًا متغيرة، ولا تصغير تلقائي للخط كي يدخل في مساحة ضيقة.
- لا Horizontal Scroll للشاشة كلها. يستثنى فقط المحتوى ثنائي الأبعاد الذي لا يمكن إعادة تركيبه، مع بديل مقروء واضح.

#### ترتيب الحماية عند ضيق المساحة

1. الرقم أو النتيجة الرئيسية.
2. الحالة المالية.
3. الإجراء الأساسي.
4. اسم العنصر.
5. المعلومات المساندة.
6. الزخارف والتفاصيل الثانوية.

تزال الزخرفة أولًا، ولا يخفى المحتوى الضروري لمجرد المحافظة على شكل ثابت.

#### قواعد النص والقيم

- لا يقص المبلغ أو الحالة أو الإجراء الأساسي، وتبقى القيمة ووحدة العملة معًا.
- الاسم أو الوصف الطويل يلتف، ويجوز اختصار النص الثانوي فقط إذا أمكن كشفه كاملًا بطريقة واضحة.
- تزيد Line Height وارتفاعات الصفوف وفق قياسات العربية، ولا يضغط النص عموديًا أو تتداخل حروفه وعلاماته.
- لا يعتمد نجاح المكوّن على سطر واحد، ولا تستخدم Ellipsis للمعلومة التي تغير قرار المستخدم.

#### إعادة تركيب المكونات

- الصف الأفقي يتحول إلى تركيب رأسي منظم عندما لا يعود يحقق القراءة والمقارنة.
- ينتقل الإجراء إلى سطر مستقل قبل ضغط الرقم أو إخفائه.
- تتحول مجموعة الإجراءات إلى إجراء أساسي ظاهر وقائمة إجراءات ثانوية معتمدة بدل صف أزرار مزدحم.
- تنتقل الفلاتر إلى طبقة الاختيار المعتمدة عندما لا تتسع، ولا تصغر إلى Chips غير مقروءة.
- تزيد البطاقة المالية ارتفاعها وتحافظ على قيمة الرقم ووزنه البصري.
- يسمح Bottom Navigation بارتفاع متكيف وتسميات من سطرين عند الضرورة؛ لا يحول التبويبات الخمسة إلى أيقونات بلا أسماء.
- لا تغير إعادة التوزيع معنى الترتيب أو موضع الإجراء بشكل مفاجئ بين أحجام متقاربة.

#### الجداول والرسوم

- الجدول الواسع يتحول إلى صفوف مالية منظمة أو عرض تفصيلي، ولا يصغر أفقيًا حتى يفقد القراءة.
- يتكيف الرسم مع العرض، ولا تصغر التسميات أو القيم دون حد القراءة.
- يبقى `عرض البيانات` بديلًا صريحًا وثابتًا للرسم، ولا يكون Tooltip المصدر الوحيد للقيمة.
- عند ضيق المساحة تزال التفاصيل الزخرفية والمحاور الثانوية قبل القيمة والسياق والمقارنة الضرورية.

#### Sheets وDialogs ولوحة المفاتيح

- محتوى الطبقة قابل للتمرير، ويبقى العنوان والإغلاق والإجراء الأساسي قابلًا للوصول من دون حجز مساحة مفرطة.
- لا تختفي الحقول أو رسائل الخطأ أو زر الإجراء خلف لوحة المفاتيح.
- إذا لم تعد Sheet الثقيلة مريحة بعد تكبير النص، تتحول إلى شاشة كاملة؛ وضوح المهمة أعلى أولوية من الحفاظ على نوع الطبقة.
- الانتقال بين الحقول لا يسبب قفزة تفقد المستخدم سياقه، وتبقى القيمة التي يعدلها ظاهرة.

#### الحماية من القوالب الجاهزة

- Reflow لا يعني تحويل كل عنصر إلى Generic Card أو تكديس كل شيء رأسيًا.
- يحافظ التكوين على الكتل اللونية والـSeams والهرمية وShape Language الخاصة بـMicro.
- يتغير التكوين الداخلي للمكوّن قبل إنشاء Container أو Card إضافية.
- لكل مكوّن استجابة مرتبطة بوظيفته وأولوية معلوماته، ولا يستخدم Grid جاهزًا يقرر الترتيب بدل المصمم.

#### مصفوفة التحقق

تفحص الشاشات على عروض `320px` و`360px` و`390px`، ومع تكبير نص `100%` و`150%` و`200%`، وتشمل أسماء عملاء ومنتجات طويلة، ومبالغ كبيرة وثلاث منازل عشرية، ورسالة خطأ طويلة، وظهور لوحة المفاتيح، وسطرًا عربيًا مختلطًا برقم وهاتف وتاريخ.

### 16.6 Reduced Motion, Timing and Sensory Comfort

**الحالة: مغلق**  
**الأنظمة المعتمدة:** `Micro Sensory Comfort Contract` و`Micro Reduced Motion Translation` و`One Active Motion Rule`

الحركة في Micro تشرح ما ضغطه المستخدم، وأين ظهرت النتيجة، وما الذي تغير، وهل اكتملت العملية. لا تستخدم لاستعراض التصميم أو لإخفاء انتظار حقيقي.

#### التوقيت المرجعي

| الحالة | التوقيت |
|---|---:|
| استجابة اللمس | `50–100ms` |
| تغير محلي | `120–180ms` |
| توسع مكوّن | `180–240ms` |
| انتقال شاشة | `220–320ms` |
| نتيجة مركبة | `240–320ms` |
| بدء مؤشر الانتظار | بعد قرابة `300ms` |

- الإغلاق أسرع من الفتح بنحو `30–50ms`.
- الحركة قابلة للمقاطعة ولا تتراكم في Queue، ولا يتحرك الهدف بعد بدء التفاعل معه.
- لا تجمع الحركة Slide وFade وScale وRotation في انتقال واحد.
- لا Bounce أو Preset Ripple أو حركة مكتبة جاهزة، ويستهدف الأداء ثباتًا قريبًا من `60fps` على هاتف متوسط.

#### Micro Reduced Motion Translation

يتبع Micro إعداد Reduced Motion في الجهاز تلقائيًا، من دون Theme أو وضع وصول منفصل.

تلغى الانتقالات الاتجاهية الكبيرة، وScale وZoom وParallax وDepth Effects وStagger، ورسم الخطوط تدريجيًا، وCount-up، والدوران، وAuto-scroll وAuto-carousel، وShimmer المتكرر، وأي Loop مستمرة.

تستبدل بـ:

- تغير Ink أو Surface مباشرة.
- Crossfade محلية بين `100–150ms`.
- ظهور النتيجة في موضعها النهائي.
- نص وحالة ثابتين بدل الحركة.
- مؤشر انتظار صغير ومحدود داخل موضع العملية عند الحاجة.

Reduced Motion لا يعني واجهة ميتة؛ تبقى الاستجابة والسبب والنتيجة واضحة من دون انتقال مكاني مزعج.

#### One Active Motion Rule

- توجد منطقة متحركة رئيسية واحدة فقط داخل مساحة العرض في اللحظة نفسها.
- لا خلفيات متحركة أو Confetti أو Pulse مستمر أو Shake أو Flash أحمر.
- لا Blur متحرك أو ظلال ثقيلة أو تأثيرات زجاجية متغيرة.
- لا تحرك قيمة مالية لمجرد تغيرها؛ تحدث في مكانها وتبرز مرة واحدة ثم تستقر.
- العجلة أو الخطر يظهران بالنص والموضع والتباين، لا بالنبض والوميض.

#### التحميل والانتظار

- لا يظهر مؤشر إذا انتهت العملية قبل نحو `300ms` لتجنب الوميض.
- بعد ذلك يظهر مؤشر محلي مرتبط بمصدر الانتظار، ومع الانتظار الملحوظ يظهر وصف مثل `جارٍ حفظ البيع`.
- لا Skeleton للشاشة كلها عندما ينتظر جزء صغير، ولا أكثر من Loading Animation واحدة في مساحة العرض.
- في Reduced Motion يستخدم Placeholder ثابت أو تغير Opacity هادئ بدل Shimmer.
- لا تستخدم الحركة لإعطاء إحساس زائف بالسرعة أو لإخفاء حالة الاتصال والحفظ والإرسال.

#### الرسائل والقيود الزمنية

- المعلومات المهمة والأخطاء والتعليمات المطلوبة لا تختفي تلقائيًا.
- Undo المؤقت يمنح وقتًا كافيًا ويتوقف عدّه عند التفاعل معه أو وصول قارئ الشاشة إليه.
- لا Countdown في المهام المالية اليومية.
- إذا فرض الأمان انتهاء جلسة، يظهر تحذير مبكر وإمكانية التمديد، ولا تضيع البيانات المدخلة.
- Toast لمعلومة قصيرة لا يحتاج المستخدم الرجوع إليها؛ النتيجة الحساسة تترك دليلًا دائمًا في سياقها.

#### الصوت والاهتزاز

- لا تعتمد نتيجة على الصوت أو Haptics وحدهما.
- الاهتزاز إن استخدم يكون قصيرًا ومحدودًا للنجاح أو الخطأ المهم، ويحترم إعداد الجهاز.
- لا أصوات احتفالية أو نغمات داخل العمليات المالية اليومية، ويمكن تعطيل الإشارات الحسية دون خسارة معنى.

#### التنقل وRTL

- في الوضع الطبيعي تحتفظ انتقالات الشاشة باتجاه RTL المعتمد، وتتبع حركة Swipe إصبع المستخدم مباشرة.
- في Reduced Motion تزال الحركة الاتجاهية ويستخدم انتقال محلي قصير.
- لا تعاد Animation كاملة عند العودة إلى شاشة سبق فتحها؛ يحفظ موضع التمرير والعنصر والتركيز.

#### الحماية من القوالب الجاهزة

- لا Fade عام لكل شيء، ولا Scale عند كل ضغطة، ولا دخول متتابع للبطاقات لمجرد الاستعراض.
- لا Animation Presets أو Lottie عامة أو مؤشر تحميل ضخم في منتصف الشاشة.
- تنشأ الحركة من مادة العنصر وموقع النتيجة والعلاقة بين المصدر والوجهة، وتستطيع الاختفاء دون فقدان معنى الواجهة.

### 16.7 Accessibility and RTL Final Quality Gate

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Inclusive Quality Gate`

هذه بوابة قبول ملزمة وليست قائمة اقتراحات. لا يعد التصميم أو التنفيذ مطابقًا لمجرد وجود RTL أو نجاح أداة آلية؛ يجب إثبات عمل المكوّن والشاشة والرحلة على هاتف فعلي وفي الحالات الواقعية أدناه.

#### الطبقة الأولى: اختبار المكوّن

يختبر كل مكوّن في حالات Default وPressed وFocused وSelected وLoading وEmpty وError وUnavailable وSaved Locally وPending Sync، ومع محتوى طويل وتكبير النص وReduced Motion.

يجب التحقق من:

- مساحة لمس لا تقل عن `44×44px` وFocus واضح وغير محجوب.
- اسم ودور وحالة وقيمة صحيحة لقارئ الشاشة.
- عدم الاعتماد على اللون أو الحركة وحدهما.
- عدم قص الرقم أو العملة أو الحالة أو الإجراء الأساسي.
- بقاء المكوّن ضمن Shape Language وهوية Micro عند تغير حالته، وعدم تحوله إلى قالب عام.

#### الطبقة الثانية: اختبار الشاشة

| البعد | حالات الاختبار الملزمة |
|---|---|
| العرض | `320px`، `360px`، `390px`، `412px` |
| تكبير النص | `100%`، `150%`، `200%` |
| الحركة | عادية وReduced Motion |
| الإدخال | لمس، لوحة مفاتيح، قارئ شاشة |
| المحتوى | فارغ، جزئي، مكتمل، طويل، خطأ |
| الأرقام | صفر، سالب، كبير، حتى 3 منازل عشرية |
| الاتصال | متصل، بطيء، منقطع، مستعاد |
| الرؤية | ضوء قوي، سطوع متوسط، سطوع منخفض |

لا تعد Screenshot ثابتة أو Zoom في المتصفح اختبار هاتف. تغطى كل شاشة بالمقاس المرجعي والحالات ذات الصلة، وتفحص المكونات والرحلات الحرجة عبر توليفات العرض والتكبير والإدخال والاتصال ذات المخاطر العالية؛ لا يفترض ضرب جميع الأبعاد حسابيًا لكل شاشة. يجب إثبات التفاعل وإعادة التوزيع على جهاز فعلي في العينات الحرجة، وتوثيق سبب اختيار العينات.

#### الطبقة الثالثة: الرحلات الأساسية

تختبر المهمة من نقطة الدخول حتى ظهور نتيجتها الدائمة في: البيع النقدي، البيع الآجل، المصروف، تحصيل الدين، إنشاء ومتابعة الطلب، الشراء أو دفعة المورد، قراءة الكاش والربح والدين، الفلاتر، الضغط وSwipe بين التبويبات، الحساب والإعدادات، الرجوع من التفاصيل، وفشل الحفظ أو الاتصال.

في كل رحلة يثبت ما يلي:

1. نقطة البداية والقرار التالي واضحان.
2. نتيجة الإجراء متوقعة ويظهر أثرها في مصدرها الصحيح.
3. الأرقام والمعاني المالية لا تتغير أو تلتبس بسبب العرض.
4. التصحيح أو الاسترجاع متاح عندما يلزم.
5. المهمة تعمل باللمس ولوحة المفاتيح وقارئ الشاشة.
6. الرجوع يحفظ الموضع والسياق والتركيز.

#### الطبقة الرابعة: الأجهزة والبيئة

- هاتف Android فعلي متوسط الإمكانيات هو جهاز الاختبار الأساسي لملاءمته للفئة المستهدفة في الأردن.
- يضاف هاتف صغير وجهاز حديث مرجعي لمنع تحسين التصميم لمقاس واحد.
- إذا كانت iOS منصة مدعومة، يصبح اختبار iPhone وVoiceOver إلزاميًا لا اختياريًا.
- يستخدم TalkBack على Android، ولوحة مفاتيح فعلية أو Keyboard Navigation في نسخة الويب المدعومة.
- يختبر الاستخدام بيد واحدة، ومع واقي شاشة، وفي الضوء القوي والاتصال غير المستقر.

#### بيانات الضغط الواقعية

تشمل البيانات أسماء عربية طويلة، وأسماء مختلطة بالعربية والإنجليزية، وهواتف أردنية، و`0.00 د.أ`، وقيمة مثل `1,250,000.000 د.أ`، ونتيجة سالبة، وقيمة غير معروفة، وبيانات ناقصة، وعدة حالات مالية في الشاشة نفسها، وقوائم كبيرة من العمليات والمنتجات والعملاء.

#### تصنيف العيوب وقرار القبول

| المستوى | التعريف | قرار القبول |
|---|---|---|
| `P0` | يمنع المهمة أو يقدم معنى ماليًا خاطئًا | يمنع الاعتماد فورًا |
| `P1` | يخفي قيمة أو إجراء أو يفشل الوصول | يمنع الاعتماد |
| `P2` | يسبب صعوبة أو التباسًا ملحوظًا | يعالج قبل التسليم النهائي |
| `P3` | تحسين بصري محدود | يوثق ويرتب حسب الأولوية |

لا يغلق تسليم بوجود `P0` أو `P1`، ولا بتكرار `P2` داخل رحلة أساسية. النجاح الآلي لا يلغي الفحص البشري على الهاتف.

#### اختبار مستخدم أردني

تنفذ جولة تكوينية مع `5–7` مستخدمين تشمل صاحب مشروع منزلي، ومستخدمًا محدود الخبرة المالية أو الرقمية، ومستخدمًا يعتمد على الكاش وWhatsApp، وصاحب مشروع لديه ديون أو طلبات أو توصيل.

لا يسأل المشارك إن كان التصميم جميلًا؛ يعطى مهمة، ويراقب أين يبدأ، وكيف يفسر الرقم، وهل يميز الربح من الكاش، وهل يجد التحصيل أو المصروف، وأين يتردد، وهل يفهم النتيجة بعد الحفظ. الانطباع اللوني لا يحول الفرضية إلى حقيقة.

#### فحص منع القوالب وVibe Coding

1. `Color Removal Test`: عند إزالة الألوان مؤقتًا تبقى الهرمية والتكوين الخاصان بـMicro واضحين.
2. `Pattern Repetition Audit`: لا تستخدم البطاقة نفسها لكل نوع محتوى دون سبب وظيفي.
3. `Brand Substitution Test`: إذا أمكن وضع اسم تطبيق آخر دون تعديل التكوين، فالهوية غير كافية ويرفض الناتج.

#### شروط الإغلاق

أغلق محور Accessibility وRTL لأن قرارات القبول أصبحت مثبتة. أما التنفيذ اللاحق فلا يجتاز البوابة إلا إذا:

- عملت الرحلات الأساسية في RTL دون أخطاء اتجاه.
- بقيت القيم والإجراءات كاملة مع تكبير النص.
- أمكن إنجاز المهام باللمس ولوحة المفاتيح وقارئ الشاشة.
- عمل Reduced Motion دون خسارة معنى.
- لم تعتمد حالة على اللون أو الحركة وحدهما.
- لم توجد مشكلة `P0` أو `P1`.
- قدمت أدلة اختبار محددة، لا عبارة عامة مثل `تم الفحص`.

---

## 17. قرارات مرفوضة وغير مرجعية

المواد والاتجاهات التالية لا تمثل الرؤية الحالية ولا يجوز استخدامها كأساس:

- أي V1 أو V2 بصري سابق يتعارض مع هذه الوثيقة.
- ملفات HTML التي أعادت شكل Micro الحالي مع تغيير الألوان.
- `Bold Modular` كما نُفذ سابقًا إذا كان مجرد بطاقات وألوان.
- Beige / Clay كهوية رئيسية.
- Rail كعنصر شكلي مفروض.
- الزوايا المقطوعة.
- اعتبار الهدوء اللوني مرادفًا للجودة.
- نسخ Dashboard عالمي أو Design System معروف حرفيًا.

وجود اسم أو ملف قديم لا يمنحه سلطة على هذه الوثيقة.

---

## 18. المحاور المفتوحة

أغلقت محاور القرار الموثقة، بما فيها قاعدة القبول المرحلي في §21.5. لا تفسر جاهزية الحزمة للتوجيه بوصفها قبولًا لاستوديو تصميم لم ينتج بعد أو تفويضًا بالتنفيذ في النظام الحالي. التفاصيل التي لا تحددها هذه الوثيقة تُسجل فجوة أو اقتراحًا منفصلًا، ولا تُخترع كقرار معتمد.

---

## 19. بوابة الجودة لأي عمل لاحق

قبل قبول أي اتجاه أو واجهة لاحقة، يجب أن تجيب المراجعة عن الأسئلة التالية:

1. هل تغيرت فلسفة الواجهة وتكوينها، أم تغيرت الألوان فقط؟
2. هل يستطيع صاحب المشروع قليل أو متوسط الخبرة فهم الأولوية خلال ثوانٍ؟
3. هل يبقى المستخدم يقظًا من دون ضوضاء أو ضغط نفسي؟
4. هل المعلومة الأساسية ظاهرة وليست مخفية في Carousel أو طبقة؟
5. هل كل لون وحركة وشكل يؤدي وظيفة واضحة؟
6. هل تبدو الواجهة خاصة بـMicro أم كقالب جاهز؟
7. هل تظل الأرقام الإنجليزية واضحة داخل RTL؟
8. هل الحالات المالية الحساسة موضحة بالنص والبنية، لا باللون وحده؟
9. هل الحركة تشرح نتيجة الفعل في موضعها الحقيقي؟
10. هل يمكن إزالة أي عنصر زخرفي من دون خسارة معنى؟ إذا نعم، فيجب مراجعة سبب وجوده.
11. هل النموذج يكشف المطلوب دون إغراق المستخدم بالخيارات؟
12. هل فُصل الصفر الحقيقي عن القيمة غير المعروفة أو غير المسجلة؟
13. هل فُصل الكاش والمبيعات والديون والنتيجة بصريًا؟
14. هل أخفيت معلومة حرجة داخل Swipe أو طبقة؟
15. هل يوثق القرار واجهة مرئية فقط، أم اخترع قاعدة مالية أو منطق نظام خارج النطاق؟
16. هل اختير نوع الرسم لأنه يشرح البيانات، أم لأنه جذاب فقط؟
17. هل توجد قيمة دقيقة أو `عرض البيانات` بدل الاعتماد على الرسم وحده؟
18. هل تتضمن Insight القابلة للإجراء خلاصة ودليلًا وحالة اكتمال وإجراءً واضحًا؟
19. هل بقيت المعلومة المحايدة قرب سياقها بدل رفعها إلى `الأهم الآن`؟
20. هل فُصل معنى القيمة عن حالة حفظ العملية أو إرسالها؟
21. هل يستخدم `Micro Half-Ring` لأجزاء حقيقية من مجموع، ومن دون إشارات Gauge؟
22. هل تملك المكونات حالاتها، أم أضيفت Alerts وCards منفصلة فوقها؟
23. هل ترتبط الحالة بمصدرها وتنكشف وتتعافى في المكان نفسه؟
24. هل يوجد أكثر من عنصر عالي الحضور يتنافس على الانتباه؟
25. هل يبدو أي Notice أو Empty State كقالب جاهز حتى بعد تغيير ألوانه واسمه؟
26. هل صُنفت حالة الفراغ حسب سببها الحقيقي بدل استخدام `لا توجد بيانات` للجميع؟
27. هل حافظ تغير الحالة على بنية الشاشة والذاكرة المكانية للمستخدم؟
28. هل يعكس التحميل شكل المحتوى وأولويته بدل عرض Skeleton عام؟
29. هل توجد حركة Loading واحدة فقط داخل مساحة العرض؟
30. هل يظهر نجاح الإجراء في نتيجته ويترك دليلًا دائمًا عندما يكون حساسًا؟
31. هل يحافظ الخطأ على عمل المستخدم ويكشف موضع الانقطاع ومسار الاسترجاع؟
32. هل فُصل الاتصال عن الحفظ والإرسال والتعارض؟
33. هل تحولت حالة عدم الإتاحة إلى إجراء ممكن أو قراءة أو متطلب أو سحب، بدل Disabled UI؟
34. هل يملك الإجراء السياقي Action Slot واحدًا ولا يتكرر في أكثر من موضع؟
35. هل تستخدم الواجهة Patterns مألوفة لحماية الفهم من دون نسخ شكل مكتبة جاهزة؟
36. هل يمكن فصل الحالة السياقية ووضعها في تطبيق آخر بلا تعديل؟ إذا نعم، فيجب إعادة تكوينها داخل لغة Micro.
37. هل يتبع التفاعل `Discoverable → Acknowledge → Commit → Resolve → Settle`؟
38. هل استجابة Press ناتجة عن مادة العنصر أم Preset Ripple أو Scale عام؟
39. هل للحركة مصدر ووجهة وAnchor ثابت وطريق عودة واضح؟
40. هل تملك كل Gesture منطقة واضحة وبديلًا ظاهرًا ولا تتعارض مع نظام الهاتف؟
41. هل وزن الطبقة مناسب لمدة المهمة وأثرها أم استخدم Overlay كحل جاهز؟
42. هل يعرض القرار الحساس هوية العنصر والعاقبة وإمكان التراجع والفعل الصريح؟
43. هل تبقى نتيجة التفاعل ومكان المستخدم وFocus واضحين بعد الاستقرار؟
44. هل تعمل المهمة نفسها باللمس ولوحة المفاتيح وقارئ الشاشة وReduced Motion؟
45. هل يحافظ تكبير النص على المحتوى والقيم والإجراءات من دون قص أو تداخل؟
46. هل تبدو الحركة والتفاعل خاصة ببنية Micro أم إعدادات مكتبة جاهزة؟
47. هل السلوك مألوف وفق المنصة حتى عندما يكون الشكل خاصًا بـMicro؟
48. هل تتبع حركة Swipe الإصبع وسلوك RTL القياسي بدل اتجاه يدوي مخترع؟
49. هل بقيت الأرقام والمبالغ والتواريخ والمعرفات صحيحة ومعزولة داخل RTL؟
50. هل ظهرت العملة بالقدر الذي يمنع الالتباس من دون تكرار يخلق ضوضاء؟
51. هل تحقق النصوص والعناصر التفاعلية حدود التباين من دون تحويل الشاشة إلى Wireframe؟
52. هل بقي معنى الحالات واضحًا عند إزالة اللون؟
53. هل استخدمت Interactive Boundary عند الحاجة فقط، وبقيت Dividers الزخرفية خفيفة؟
54. هل أزواج Foreground/Background معتمدة بدل تركيب ألوان عشوائيًا؟

---

## 20. ترتيب العمل المعتمد من هنا

تم إغلاق محاور التصميم البصري و**Accessibility وRTL** وقاعدة التسليم والقبول المرحلي بالكامل. الحزمة الحالية تجهيز لتوجيه مصمم/Agent، وليست تنفيذًا لاستوديو التصميم أو تعديلًا لتطبيق Micro. اكتمال ملف القرارات لا يثبت إنتاج معاينة الشاشات أو إجراء اختبارات المستخدم.

---

## 21. Delivery, Implementation and Documentation Specification

**الحالة: مغلق كمرجع لتسليم التصميم؛ إنتاج الاستوديو والتحقق الواقعي لم يحدثا بعد**
**النطاق:** تحديد ما يجب تسليمه، وأين توجد الحقيقة المعتمدة، وكيف ينتقل التصميم إلى المنفذ من دون إعادة تفسير أو اجتهاد صامت. يتضمن عقد إنشاء استوديو تصميم مستقل قابل للتشغيل، ولا يجيز تغيير كود Micro الإنتاجي.

### 21.1 Delivery Architecture and Source of Truth

**الحالة: مغلق**  
**النظام المعتمد:** `Micro Design Source-of-Truth Architecture`

#### الملفات الرسمية

1. `MICRO-VISUAL-DESIGN-DECISIONS-V1.md`: المرجع الأعلى للفلسفة والهوية والقواعد والقرارات المعتمدة والمرفوضة.
2. `Micro Visual System — V1`: استوديو تفاعلي مستقل داخل هذا المستودع، بمصدر قابل للتعديل ومعاينة هاتف يمكن للمالك فتحها؛ Figma مخرج إضافي اختياري.
3. `Micro Visual Assets — V1`: حزمة الأصول المعتمدة فقط، وتشمل الشعار والأيقونات وSVG والخطوط وتراخيصها والرسوم والـPatterns والـTokens اللازمة.
4. `Micro Design Handoff Specification — V1`: مواصفات القياسات والحالات وReflow وRTL والحركة والوصول وما يمنع على المنفذ اختراعه.
5. `Micro Visual QA Evidence — V1`: أدلة التباين والتكبير وRTL والأجهزة وReduced Motion والعيوب ونتائج التحقق.

لا تعاد مرحلة `C1 / C2 / C3` بعد إغلاق الاتجاه. يوضع أي استكشاف مؤقت في مساحة منفصلة، ثم يعتمد أو ينقل إلى الأرشيف ولا يبقى خيارًا موازيًا للتنفيذ.

#### ترتيب السلطة عند التعارض

1. ملف القرارات المعتمد.
2. عقود المكونات وTokens المعتمدة في الاستوديو بعد مراجعة المالك.
3. مواصفات Handoff.
4. الشاشة المعتمدة.
5. ملاحظات المصمم أو المطور.

لا تلغي الشاشة قاعدة في ملف القرارات، ولا ينشئ تعليق داخل أداة التصميم قرارًا دائمًا. يسجل أي تعارض في `Decision Conflict Log` ويوقف الجزء المتأثر حتى يحسم، ولا يحل باجتهاد صامت.

#### تنظيم التسليم

```text
Micro Design Delivery
├── 00 — Read Me
├── 01 — Source of Truth
├── 02 — Interactive Studio Source & Preview
├── 03 — Visual Assets
├── 04 — Handoff Specification
├── 05 — QA Evidence
└── 99 — Archive
```

يحمل الأرشيف علامة `NOT FOR IMPLEMENTATION`. لا تستخدم أسماء مثل `final-final-2`؛ كل إصدار يحمل رقمًا وتاريخًا ووصف تغيير، وتوثق التغييرات في Change Log. لا تحذف النسخ السابقة ولا تستخدم تعليقات أداة التصميم بوصفها سجل قرار دائمًا.

#### قواعد الأصول والمكتبات

- لا تحتوي حزمة الأصول ملفات تجريبية أو صورًا مرجعية أو أيقونات غير مستخدمة أو أصولًا مجهولة الترخيص.
- يمكن استخدام مكتبة خارجية لPrimitive أو للتحقق التقني، ولا تستخدم UI Kit أو Community File كتصميم نهائي.
- يعاد تكوين كل مكوّن وفق Shape Language وهوية Micro، وتوجد Component Library رئيسية واحدة فقط.
- لا ينشئ كل مصمم أو Agent نظام ألوان أو Spacing أو Buttons خاصًا به.
- Screenshots المرجعية دليل اتجاه وليست أصولًا تنسخ حرفيًا.

#### معيار الاكتمال

يستطيع أي عضو جديد معرفة المرجع النهائي، والنسخة الحالية، والممنوع استخدامه، ومكان الأصول والمواصفات والأدلة، ومسار معالجة التعارض، من دون سؤال شفهي أو تخمين.

### 21.2 Interactive Studio Architecture and Design System Construction

**الحالة: مغلق**  
**المخرج المطلوب:** `Micro Visual System — V1` في استوديو مستقل داخل المستودع. مصدره وTokens وfixtures جزء من التسليم. لا تعتبر شاشاته معتمدة بمجرد تشغيله؛ تحتاج مراجعة المالك.

يبنى كنظام تصميم وتشغيل واحد، لا كمجموعة Screens مرسومة يدويًا ولا كـUI Kit عام. يوفر مسارات للمراجعة على الهاتف ومستكشف مكونات وحالات؛ لا يفرض إطار عمل أو Storybook بعينه.

#### أقسام الاستوديو

| القسم | المحتوى |
|---|---|
| `00 — Start Here` | حالة الملف والإصدار والروابط والمالك وطريقة الاستخدام |
| `01 — Foundations` | الألوان والخطوط والمسافات والأحجام وRadius والحدود والحركة |
| `02 — Identity & Assets` | الشعار ورمز الحساب والأيقونات والـPatterns والأصول |
| `03 — Core Components` | الأزرار والحقول والقوائم والطبقات والتنقل |
| `04 — Financial Components` | المبالغ والبطاقات والرسوم والديون والحالات المالية |
| `05 — Contextual Patterns` | Insights وEmpty وLoading وError وOffline وSuccess |
| `06 — Overview & Insights` | الوضع الحالي والملخصات وInsights |
| `07 — Operations & Orders` | العمليات والطلبات ومسارات التسجيل |
| `08 — Finance & Money` | الكاش والنتيجة والديون والمحافظ والحركة المالية |
| `09 — Catalog & Relationships` | المنتجات والخدمات والعملاء والموردون |
| `10 — Tools & Utilities` | الأدوات والحاسبة |
| `11 — Market & Services` | السوق والتوصيل والخدمات |
| `12 — Cross-flow Layers` | الحساب والإعدادات والفلاتر وSheets وDialogs |
| `13 — Stress & Edge Cases` | النص الطويل والمبالغ الكبيرة والأخطاء والشاشات الصغيرة |
| `14 — Motion, RTL & Accessibility` | الحركة والاتجاه وReflow وFocus وقارئ الشاشة |
| `15 — Handoff & QA` | القياسات والملاحظات والأدلة وحالة الاعتماد |
| `99 — Archive` | الاستكشافات والنسخ الملغاة فقط |

لا تنشأ نسخة مستقلة من الأساسات لكل شاشة، ولا تجمع كل الشاشات في صفحة ضخمة. ترتبط Routes المراجعة بـScreen IDs مستقلة عن ترتيب التبويبات؛ يشرح README طريقة التشغيل محليًا والبناء والوصول لمعاينة الهاتف.

#### عقد الاستوديو الحي

- المشروع مستقل عن تطبيق Micro الإنتاجي؛ يجوز كود الواجهات في مستودع الـHandoff فقط. لا يُنقل الكود إلى الإنتاج تلقائيًا ولا يدخل منطق محاسبي أو API أو بيانات مستخدم حقيقية.
- مسارات للمكونات والحالات ومسارات للشاشات الثلاث أولًا ثم لبقية المجال المثبت؛ لكل شاشة Screen ID وحالة بيانات ومصدر قرار ورابط قابل للفتح. أدوات اختيار الحالات والأحجام للمراجعة لا تصبح تلقائيًا عناصر في واجهة مستخدم Micro.
- يعمل التفاعل الحقيقي للواجهة: فتح وإغلاق Filter/Sheet/Dialog، انتقال ورجوع، Focus، Swipe حيث تقرر، وReduced Motion. الأفعال التي تحتاج حفظًا أو حسابًا أو خدمة لا تنفذ عملية حقيقية؛ تستخدم fixtures موسومة ومحاكاة معلنة دون رسالة نجاح كاذبة.
- توجد حالات Loaded وEmpty وPartial وUnknown وLoading وError وOffline حيث تؤثر على الفهم، وfixtures أردنية بأرقام `0–9` و`د.أ`، ومصدر معاني البيانات واضح؛ يمنع اختراع نتيجة ربح أو قيمة سوق.
- تحفظ الأدلة للشاشات والحالات الحرجة مع عرض `320/360/390/412` وتكبير `150/200%` وRTL وReduced Motion. تفصل اختبارات المتصفح عن Android فعلي وTalkBack واختبار المستخدم؛ غير المنجز `UNVERIFIED`.
- يثبت رابط عام للمعاينة وطريقة تشغيلها ونشرها إذا توفرت استضافة مصرح بها؛ المعاينة الحية شرط إعلان `Design Delivered for Owner Review` كاملًا. يرفع مصدرها هنا ولا يرفع أسرارًا أو بيانات حقيقية.

#### مستوى الهاتف المرجعي

- العرض المرجعي `360px` على Android متوسط الإمكانيات.
- ارتفاع الشاشة مرن حسب المحتوى ولا يستخدم Frame ثابتًا يقص الشاشة.
- يثبت Reflow على `320px` و`390px` و`412px`.
- توثق Safe Areas وشريط النظام، ويعرض اندماج المنطقة العلوية مع شريط الهاتف داخل معاينة المتصفح مع توضيح حدود تمثيل شريط النظام؛ لا يعد ذلك اختبارًا لتكامل تطبيق أصلي.

#### هرم النظام

```text
Foundations
→ Core Components
→ Financial Components
→ Contextual Patterns
→ Screens
```

لا يطبق Atomic Design حرفيًا إذا فكك المعنى المالي أو فرض مكونات تقنية عامة.

`Core Components` تشمل Buttons وIcon Buttons وInputs وSelection Controls وTabs وBottom Navigation وRows وMenus وDropdowns وSheets وDialogs وHeaders وFeedback.

`Financial Components` تبقى مكتبة مجال مستقلة وتشمل Financial Truth Layer وFinancial Summary Block وMoney Value ووحدات Cash / Revenue / Profit / Debt وTransaction Row وDebt and Collection Block وSupplier Balance وFinancial Impact Preview وMicro Half-Ring وChart Container وData Quality State وInsight Unit.

#### Variables وTokens

1. `Core Tokens`: Color Primitives وSpacing وRadius وStroke وTypography Values وIcon Sizes وMotion Duration وElevation.
2. `Semantic Tokens`: Canvas وSurfaces وText وActions وInteractive Boundary والحالات الدلالية.
3. `Component Tokens`: تضاف فقط لحاجة حقيقية مثل Button Background أو Input Error Boundary أو Navigation Selected Surface.

لا تستخدم المكونات Hex مباشرًا، ولا ينشأ Token لكل تفصيل صغير. `Light Mode` هو الوضع الوحيد، ولا تنشأ Dark Tokens فارغة للمستقبل. Reduced Motion يوثق كسلوك لا Mode لوني.

#### Typography Styles

- Alexandria هو الخط الأساسي وNoto Sans Arabic هو Fallback.
- تستخدم الأرقام الإنجليزية وTabular Numbers للقيم المالية.
- تسمى الأنماط وظيفيًا: `Type/Hero` و`Type/Screen Title` و`Type/Section Title` و`Type/Card Title` و`Type/Body` و`Type/Supporting` و`Type/Button` و`Type/Navigation` و`Type/Money Hero` و`Type/Money List`.
- لا تستخدم أسماء قياس مجردة مثل `24 Bold` بوصفها النظام الأساسي.

#### قواعد بناء المكونات

- تستخدم تخطيطات مرنة وCSS logical properties وTokens خارج المكونات وخصائص واضحة للحالات، مع RTL أصلي ومحتوى قابل للتغيير وReflow موثق.
- يمنع التموضع اليدوي لتصحيح كل شاشة، ورسم النص داخل Vector، والمسافات الفارغة بدل Padding، وتكرار Hex، ونسخ مكوّن لإنشاء نسخة غير موثقة، وقلب الشاشة كاملة بـMirror أو Transform.
- ينشأ Variant عند تغير السلوك أو البنية أو الحالة أو الحجم المعتمد أو الدور الدلالي، لا عند تغير النص أو الرقم فقط.
- لا تولد جميع التركيبات النظرية؛ كل Variant يجب أن يخدم حالة فعلية داخل Micro.

مثال تسمية خصائص:

```text
Button
Role = Primary | Secondary | Destructive
State = Default | Pressed | Focused | Loading | Unavailable
Size = Regular | Compact
Icon = None | Leading | Only
```

#### التسمية

تكون أسماء المكونات والطبقات التقنية واضحة ومتسقة بالإنجليزية، والمحتوى المرئي عربيًا.

```text
Navigation/Bottom/Item
Financial/MoneyValue/Hero
Financial/TruthLayer/Partial
Form/AmountInput/Error
Context/Insight/Actionable
Overlay/FilterSheet/Expanded
```

تسمى مسارات العرض/الحالات مثل `Finance/Overview/Loaded/360` و`Work/Orders/Empty/360`. تمنع أسماء مثل `Frame 128` و`Card New` و`Button Copy 7` و`Group 99`.

#### توثيق المكونات والمحتوى

لكل مكوّن رئيسي: الوظيفة، ومتى يستخدم ولا يستخدم، وAnatomy، والحالات، والمحتوى، وReflow، وRTL، ومناطق اللمس، وFocus، وقارئ الشاشة، والحركة، ومثال صحيح وآخر مرفوض.

تستخدم بيانات عربية وأردنية واقعية تشمل أسماء طويلة ومبالغ بالدينار وصفرًا حقيقيًا وقيمًا سالبة وديونًا جزئية وبيانات ناقصة وهواتف أردنية. يمنع Lorem Ipsum وJohn Doe والدولار والبيانات المثالية المتكررة.

#### الحماية من القوالب وVibe Coding

- لا تستورد Material UI أو Ant Design أو Dashboard Kit كقاعدة شكلية.
- لا يستخدم Community File كنظام Micro، ولا تصبح كل حاوية Card أو كل Screen Stack عموديًا.
- لا تقاس الجودة بعدد المكونات؛ تبقى Financial Components وContextual Patterns ذات تكوين خاص بـMicro.

### 21.3 Screen Coverage and Extensible Product Architecture

**الحالة: مغلق**  
**الأنظمة المعتمدة:** `Micro Screen Coverage Ledger` و`Micro Extensible Product Architecture`

قائمة الشاشات التالية هي Baseline للإصدار `V1` وليست Sitemap أبدية. تثبت لغة Micro وعقود الجودة، بينما يبقى ترتيب التبويبات وربط الشاشات ومجالات التوسع قابلًا للتغيير المنضبط.

#### ما هو ثابت وما هو مرن

| ثابت | قابل للتغيير بإصدار موثق |
|---|---|
| الهوية وSemantic Tokens | ترتيب التبويبات وتسمياتها |
| RTL والوصول | موقع الشاشة ونقاط دخولها |
| المعاني المالية | إضافة أو دمج شاشة أو مجال |
| عقود المكونات والحالات | أولوية الكتل والمحتوى |
| السلوك المألوف وبوابة الجودة | تطور السوق والتوصيل والأدوات |

التبويبات الخمسة هي `Navigation Mapping — V1`، لا قانونًا دائمًا. تغييرها مستقبلًا قرار IA بإصدار موثق، ولا يستلزم إعادة بناء المكونات أو هوية النظام.

#### المعرفات والمجالات

تعتمد Screen IDs على المهمة والمجال، لا موقعها في التنقل:

```text
OVR-NOW
OPS-ORDERS-LIST
OPS-SALE-CREATE
FIN-OVERVIEW
FIN-WALLETS
REL-CUSTOMER-DETAIL
CAT-PRODUCTS-LIST
MKT-SERVICE-DETAIL
```

تمنع معرفات مثل `TAB-2-SCREEN-4` أو `THIRD-PAGE` لأنها تنكسر عند إعادة الترتيب.

#### Navigation Mapping — V1

يسجل لكل Screen ID: موقعه الحالي، ونقاط الدخول السياقية، ومسار العودة. يمكن مثلًا فتح المحافظ من المالية ومن ملخص مشروعي، وفتح المنتجات من العمل ومن البيع والطلب، وفتح التحصيل من العمل ومن العميل والديون. تعديل هذا الجدول لا يغير هوية الشاشة أو مكوناتها.

#### Baseline الشاشات العالمية

- الهيكل ذو التبويبات الخمسة وحالات الاختيار والضغط وSwipe.
- المنطقة العلوية قبل التمرير وبعده، ولوحة الحساب مكتملة وغير مكتملة.
- حسابي وبيانات المشروع وإعدادات النظام وتأكيد تسجيل الخروج.
- التوصيل واسأل Micro.
- شاشة تفاصيل من المستوى الثاني ومهمة مركزة من المستوى الثالث.
- الرجوع مع تغييرات غير محفوظة، والفلاتر وSheets وDialogs والاختيارات المشتركة.

#### Overview & Insights

- وضع كامل موثوق، وبيانات جزئية، وبيانات غير كافية للحكم، ومشروع جديد.
- كاش موجود مع نتيجة غير مكتملة، وديون أو مصروفات تحتاج انتباهًا، وحالة مستقرة.
- كتلة مالية واحدة مع Swipe، و`عرض الكل`، وInsight قابلة للإجراء وأخرى معلوماتية، وتفاصيل الدليل.
- Loading محلي، وخطأ قابل للاسترجاع، وOffline، ونصوص وأرقام كبيرة.

#### Operations & Orders

- مركز العمل والإجراءات المتكررة وآخر العمليات والعناصر التي تحتاج متابعة.
- واجهات البيع النقدي والآجل والمصروف والتحصيل ودفع المورد والشراء ومال المالك والنقل، مع Impact Preview والنجاح وفشل الحفظ.
- قائمة الطلبات وحالاتها وفلاترها وتفاصيلها وإنشاؤها وتعديلها ومراحلها والمبالغ المتبقية والخروج غير المحفوظ.

هذه واجهات عرض وإدخال فقط؛ لا يحدد المصمم أثر العملية المالي.

#### Catalog & Relationships

- قائمة المنتجات والخدمات والبحث والفلترة والتفاصيل والإضافة والتعديل والاختيار داخل عملية.
- حالات المنتج والخدمة والبيانات الناقصة والقائمة الفارغة والمخزون المنخفض عند انطباقه.
- قوائم العملاء والموردين، وتفاصيل الشخص وحركاته ودينه أو رصيده، والتحصيل والدفعات والاختيار داخل نموذج.
- تبقى `منتجاتي وخدماتي` شاشة سياقية متعددة المداخل، لا تبويبًا سادسًا.

#### Finance & Money

- النظرة المالية والكاش والمبيعات والمصروفات والديون والنتيجة والمقارنة.
- الصفر الحقيقي والسالب والقيمة غير المعروفة والبيانات الجزئية.
- المحافظ وتفاصيلها وحركتها والتوزيع الكامل والناقص والمتجاوز والنقل بينها.
- ديون العملاء ومستحقات الموردين والتفاصيل والتحصيل أو الدفع الجزئي والكامل وImpact Preview.
- سجل الحركات والفلاتر والتفاصيل والتصحيح والعكس والحفظ المحلي وانتظار المزامنة والتعارض.
- Micro Half-Ring وBar وLine وTooltip والفترة والمقارنة وفجوات البيانات و`عرض البيانات` والقيم المتطرفة.

#### Tools, Market and Services

- الصفحة الرئيسية للأدوات والحاسبة ونتيجتها وكل أداة معتمدة، مع القراءة فقط والحاجة إلى إعداد وعدم الإتاحة المفسرة.
- السوق بمحتوى مفيد: الصفحة الرئيسية والأقسام والقوائم والتفاصيل والتوصيل والخدمات والبحث أو الفلترة وحالات الفراغ والخطأ وOffline.
- لا يكون السوق `قريبًا`، ولا تصبح الأدوات Grid من بطاقات متطابقة بلا هرمية.

#### حالات التغطية الإلزامية

- القوائم: First-use Empty وFiltered Empty وقصيرة وطويلة وLoading إضافي وخطأ وOffline وصف طويل وقيمة مجهولة وإجراء غير متاح.
- النماذج: فارغ ومعبأ واختياري ومشروط وخطأ واحد ومتعدد وحفظ وفشل ومحلي ونجاح وخروج غير محفوظ ولوحة مفاتيح وتكبير `200%`.
- التفاصيل: طبيعي ومطوي وموسع وحساس وغير متاح وناقص ومحدث وOffline ورجوع للموقع السابق.
- الطبقات: Menu وDropdown وFilter Panel وAccount Panel وBottom Sheet وDialog وFull-screen Layer وKeyboard وReduced Motion.

لا تضرب جميع الشاشات في جميع الحالات. كل شاشة إنتاجية تملك Default، وكل بنية مختلفة تثبت حالاتها، وكل رحلة حرجة تصمم من البداية إلى النتيجة، والحالة المتكررة توثق مرة في Component أو Pattern ما لم تغير تركيب الشاشة أو معنى القرار.

#### Screen Coverage Ledger

يسجل لكل شاشة: Screen ID، الاسم، المجال، Navigation Mapping الحالي، المستوى، نقاط الدخول والخروج، الحالات، المكونات، حالة RTL والوصول، حالة الاعتماد، وما يجب أخذه من منطق النظام دون اختراع.

حالات الشاشة/المكون هي `Exploration` و`In Review` و`Approved by Owner` و`Archived`. فقط `Approved by Owner` يصل إلى تطوير Micro الإنتاجي، ولا تعد التغطية مكتملة بوجود Route أو Layer إنتاجية غير مسجلة.

#### Extension Protocol

عند إضافة شاشة أو ميزة:

1. تحدد مهمة المستخدم ومجال المنتج بعيدًا عن التبويب.
2. تفحص المكونات والأنماط الحالية.
3. يعاد استخدام الموجود إذا كان مناسبًا، وينشأ Pattern جديد فقط لاختلاف حقيقي.
4. تحدد الحالات وStress Cases وRTL والوصول.
5. تضاف الشاشة إلى Coverage Ledger ثم تربط بـNavigation Mapping الحالي.
6. تمر عبر بوابة الجودة قبل الانتقال إلى Stable.

#### نضج المكونات والإصدارات

- `Candidate`: قيد الاختبار ولا يعمم بعد.
- `Stable`: معتمد للإنتاج.
- `Deprecated`: قديم مع بديل ومسار وتاريخ إزالة؛ لا يحذف فجأة.
- `Patch`: إصلاح بصري أو وصول غير كاسر.
- `Minor`: إضافة شاشة أو مكوّن متوافق.
- `Major`: تغيير Navigation أو IA أو عقد مكوّن مستخدم.

#### الحماية من الجمود والقوالب

- لا تستخدم الشاشة نفسها مع تغيير العنوان لكل مجال، ولا تربط مكونًا باسم تبويب أو ترتيبه.
- `Overview` للقراءة والقرار، و`Operations` للإجراء والمتابعة، و`Finance` للحقيقة والتفسير، و`Tools` للوصول المباشر، و`Market` للاستكشاف والقيمة.
- ثبات النظام يعني ثبات اللغة والعقود، لا تجميد مواضع الشاشات أو منع التطور.

### 21.4 Design-to-Development and AI Execution Contract

**الحالة: مغلق**  
**الأنظمة المعتمدة:** `Micro Design-to-Development Contract` و`Micro AI Executor Operating Contract` و`Legacy Visual Quarantine`

لأن المنفذ المتوقع غالبًا Agent ذكاء اصطناعي، لا يكفي Handoff تقليدي. يجب أن تكون المدخلات والحدود والمخرجات قابلة للتحقق، وأن يمنع الخلط بين الرؤية الجديدة وواجهة Micro الحالية.

#### ترتيب مصادر السلطة

1. `MICRO-VISUAL-DESIGN-DECISIONS-V1.md`.
2. المكونات والشاشات ذات حالة `Approved` في `Micro Visual System — V1` بعد مراجعة المالك.
3. `Micro Design Handoff Specification — V1`.
4. Screen Coverage Ledger وNavigation Mapping المعتمدان.
5. `Micro Visual Assets — V1`.
6. النظام الحالي مرجع للوظائف والبيانات ومسارات العمل فقط، وليس مرجعًا بصريًا.

لا تستخدم نتائج بحث أو اقتراحات Agent أو مكتبة خارجية لتجاوز مصدر أعلى. عند التعارض يطبق المصدر الأعلى ويسجل التعارض.

#### Legacy Visual Quarantine

- توضع Screenshots وملفات واجهة Micro الحالية داخل مساحة معزولة تحمل `LEGACY CURRENT UI — FUNCTIONAL REFERENCE ONLY — DO NOT COPY VISUALS`.
- يسمح باستخدامها لاستخراج Route أو وظيفة أو محتوى أو حالة منطقية مفقودة فقط.
- يمنع أخذ ألوانها وRadius والمسافات والبطاقات والأيقونات والتنقل والحركة والـCSS والتكوين البصري.
- لا تنسخ Component أو Layout أو Screenshot أو Style من النظام الحالي إلى النظام الجديد.
- يبدأ التصميم الجديد من Foundations والقرارات المعتمدة، لا من تعديل Screenshot أو HTML حالي.
- لا تفتح المرجعية القديمة أثناء الاستكشاف البصري إلا للحاجة الوظيفية المحددة، ويسجل ما استخرج منها كنص وظيفي.
- يقدم المنفذ في النهاية `Legacy Visual Contamination Check` يبين أن التصميم الجديد لم يرث البنية البصرية القديمة إلا ما نصت الوثيقة صراحة على الحفاظ عليه.

#### Agent Preflight الإلزامي

قبل إنشاء أي تصميم أو كود، يقرأ الـAgent جميع المصادر المعتمدة كاملة ثم يسلم:

1. `INPUT-INVENTORY.md`: الملفات والإصدارات التي قرأها.
2. `AUTHORITY-MAP.md`: ترتيب السلطة وما هو مسموح وممنوع.
3. `UNDERSTANDING-SUMMARY.md`: فهمه للاتجاه من دون اقتراح تصميم موازٍ.
4. `CONFLICT-AND-GAP-LOG.md`: التعارضات والفجوات المصنفة.
5. `EXECUTION-COVERAGE-PLAN.md`: ما سينتجه وربطه بأقسام القرار وScreen IDs.

لا يعد اجتياز Preflight موافقة على اختراع حلول؛ هو إثبات أن الـAgent فهم الحدود قبل العمل.

#### منع الافتراض والهلوسة

كل معلومة ناقصة تصنف كواحدة من:

- `Design Gap`.
- `Product Logic Gap`.
- `Asset Gap`.
- `Content Gap`.
- `Technical Constraint`.
- `Decision Conflict`.

يمنع ملء الفجوة بصفر أو نص عام أو مكوّن افتراضي أو استنتاج مالي أو تصميم من النظام القديم. إذا كانت الفجوة تحجب جزءًا، يوقف ذلك الجزء فقط ويستكمل الأجزاء المستقلة، مع وسمه `BLOCKED — NOT APPROVED`. لا يجوز إخفاء الفجوة أو الادعاء أن التسليم مكتمل.

أي تحسين غير منصوص عليه يوضع في `OPTIONAL-PROPOSALS.md` منفصلًا، ولا يدخل التصميم المعتمد تلقائيًا.

#### التتبع إلى القرار

- كل Screen وComponent وPattern رئيسي يحمل `Source References` تشير إلى الأقسام المعتمدة في هذه الوثيقة.
- يسجل `Decision Traceability Matrix` العلاقة بين القرار والمكوّن والشاشات وحالة التنفيذ والاختبار.
- أي عنصر بلا مصدر أو مبرر وظيفي يعامل كافتراض حتى يراجع.

#### حزمة التسليم الكاملة

لا يبدأ تنفيذ Micro الإنتاجي قبل توفر شاشة وحالات معتمدة، والمكونات والحالات، والـTokens، والقياسات، وReflow، وRTL، وحالات البيانات، والحركة، والوصول، والأصول، وScreen ID ومعايير القبول.

ويجب أن يسلم الـAgent في النهاية:

1. استوديو مستقل قابل للتشغيل من مصدره داخل المستودع، مع رابط معاينة مفتوح من هاتف المالك ودليل تشغيل وبناء موثق؛ رابط المستودع وحده لا يساوي معاينة حية.
2. Component Library حية وTokens منفصلة وحالات/variants موثقة، وfixtures معلّمة بوصفها بيانات عرض وليست حقائق للمنتج.
3. Screen Coverage Ledger مكتملًا.
4. Navigation Mapping الحالي.
5. `Design-to-Code Map`.
6. Token Dictionary قابلًا للتسليم، مع مصدر كل Token.
7. Asset Manifest وملفات SVG والخطوط والتراخيص.
8. Motion and Interaction Map.
9. RTL and Accessibility Annotations.
10. Stress and State Coverage Matrix.
11. QA Evidence Package.
12. Conflict, Gap and Exception Logs.
13. Change Log والإصدار النهائي.
14. `DELIVERY-MANIFEST.md` يحوي كل ملف ورابطه وإصداره وحالته وما إذا كان كاملًا أو محجوبًا.
15. `FINAL-COMPLETION-REPORT.md` يذكر المنجز والناقص والمرفوض ونتائج البوابات بلا عبارات عامة.

يلزم استوديو تفاعلي بمصدر قابل للتعديل: HTML ثابت أو Screenshots أو رابط جلسة Agent وحده غير كافٍ. يعد Figma أو Penpot إضافة اختيارية إذا توفرت الأدوات، ولا يحجب غيابهما التسليم. إذا تعذر توفير معاينة هاتف يمكن للمالك فتحها، يسجل المنفذ قيد الاستضافة ويستكمل المصدر وما يستطيع فحصه، مع إبقاء بند المعاينة `Blocked` وعدم إعلان الحزمة كاملة.

#### Design-to-Code Map

يربط كل Design Component بنظيره التقني والـTokens والحالات وحالة الجاهزية. لا يفرض Framework، لكنه يمنع وجود مكونات تقنية لا أصل لها أو حالات تصميم لا مقابل لها.

#### حدود المسؤولية

- المصمم يحدد التكوين والهرمية والألوان والخطوط والأحجام والحالات والحركة ومواضع الإجراءات وReflow وRTL المرئي ومعالجة البيانات.
- المطور يحدد البنية التقنية وإدارة الحالة والأداء وجلب البيانات وتنظيم الملفات والاختبارات وطريقة تحقيق الدلالات والوصول.
- لا يحق للمطور أو الـAgent تغيير لون أو Radius أو مكوّن أو ترتيب معلومة أو Layer Type أو أيقونة أو حالة أو حركة بدعوى سهولة التنفيذ.
- لا يخترع المصمم أو المطور أثرًا ماليًا أو قاعدة حساب أو حالة طلب أو صلاحية إجراء.

#### Design Gap Protocol

عند غياب تصميم: لا يستخدم Default UI، ولا ينسخ أقرب شاشة، ولا يرجع إلى شكل Micro الحالي. يسجل Screen ID والحالة والأثر ونوع الفجوة، ويستكمل غير المتأثر. بعد الحسم يحدث الاستوديو وHandoff وTraceability Matrix.

#### قواعد التنفيذ المرئي

- تبنى Tokens ثم Typography والأيقونات وCore Components وFinancial Components وContextual Patterns وGlobal Shell والرحلات الحرجة ثم بقية الشاشات وStress Cases.
- لا معالجة مرئية مختلفة للمكوّن نفسه في كل شاشة، ولا قيم تقريبية أو Screenshots بدل UI أو نص داخل Vector.
- الأصول SVG، والنص فعلي، وRTL لا ينفذ بقلب الصفحة كاملة، ولا يصغر Desktop Layout باستخدام Zoom.
- المكتبات التقنية مسموحة خلف النظام إذا أمكن ربطها بالـTokens والوصول وضبط الحالات؛ لا يظهر Material أو Bootstrap أو Ant أو Default Recharts للمستخدم.
- يستخدم Recharts عبر `Micro Chart System` فقط، وتخضع أيقونات المكتبة لقواعد Iconography.

#### تشغيل Agent واحد أو عدة Agents

- يبقى Agent رئيسي واحد مسؤولًا عن الاتساق والقبول النهائي.
- الوكلاء الفرعيون إن استخدموا يعملون على Work Packages محددة ولا ينشئون Design Direction أو Tokens أو Component Library مستقلة.
- لا يدمج مخرج فرعي قبل مراجعته مقابل Source References وبوابة الجودة.
- يمنع توزيع C1 وC2 وC3 على وكلاء بعد إغلاق الاتجاه، أو السماح لكل وكيل بتفسير الهوية منفردًا.

#### حالات التسليم

```text
Not Ready
→ Ready for Development
→ In Development
→ Ready for Visual QA
→ Changes Required
→ Accepted
```

فقط `Approved` و`Ready for Development` يسمحان ببدء التنفيذ البرمجي. تشير `Accepted` هنا إلى قبول مخرجات التطوير في مسارها، ولا تعني تلقائيًا أن اختبار التجربة الواقعية اكتمل؛ يفصل §21.5 المرحلتين. يستطيع Agent المصمم تسليم شاشات الاستوديو بحالة `In Review` إلى المالك، ولا يمنحها اعتماد المالك بنفسه.

#### العيب مقابل التغيير

- مخالفة التصميم أو Missing State أو RTL أو قص النص أو ظهور أسلوب مكتبة جاهزة هي Defect وتعالج دون إعادة فتح قرار.
- إضافة شاشة أو تغيير Navigation أو Component Contract أو الهوية أو الأولوية هي Change Request وإصدار جديد.

#### ممنوعات الإكمال الوهمي

يرفض التسليم إذا احتوى Placeholder غير موسوم، أو شاشة بلا Screen ID، أو عنصرًا بلا مصدر، أو `MISSING / UNVERIFIED` مخفيًا، أو اعتمد النظام الحالي بصريًا، أو غيّر الألوان فقط، أو استخدم Dashboard Template، أو صمم Happy Path فقط، أو أغفل ملفات الحزمة، أو وصف العمل بأنه كامل من دون أدلة.

### 21.5 Final QA Evidence and Owner Acceptance Gate

**الحالة: مغلق بقرار المالك اعتماد التسليم المرحلي**  
**النظام المعتمد:** `Micro Final Acceptance Gate`

تحدد هذه البوابة أدلة التسليم المطلوبة من المنفذ؛ لا تنشئ وحدها موافقة من المالك ولا تثبت تنفيذ الاختبارات قبل إجرائها. **تسليم التصميم الكامل القابل للتحرير مرحلة مستقلة عن التحقق الواقعي مع المستخدمين والأجهزة.** يجوز للـAgent إنجاز الحزمة وتسليمها `Ready for Owner Review` حتى لو بقي اختبار واقعي غير متاح له، بشرط تسمية كل بند `UNVERIFIED` وأثره وخطة التحقق اللاحق. لا يصف التصميم أو التجربة بأنهما `Accepted` نيابة عن المستخدم.

#### بوابة محتويات التسليم

يلزم تقديم مصدر الاستوديو داخل هذا المستودع ومعاينة حية يفتحها المالك على الهاتف مع طريقة تشغيل وبناء قابلة للتكرار، والمكونات والـTokens والشاشات والحالات وfixtures والأصول وتراخيصها، وملفات Screen Coverage وNavigation Mapping وDesign-to-Code Map وMotion/RTL/Accessibility Annotations وQA Evidence وسجلات الفجوات والتغييرات وDelivery Manifest والتقرير النهائي. يكتب أمام كل بند `Delivered` أو `Blocked` أو `Deferred by Owner` مع رابط أو مسار قابل للفتح؛ لا تعني قائمة أسماء وحدها وجود مخرجات فعلية.

#### بوابة التتبع والتغطية

- لكل شاشة ومكوّن أساسي معرف ومصدر قرار وحالة ورابط Route أو مكون في الاستوديو واختبار أو تفسير لعدم الاختبار.
- كل Route أو Layer معروف داخل نطاق V1 ممثل في Coverage Ledger أو مسجل كفجوة.
- لا يعد `Approved` مكوّنًا أو شاشة تحتوي Placeholder غير موسوم أو قيمة مالية مفترضة أو Missing State.
- لا تحول `Blocked` أو `Deferred` إلى `Approved` لتجاوز شرط التسليم؛ تذكر أثرها على جاهزية الرحلة.

#### عزل النظام القديم

يقدم `LEGACY-VISUAL-CONTAMINATION-AUDIT.md` أمثلة مقارنة للشكل الجديد والقديم ويصنف أي تشابه إلى سلوك قياسي معروف، أو قرار منصوص عليه، أو توريث بصري غير مسموح. لا يكفي الإقرار النصي `PASS` دون دليل بصري وتعليل. لا تعد البنى القياسية مثل زر الرجوع والحقول مخالفة لمجرد التشابه السلوكي.

#### بوابة الهوية والمكونات

- يفحص التكوين في غياب اللون مؤقتًا، وتكرار البطاقات، وإمكان تبديل العلامة التجارية دون فقد هوية الشاشة، ومظهر مكتبة UI الافتراضي.
- تفحص الألوان الفعلية وتباينها، والـTokens، والتسميات، وعدم وجود قيم عشوائية أو نسخ منفصلة غير مبررة من المكونات.
- هذه فحوص تشخيصية لا درجات رقمية مزيفة؛ يذكر الدليل البصري والسبب والتصحيح عند الفشل.

#### بوابة الأجهزة والتفاعل

تطبق مصفوفة `16.7` وفق مخاطر الشاشة، مع هاتف Android متوسط فعلي، و320/360/390/412 عرضًا للحالات المناسبة، وتكبير 100/150/200، وTalkBack وKeyboard وReduced Motion وOffline والضوء القوي. إذا كان iOS مدعومًا يضاف iPhone وVoiceOver. إذا تعذر اختبار جهاز أو مستخدم فعلي فلا يخترع دليل: يوضع `UNVERIFIED` معلنًا مع مسؤول وخطوة لاحقة. **هذا لا يمنع تسليم استوديو التصميم المكتمل؛ يمنع فقط ادعاء اكتمال التحقق الواقعي أو القبول النهائي للتجربة.**

#### درجات العيوب

تستخدم درجات `P0–P3` في `16.7`. يلزم `0` من `P0` و`P1` و`P2` المؤثر على رحلة حرجة قبل توصية القبول. لا تمر فجوة مالية أو RTL أو وصول حاسمة على أنها ملاحظة `P3`. تسجل `P3` مع أثرها ولا تؤثر في الفهم أو الثقة أو الوصول.

#### المراجعة والاستقلال

1. `Builder Self-Audit`: تدقيق المنفذ وإرفاق الأدلة، دون سلطة منح نفسه `Accepted`.
2. `Design Delivery`: يسلّم Agent رابط معاينة الاستوديو ومصدره والحزمة والأصول والتقارير كاملة للمراجعة، مع `UNVERIFIED` صريح للاختبارات الواقعية التي لم تحدث. هذه مرحلة تسليم صحيحة وليست قبولًا نهائيًا للتجربة.
3. `Independent Review`: مراجع مختلف عن منفذ التصميم يفحص المصادر والتسليم عند توفره؛ عدم توفره يوثق في المرحلة اللاحقة ولا يمنع وصول الملفات إلى المالك.
4. `Owner Review`: المالك يراجع الملف المقدم ويقرر قبوله كتصميم أو طلب تعديل، ويقرر لاحقًا قبول التجربة بعد التحقق الواقعي المطلوب.

#### بنية حزمة الأدلة

```text
QA-EVIDENCE/
  QA-SUMMARY.md
  DECISION-TRACEABILITY-MATRIX.csv
  SCREEN-COVERAGE.csv
  COMPONENT-INVENTORY.csv
  ASSET-MANIFEST.csv
  DEVICE-TEST-MATRIX.csv
  ACCESSIBILITY-RESULTS.md
  RTL-RESULTS.md
  MOTION-RESULTS.md
  LEGACY-VISUAL-CONTAMINATION-AUDIT.md
  TEMPLATE-SIGNATURE-AUDIT.md
  OPEN-ISSUES.md
  SCREENSHOTS/
  RECORDINGS/
```

لكل اختبار: Requirement وSource Reference وScreen/Component ID والجهاز والحالة والطريقة والنتيجة ورابط الدليل وIssue ID عند الفشل. لا تسجل `PASS` دون تجربة فعلية.

#### صلاحية التسليم للمستخدم

يؤكد المنفذ أن المالك يستطيع فتح المعاينة من هاتفه وأن المصدر وTokens وfixtures وخطوات التشغيل والبناء موجودة في المستودع، وأن روابط الأصول واللقطات والتسجيلات والتقارير تعمل. يحدد إصدار المعاينة والناقص والمؤرشف. رابط محلي في جلسة Agent أو لقطات فقط لا يعد تسليمًا. لا تنشر على خدمة خارج هذا المستودع من دون تفويض؛ إن لم تتوفر معاينة قابلة للفتح، يبقى البند `Blocked` مع بديل تشغيل محلي موثق، ولا تُدّعى جاهزية التسليم الكامل.

#### حالات الإنجاز

`Design Incomplete → Design Delivered for Owner Review → Design Revisions / Design Accepted by Owner → Real-world Validation Pending / Completed → Experience Accepted by Owner`

لا تستخدم نسب اكتمال عامة ولا تعلن اكتمال الاختبارات مع فجوات مخفية. يمكن تسليم التصميم كاملًا قبل اختبار المستخدمين، لكن يجب الفصل الصريح بين قبول ملفات التصميم وقبول التجربة الواقعية.
