# ZAI — revise the existing interactive design studio after the Micro color decision

**Target:** `Qays7753/Micro-Bold-Modular-Design-Handoff-V1`, branch `exec/interactive-design-studio-zed-20260923`, directory `interactive-design-studio/`. Update the existing candidate and commit/push a new revision to **this branch**; never force-push or touch production `Qays7753/Micro` or `main`. This brief replaces obsolete color instructions in `ZED-START-HERE-INTERACTIVE-DESIGN-STUDIO.md`. **Read `17-COLOR-DECISION-2026-09-23.md` first**, then the updated root decision file, then the studio sources/reports. The owner has fixed `#D97757` as brand lead and instructed the design lead to make other color choices; do not halt to re-ask for color approval.

**Review these existing evidence images first:** `interactive-design-studio/evidence/screenshots/fullpage-ovr_now_complete_360.png`, `interactive-design-studio/evidence/screenshots/fullpage-fin_overview_complete_360.png`, `interactive-design-studio/evidence/screenshots/fullpage-fin_overview_complete_320.png`, `interactive-design-studio/evidence/screenshots/ops-sale-create_filled_360.png`. They capture the old version and are **not** proof of the revision.

**Complete-coverage contract:** Read `19-STUDIO-COMPLETE-COVERAGE-GATE-2026-09-23.md` before editing. The three examples below receive particular composition fixes, but the required migration covers **every existing implemented component, variant, state, shell, route, deferred placeholder, visible studio control and maintained artifact**. A screen-only recolor, an uninspected component or unrevised documentation cannot satisfy this brief. Build the coverage ledger and work through every row; evidence and remaining gaps must be traceable to actual code.

## 1. What must change, not merely recolor

1. Replace the complete old Indigo/Citrus/pale-lavender visual family with the new **whole palette and its roles** in `17-COLOR-DECISION-2026-09-23.md`. Update color primitives, semantic/component tokens, state/interaction/chart colors and all rendered UI. Keep all HEX primitives in tokens, not scattered through components. Remove obsolete tokens and labels rather than leaving an invisible legacy family.
2. Recompose the three actual mobile examples for their distinct questions: `OVR-NOW` (business now and next action), `OPS-SALE-CREATE` (enter sale and understand its effect), `FIN-OVERVIEW` (cash, flows, receivables, result and completeness). Do **not** keep an identical dominant money block in Overview and Finance with a hue swap. The owner asked for living, visually expressive work, including room for intentional substantial brand-colored areas, without a generic repeated dashboard grid or pastel badge collection. Make the distinction visible even when converted to grayscale.
3. Do not replace the former single bright Indigo mass with a single dark terracotta mass repeated across screens. `#D97757` is permitted in meaningful large areas; place normal-sized detail text in dark ink or a proper light surface, and genuinely large white type only where contrast requirements actually apply. `#A94630` is the default solid action with normal white text; test the real Arabic type and all states. Never confuse color of identity/action with semantic financial outcomes.
4. Inspect all composed screen states, not just swatches. The finance screenshot previously had crowded wallet rows inside the large colored panel, with labels, dates and units wrapping awkwardly and a unit stranded on its own line. Rework hierarchy/spacing/reflow so amounts and `د.أ`, long Arabic labels, timestamps and controls remain readable on 320/360/390/412 and 150/200% text. Do not solve this by hiding essential information or relying on desktop zoom.
5. In Overview, the emphasized cash block visually dominates while the answer about the project's result appears far below. Keep cash clearly distinct from profit, but bring the **availability and completeness of the result** into the first decision area in a way appropriate to `OVR-NOW`. Critical debt or a serious data gap cannot be buried behind card swipes or later scrolling.
6. Revisit bottom navigation at narrow widths: active tab labeling, badges, safe area and fixed-bar height must not clip or squeeze Arabic labels. Keep the owner's five familiar tabs and accessible navigation behavior.
7. Keep the studio toolbar separate from the phone UI; it must not dominate visual-review evidence. Deliver one phone-sized, legible screenshot per screen/state plus a clearly labeled comparison contact sheet. If generating comparison images, render Arabic glyphs correctly: no □/tofu placeholders, unreadably tiny text or four narrow phone frames pretending to represent real phone viewing. Do not present a color board as evidence that a screen is comfortable on a phone.
8. Audit all lingering stock-template signatures: identical tinted header shapes on all three screens, generic large numerical cards, blanket colored action pills, monotonous pale surfaces and repeated margins/padding independent of content. Keep familiar controls and platform behavior; make the **composition** answer each screen's purpose. Record concrete before/after findings in `interactive-design-studio/reports/VISUAL-PROOF-REVIEW.md`.

## 2. Functional gaps already found in the review studio

Keep this limited to the independent *design studio*, not the production Micro logic.

- `SnapshotDeck.tsx` tells users «اسحب لعرض بطاقة أخرى» but implements arrow-button changes and no actual touch/pointer swipe. Support real direct RTL swipe, maintain working buttons for accessibility, and prevent its gesture from conflicting with between-page swipe.
- Check the owner's approved swipe between main pages as well: direct right/left dragging should lead to the corresponding page without reversing the physical movement. Keep taps on the five tabs and accessible alternatives; do not hijack horizontal scrolling inside a financial card.
- `OvrNow.tsx` routes «عرض الكل» to `OVR-SNAPSHOT-ALL`, which is absent from the studio route map. Give it a real in-studio all-snapshots view or truthful in-place expansion, preserving screen hierarchy and return behavior; do not leave a dead click.
- `OpsSaleCreate.tsx` initializes mode/cart/customer/notes/errors/phase/echo from props once. Switching scenario fixtures without remount/reset can show stale form values or success/error from the previous scenario. Reset deterministically on scenario changes without clobbering edits while a scenario remains active. Exercise the change path in a browser.
- Audit other visible interactive promises (selected filters, overlays, close/back, chart information, period controls) and fix any that appear clickable but are inert; log any truly deferred feature explicitly. Preserve familiar system gestures, RTL direction and reduced motion.

## 3. Evidence and handoff

- Recalculate contrast for **actual** pairs used after the token replacement: brand large white and normal dark text; action white; secondary copy; component boundaries; focus; selected and pressed states; semantic containers; overlays and chart lines. Label a failed pair `FAIL`, fix it, and regenerate `TOKEN-DICTIONARY.md/.json`, `DESIGN-SYSTEM.md`, `ASSET-MANIFEST.md`, `CONTRAST-REPORT.md` and affected catalogs/reports. The previous report's green ticks do not transfer to this palette.
- Use the same Jordanian Arabic and `0–9` + `د.أ` fixtures for before/after claims. Compare default, partial/unknown, error/recovery and offline where present. Verify on widths 320/360/390/412, at 150/200% text, with grayscale and reduced motion. Run the build and meaningful browser interactions. If a real Android/TalkBack/Jordanian-user study was not performed, mark it `UNVERIFIED`.
- Regenerate screenshots and the QA report from the **new running studio**, keep old image evidence identifiable as previous-version history if retained, and do not claim the owner visually approved the revision. The comparison should explain **why** a composed screen differs from stock templates; swapping #4F46E5 for #D97757 does not pass.
- Deliver a readable changelog of changed colors/roles, changed compositions, fixed functions, files affected, commands/tests/results, unresolved defects, preview instructions/URL if actually available, and commit hash + branch link. Update root delivery/manifest documents only where they describe current implementation; never rewrite past observations to imply they verified the new version.
- Status on delivery: `Ready for Owner Review`. Push to the dedicated branch after verification, no PR/merge unless separately asked. If push cannot be authenticated, do all local work, report `PUSH BLOCKED` with a non-secret remedy; do not print tokens or ask to paste one into tracked files.

## 4. Exhaustive coverage and acceptance

- Create and complete the source-to-UI coverage ledger described in `19-STUDIO-COMPLETE-COVERAGE-GATE-2026-09-23.md`, including every already implemented component and state, all three example screens, five-tab global shell, deferred placeholders and studio review chrome. The ledger is a living inventory: add newly found surfaces rather than treating the listed files as an exhaustive ceiling. Do not present a newly unimplemented product screen as a requirement of this color revision.
- Replace legacy roles at the foundations and component levels and inspect their **rendered uses**. Check pseudo-elements, inline SVGs/icons, active/pressed/focus/disabled/validation/loading, overlays, dividers, shadows, charts if implemented, generated files and all data fixtures that change state. Review whether a formerly Indigo-dominant component still makes sense compositionally after the color change; fix repetition wherever it occurs, not only on OVR and FIN.
- Each implemented catalog component and its implemented variants must be reachable in an existing scenario or a dedicated studio review fixture/showcase, with a recorded result. No untested hidden variant may be marked `PASS`. An intentionally deferred feature is `NOT IMPLEMENTED`, not an implemented component silently omitted from review.
- Refresh current token dictionaries, catalogs, design-system, state/navigation maps, manifests, reports and new screenshots from the updated source. Do not rewrite historical evidence as if it showed the new revision. Run the automated and browser-based acceptance gates in 19; attach the ledger and truthful failures before claiming `Ready for Owner Review`.

## 5. Stop conditions for this revision

Do not call the work complete if any implemented component or state has no reviewed ledger row or evidenced disposition; if the old Indigo/Citrus primitives or visible purple/lime UI remain; if the only distinction is recoloring; if critical Arabic or currency wraps badly on phone; if normal white text is placed on `#D97757`; if «اسحب» or «عرض الكل» remains a false promise; if the sale fixture state leaks; or if new contrast/screenshots have not been checked against the new code. A successful build alone is not acceptance.
